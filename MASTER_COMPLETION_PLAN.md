# Master Completion Plan

> Mission started: 2026-09-09. Baseline: 28% product, 25/100 production, 10/100 autonomy. A goal is COMPLETE only after its listed acceptance criteria pass with real evidence. This file records implementation status; it is not a completion claim.

## Status and Evidence Rules

Statuses: `NOT_STARTED`, `IN_PROGRESS`, `BLOCKED`, `IMPLEMENTED`, `TESTING`, `VERIFIED`, `COMPLETE`. Completion requires code integration, focused tests, integration/E2E tests where applicable, failure remediation, rerun, and code-level verification. Mocks/fixtures may test boundaries but never count as proof of a real external submission. PostgreSQL/Prisma remains authoritative; Redis/BullMQ is coordination; private S3-compatible storage holds objects; MongoDB is excluded.

## Current Baseline

| Check | Result | Evidence |
|---|---|---|
| Node/npm/Prisma | VERIFIED | Node 24.20.0; npm 11.19.0; Prisma/Client 5.22.0 |
| Typecheck | VERIFIED | 7 workspaces passed on 2026-09-10 |
| Lint | VERIFIED | ESLint passed on 2026-09-10 |
| Unit tests | VERIFIED | 10 files, 50/50 passed on 2026-09-10; live PostgreSQL/Redis tests are gated separately |
| Production build | VERIFIED | API, queue, and Vite web builds passed; 1503 modules transformed |
| Dependency audit | VERIFIED | `npm audit --audit-level=high`: 0 vulnerabilities |
| PostgreSQL | IN_PROGRESS | Disposable PostgreSQL 16.15 is reachable through the approved Docker execution path; all 41 migrations apply and the aggregate integration runner now completes 28 files/115 tests. The reused volume still emits expected negative-test constraint/RLS errors, so production database certification is not claimed |
| Docker / WSL2 | IN_PROGRESS | Docker Desktop and Compose are operational through the approved execution path; disposable PostgreSQL/Redis/Quay-MinIO services are healthy. Direct sandbox Docker access remains permission-gated |
| Rollback safety | VERIFIED | External source snapshot with recorded SHA-256; directory still has no Git history |

## Dependency Order

`G0 → G1 → G2 → G3 → G4 → G5`; then `G6/G7 → G8 → G9 → G10 → G11 → G12 → G13 → G14/G19 → G15 → G16/G17 → G18 → G20 → G21`; `G22/G23/G24/G25/G26` build on durable execution; `G27/G28/G29/G30` are continuous gates; `G31 → G32`. Security, observability, resilience, and testing are cross-cutting and must be added with each goal, then certified in their dedicated goals.

## Goal Ledger

| Goal | Status | % | Dependencies | Primary files/components | Acceptance criteria | Required tests/evidence | Current blocker |
|---|---|---:|---|---|---|---|---|
| 0 — Baseline & Repository Stabilization | COMPLETE | 100 | None | root scripts/config, UI truthfulness, `BASELINE_REPORT.md` | Toolchain/dependencies/config inspected; lint/typecheck/tests/build pass; DB connectivity verified; fake behavior removed | Static suite, live Docker/PostgreSQL checks, dependency audit, rollback snapshot | None |
| 1 — Database & Migration Foundation | COMPLETE | 100 | G0 | Prisma schema/migrations/seed | Versioned additive migrations; enums, constraints, indexes, ownership, versions, required durable models/relations; migration CI | Fresh migrate, upgrade/backfill, constraint, RLS and restore tests | None |
| 2 — Application State Machine | COMPLETE | 100 | G1 | shared contracts, transition service/API | Typed allowed graph; atomic history/audit/outbox; actor/reason/idempotency; stale-write denial | Unit + PostgreSQL transaction/concurrency tests | None |
| 3 — Outbox & Idempotency | COMPLETE | 100 | G1,G2 | outbox publisher, command service | Atomic event creation; dedupe; safe retry/redelivery/crash boundaries; no lost work | Fault-injection and duplicate-delivery integration tests | None |
| 4 — AutomationJob Execution Model | COMPLETE | 100 | G1-G3 | AutomationJob repository/service | Priority, versioned payload, attempts, availability, lease, cancellation, failure, correlation and timestamps | Claim/lease/expiry/cancel/concurrency tests | None |
| 5 — Redis/BullMQ Worker Infrastructure | COMPLETE | 100 | G3,G4 | queue package, workers, dispatcher runtime, replay service | Six named queues, producers/workers, concurrency/rate limits, deterministic retry jitter, DLQ inspection/replay, pause/resume/cancel, graceful drain, tenant-safe metrics; PostgreSQL authoritative | 14 queue semantics + 4 runtime unit tests; 13 live Redis/PostgreSQL worker tests; isolated Redis stop/restart fault test; 38 live database tests | None |
| 6 — Candidate Profile & Resume Intelligence | COMPLETE | 100 | G1 | resume parser/profile API+UI, source facts | Structured facts with exact source citations and approval workflow; unsupported claims denied | 3 parser fixture tests; 4 live provenance/approval/API tests; 2 frontend API tests; full validation | None |
| 7 — Job Discovery & Normalization | COMPLETE | 100 | G4,G5 | source adapters, discovery runs/dedupe | Durable paged runs, provenance, classified errors, retry/rate limits, durable dedupe; compliant adapters | Greenhouse/Lever/Ashby fixtures, failure/dedupe integration tests | None |
| 8 — Job Analysis AI | VERIFIED | 100 | G5,G7 | AI schemas/prompts/worker/persistence | Untrusted JD boundary; runtime schema; provider/model/prompt/schema/hash/token/cost/confidence/version persisted | Contract, malformed output, injection and worker integration evaluations | None |
| 9 — Deterministic Job Matching | COMPLETE | 100 | G6,G8 | matching engine/API/UI | Deterministic validated weighted score and evidence for all required dimensions; LLM suggestions non-authoritative | Unit vectors, boundary/missing-data tests, persistence/API tests | None |
| 10 — Resume Tailoring Engine | COMPLETE | 100 | G6,G8,G9,G19 | tailoring, claim verifier, renderer | Only approved facts; dates/companies/skills/metrics immutable unless sourced; durable PostgreSQL versions with HTML content and provenance; external object files remain a Goal 19 dependency | Truthfulness/adversarial fixtures, generation/integration tests | G19 external file/object storage |
| 11 — Deterministic ATS Engine | COMPLETE | 100 | G8,G10 | ATS scanner/contracts/UI | Reproducible keyword/skill/title/experience/parse/contact/date/format/stuffing/readability signals persisted | Golden fixtures, score stability, parser/format tests | None |
| 12 — Application Quality & Policy Gate | COMPLETE | 100 | G9-G11 | policy/rules/limits service | Persist PASS/FAIL/SKIPPED with evidence for eligibility, preferences, limits, scores, truth and documents | Rule matrix, transactional budget/concurrency tests | None |
| 13 — Application Creation & Queue | COMPLETE | 100 | G3-G5,G12 | application command/API/worker | Idempotent one active user/job application with exact artifacts/decisions/source and queued job | Duplicate/concurrent creation, API/worker integration tests | None |
| 14 — Human Verification System | COMPLETE | 100 | G2,G5,G13,G23 | verification API/records | Durable CAPTCHA/MFA/anti-bot/auth approval, expiry, audited safe resume/cancel; never bypass | Lifecycle, tenant, expiry, replay and worker-handler tests | G23 verification-specific notification delivery/UI |
| 15 — Browser Automation Foundation | COMPLETE | 100 | G4,G5,G14,G19,G27 | Playwright manager/guards/sessions | Trusted-worker-only isolated Playwright contexts; HTTPS allowlist plus every-request DNS SSRF recheck; downloads/permissions disabled; tenant-idempotent, bounded, auditable sessions with safe shutdown/recovery | URL policy, hostile DNS/navigation, isolation, startup cleanup, tenant/replay/lifecycle PostgreSQL tests | G19 documents/upload; G5 runtime integration |
| 16 — Greenhouse Automation Adapter | PARTIAL | 80 | G15,G18,G19 | Greenhouse adapter/form port/service and browser fixtures | Source-specific semantic form detection, stable field identity, approved profile/cover-letter text mapping, dynamic/multistep validation, durable question/answer/audit evidence, verification and review handoff, tenant idempotency, and exact immutable ResumeVersion-linked upload path are implemented; provider-specific adapter selection and owner-bound resume/cover-letter document access are integrated; form evidence now retains the full immutable document reference including bucket, object key, object version, encryption reference, checksum, and ResumeVersion; real Chromium fixtures now cover exact clean ResumeVersion upload, submit-control interaction, and independent confirmation parsing; full evidence and live submission remain | Focused adapter/storage/provider tests pass; Greenhouse browser fixture suite passes 15/15; disposable PostgreSQL and S3 slices pass; live provider submission remains unverified | G19 full authorization matrix and G20/G21 live submission/verification |
| 17 — Lever Automation Adapter | PARTIAL | 62 | G15,G18,G19 | provider-specific Lever port/service and fixtures | Lever now has an independent Playwright port and provider adapter selection in submission execution, with page detection, semantic/accessibility extraction including hidden/dynamic-control state, native radio/checkbox/select/combobox/multi-select transport, validation, and shared fail-closed orchestration; real Chromium coverage now verifies exact approved-document upload, submit-control interaction, independent confirmation parsing, retry-safe reinspection, and dynamic field reinspection; the shared submission service now has explicit Lever-port selection coverage; live provider evidence remains | Lever/provider focused tests pass 14/14 runnable with one database-gated case; local Chromium Lever fixtures pass when executable is available; API/job-engine typechecks, root lint, and diff checks pass; full provider integration pending | G19 documents and live PostgreSQL/Redis evidence |
| 18 — Application Form Intelligence | VERIFIED | 100 | G6,G14,G15 | `packages/job-engine/src/form-intelligence.ts`, adapters, application-answer service, `packages/database/prisma/migrations/20260914124500_application_answer_source` | Provider-neutral FormDetector/FieldExtractor/FieldMapper/AnswerResolver now formalize stable semantic/occurrence identity, duplicate-control occurrence assignment, dynamic/hidden fields, explicit ARIA accessible-name fallback propagated by both provider DOM ports, supported capabilities, current-value-aware required state, validation state including field-level snapshot errors, control kinds, fail-closed policy classes, approved answer gating, provenance-required answer resolution, deterministic newest-reviewed answer selection, explicitly approved AI suggestions, multi-step fill decisions, and a first-class durable answer source checked against provenance before provider submission; durable persistence/audit exists in the API path | 52/52 focused shared intelligence and provider-port tests pass; standard disposable PostgreSQL runner passes 98/98 across 18 files including durable answer approval/provenance and provider persistence; Prisma generation/validation, API typechecks, root lint, and diff checks pass | G19/G16 document-backed provider controls and live provider submission remain outside this form-intelligence goal |
| 19 — Document Upload & Evidence System | IN_PROGRESS | 99 | G1,G27 | `apps/api/src/services/document-storage.ts`, `apps/api/src/routes/documents.ts`, retention, resume routes, object metadata, exact-version/dedupe migrations | S3 client, private object metadata, checksum, MIME+magic/size/malware controls for PDF/DOCX/TXT/PNG/JPEG, encryption, signed access, retention/deletion, configured-bucket checks, strict private-reference validation, immutable `ResumeVersion` object linkage, runtime artifact-kind/document-type binding, authenticated tenant-scoped artifact upload for cover letters/evidence/screenshots/receipts, bounded multipart size handling, authenticated generic signed-download API, owner-authorized deletion with durable tombstone/audit, upload/retention audit events, content-addressed duplicate convergence with duplicate replay, conditional immutable object writes with exact `HeadObject` reconciliation, exact submission selection, authenticated-owner read boundary, RLS-backed download/deletion metadata access, authorized signed-download boundary, first-boundary filename validation, and expiry/type mismatch rejection are implemented; every application-linked artifact now inherits and persists the application’s exact immutable `ResumeVersion` binding; metadata persistence also fails closed when encryption provenance is absent and verified reads now require the object-store encryption headers to match that immutable mode; deletion failure is fail-closed without a false tombstone; live MinIO verifies owner-bound signed access and tamper rejection; live PostgreSQL verifies exact clean ResumeVersion application binding and foreign-tenant/mismatched-version rejection; shared API attachment now delegates to the same exact binding helper; full authorization matrix and production storage evidence remain | 71 focused storage/route/retention tests pass; real disposable S3 upload/read, concurrent dedupe, signed-access, and tamper suite passes 3/3 when available; disposable PostgreSQL runner passes 28 files/115 tests; Prisma schema validates; API typecheck/root lint/diff checks pass; full database cleanup/reproducibility remains incomplete | Production S3/KMS/scanner controls and full database cleanup |
| 20 — Submission Engine | PARTIAL | 84 | G12-G19 | submission controller | Durable explicit authorization, preflight, exact-document selection, worker claim, bounded multi-step provider execution, idempotency, and `UNCONFIRMED` handoff are implemented; preflight now records the complete canonical immutable document reference including `resumeVersionId`, and both authorization and provider execution validate it through the shared stored-document boundary before browser access; each claimed execution now creates a durable `ApplicationAttempt` with bounded structured start/success/failure/unknown-outcome evidence including the exact document reference, authenticated independent-verification endpoint is wired, and the dashboard exposes the explicit version-bound authorization action; provider-owned document transport now enforces the form-port upload boundary; stale executing authorizations are reconciled to explicit unknown outcome with durable failure evidence and no retry; real Chromium coverage now exercises the shared provider submission service against a persisted executing authorization and exact bound document | Authorization/provider unit coverage; provider tests 10/10; dispatcher tests 4/4; web API tests 8/8; disposable PostgreSQL runner passes 28 files/115 tests; S3 suite passes 3/3; live Chromium provider-service fixture passes 4/4; submission evidence integration now asserts exact `resumeVersionId`; web build/typecheck/lint pass | G19 production controls and G21 live reconciliation |
| 21 — Independent Submission Verification | PARTIAL | 80 | G20 | `apps/api/src/services/submission-verification.ts`, `apps/api/src/routes/applications.ts`, `packages/database/prisma/schema.prisma` | Structured provider evidence now requires application binding, matching provider, parser version, source, timestamp, persisted submission timestamp, a deterministically selected completed durable submission attempt, bounded clock skew, and SHA-256 evidence hash; the immutable evidence record now retains the matched attempt identity; deterministic bounded confirmation parsing, tenant-owned persistence, verifier-only state transition, exact immutable replay matching, durable audit, authenticated application verification endpoint, and production automation-handler dispatch are implemented; live PostgreSQL persistence/replay evidence now exists | 19 executable parser/API/attempt-selection tests pass; disposable PostgreSQL runner passes 28 files/115 tests including exact attempt binding and replay; live Chromium/PostgreSQL fixture passes the queued `VERIFY_SUBMISSION_CONFIRMATION` handler path; Prisma schema valid; API typecheck/lint/diff checks pass | G20 provider reconciliation and broader E2E evidence |
| 22 — Scheduler & Autonomous Discovery | PARTIAL | 78 | G5,G7,G12 | `apps/api/src/services/scheduler.ts`, `apps/api/src/services/durable-scheduler.ts`, SearchProfile schedule state, worker runtime, `apps/web/src/views/SettingsView.tsx`, application schedule route | Deterministic recurring schedule kernel supports once/hourly/every-three-hours/daily/weekly/custom five-field cron and IANA timezone matching; SearchProfile stores validated provider account configuration, timezone, and durable next/last run timestamps; transactional due-slot claiming uses PostgreSQL advisory locks and creates real discovery runs/jobs; worker runtime polls due profiles with overlap suppression and graceful shutdown; authenticated dashboard can create, pause, resume, and cancel persisted schedules; application form runs can now be scheduled into the durable `AutomationJob.availableAt` path with tenant/provider/state checks, bounded future windows, idempotency, and audit evidence | Scheduler validation suite passes 14/14; existing live PostgreSQL scheduler integration proves concurrent ONCE claim de-duplication, durable discovery-job creation, recurring next-slot advancement, and timezone handling; Prisma generation/validation, API/web typechecks, web build, lint, and diff check pass; application schedule integration remains environment-gated | G5,G7, recurring restart/limits evidence and broader scheduler operations |
| 23 — Notifications & Live Events | PARTIAL | 65 | G3,G5 | notification outbox consumer/runtime, notification API/UI, SSE, `apps/web/src/views/FailedAppsView.tsx` | Existing durable outbox consumer maps ready, uncertain, confirmed, failed, interview/offer, human-verification, and classified email-outcome events to deterministic tenant-owned notifications; authenticated SSE replays ordered durable notifications from PostgreSQL; Human Review Room now loads durable verification requests and acknowledges them through the existing safe resume flow; worker runtime now drains notification outbox events with leased retries and graceful shutdown; Redis bridge delivers normalized cross-instance events without accepting malformed/untrusted payloads | Notification consumer/runtime tests 18/18 pass; disposable PostgreSQL integration proves persisted outbox consumption, tenant-owned notification creation, and exact duplicate replay; live Redis/PostgreSQL queue runner passes 15/15 including cross-instance SSE pub/sub delivery; API/web typechecks, lint, and workspace builds pass | G3,G5, durable SSE replay/multi-instance operational evidence, email outcomes |
| 24 — Email / Application Outcome Tracking | PARTIAL | 65 | G13,G21,G27 | `apps/api/src/services/email-outcomes.ts`, `apps/api/src/routes/email-outcomes.ts`, `apps/api/src/services/email-connections.ts`, `apps/api/src/services/email-sync.ts`, `apps/api/src/routes/email-connections.ts`, `packages/database/prisma/schema.prisma` | Authenticated tenant-scoped, RLS-bound list/ingest and consent/revocation API fronts durable message-id-idempotent outcomes; concurrent duplicate delivery replays safely after unique-race rollback, inputs are bounded and timestamp-validated, raw email is not returned or persisted, explicit application ownership is enforced, hostile/unknown content remains `UNCLASSIFIED`, classified outcomes emit durable outbox notifications, consent records scopes while storing only opaque credential references, reviewers can explicitly link/unlink outcomes without lifecycle mutation, and a provider-neutral connector-injected sync runner enforces active consent/provider matching, bounded opaque cursors and message pages, hash-only ingestion, cursor persistence, and audit evidence | 26 executable classifier/sync/notification boundary tests pass; disposable PostgreSQL integration proves hash-only classified persistence, one outbox emission, exact duplicate replay, and conflicting message-id rejection; Prisma generation/validation, API typecheck, lint, and diff checks pass | OAuth/provider implementations, live mailbox fixtures, live cursor persistence, richer link-review tests, application outcome state transitions |
| 25 — Interview & Offer Lifecycle | PARTIAL | 60 | G2,G23,G24 | `apps/api/src/services/application-lifecycle.ts`, `apps/api/src/routes/applications.ts`, `apps/web/src/views/ApplicationsView.tsx`, `apps/web/src/services/api.ts`, Interview/Offer models, assessment lifecycle migration | Authenticated application endpoints expose tenant-scoped interview/offer recording and explicit offer decisions with required idempotency keys, bounded fields, date/salary validation, independent-confirmation gating for interviews, state-machine transitions, and audited durable records; application detail and dashboard render real interviews/offers, with HTTPS-only meeting links; reviewed linked actionable email outcomes can now be applied through optimistic versioning and idempotency, including the durable `ASSESSMENT` state; dashboard supports user-entered reviewed interview/offer recording and explicit submission authorization through backend APIs; concurrent duplicate source-event delivery now replays the owner/application-bound existing interview or offer record after a unique race; live PostgreSQL now proves confirmed application → interview → offer → accepted transitions with durable records, HTTPS meeting-link persistence, exact source-event replay, conflicting duplicate rejection, and audit history | 9/9 lifecycle/route boundary tests and 8/8 web API tests pass; assessment/email/state-machine focused tests pass 25/25; disposable PostgreSQL runner passes 105/105 across 23 files; API/web typechecks, production build, root lint, and `git diff --check` pass | OAuth/provider implementations and richer live mailbox/link-review coverage; migration application and broader lifecycle concurrency remain open |
| 26 — Analytics & Reporting | PARTIAL | 58 | G13,G21,G25 | `apps/api/src/routes/analytics.ts`, `apps/web/src/views/AnalyticsView.tsx` | Dashboard metrics scope discovered items to the tenant and expose authoritative lifecycle counts, provider totals/confirmed/failed, failure-reason counts, resume-version application performance, ATS score distribution, average time to independent confirmation, offer rate, rejection rate, and confirmed-submission rate; web dashboard renders these real response fields; API now exposes a tenant-scoped historical date/status funnel derived from persisted applications | Analytics aggregation tests pass 2/2; disposable PostgreSQL analytics fixture proves persisted lifecycle/provider/match/ATS/failure/timing aggregation inputs and tenant-owned historical grouping; database runner passes 106/106 across 24 files; API/web typechecks, production build, root lint, and diff checks pass | Historical UI funnel rendering, route-level authenticated live response test, scale tests |
| 27 — Security & Privacy Hardening | PARTIAL | 50 | G1 and continuous | `apps/api/src/services/durable-credentials.ts`, `apps/api/src/routes/auth.ts`, `packages/security/src/credential-vault.ts`, browser/untrusted-content policies, API auth/RLS | Existing encryption, auth, SSRF/browser, untrusted-content, file, and rate-limit boundaries remain; credentials now have a PostgreSQL-backed encrypted owner-scoped record path with versioning, trusted-worker retrieval, revocation material deletion, audit events, RLS, constrained opaque references, and durable hashed refresh-session rotation/revocation with authenticated logout | 29/29 focused security/credential tests pass; disposable PostgreSQL fixture proves encrypted credential storage, cross-tenant retrieval denial, audit records, and revocation material deletion; database runner passes 107/107 across 25 files; Prisma generation/validation, API/web typechecks, lint, full suite and build pass | OAuth/credential rotation integration, full cross-tenant matrix, production secret/KMS controls, refresh concurrency and live DB evidence |
| 28 — Observability & Operations | PARTIAL | 37 | G3-G5 and continuous | `apps/api/src/middleware/request-logger.ts`, `apps/api/src/routes/automation.ts`, `packages/queue/src/metrics.ts`, `apps/api/src/observability/structured-log.ts` | HTTP requests emit structured JSON records with bounded propagated/generated correlation IDs, status, path, and duration without query strings or payloads; authenticated automation metrics expose tenant-scoped durable job, attempt, failure-category/code, and browser-session status counts, while the queue package exposes BullMQ counts; analytics exposes authoritative outcome rates; shared logs redact/bound sensitive values, omit undefined optional fields, retain application/provider/job correlation context, and safely serialize circular metadata; deterministic alert derivation now surfaces queue backlog/failures, retry pressure, aging human verification, and excessive active browser sessions | API typecheck/lint, workspace build, alert tests 2/2, and live authenticated automation-metrics endpoint test 1/1 pass against disposable PostgreSQL with tenant-scoped empty operational state; runtime telemetry/traces/alert delivery remain gated | Full metrics/traces/queue/browser dashboards and production telemetry backend |
| 29 — Resilience / Crash Recovery | PARTIAL | 32 | G3-G5,G15,G19-G21 | `apps/api/src/services/automation-jobs.ts`, `apps/api/src/services/automation-worker.ts`, `apps/api/src/services/automation-dispatcher-runtime.ts`, `apps/api/src/services/browser-session-manager.ts`, `apps/api/src/worker.ts`, `packages/queue/src/worker.ts` | Existing durable job layer enforces owner/lease expiry, atomic retry or dead-letter transitions, delivery-generation stale-worker denial, authoritative retry preparation, replay idempotency, and graceful queue shutdown; dispatcher and scheduler shutdowns now have bounded waits and cleanup; non-Error worker failures are normalized before recovery persistence; Redis outage recovery exercises a real disposable Redis restart while PostgreSQL remains authoritative; worker startup reconciles expired or stale ACTIVE browser sessions left by a process/browser crash into durable CLOSED/EXPIRED state with audit and outbox evidence | Focused automation-worker/queue/dispatcher semantics cover stale retry, stale completion, retry failure, envelope immutability, non-Error failure, bounded shutdown, and browser-session recovery; Redis outage integration passes 1/1 after restart; browser-session integration passes 7/7; PostgreSQL database runner passes 28 files/115 tests | Integrated API/DB/browser/storage/email failure suite, PostgreSQL restart and browser crash drills; broader crash/restart evidence remains open |
| 30 — Comprehensive Testing | PARTIAL | 30 | All implemented goals | Vitest workspace suite, focused provider/security/storage tests | Full configured suite completes without unexpected failures; integration-dependent files remain explicitly gated by `DATABASE_INTEGRATION`/service availability and are not counted as passes | Fresh controlled serial run: 76 files/629 tests passed and 35 files/125 tests explicitly skipped; PostgreSQL runner 28 files/115 tests, Redis outage 1/1, S3 3/3, backup drill passed; focused suites and static checks pass | Full E2E, resilience breadth, provider execution, and CI evidence |
| 31 — Production Deployment Readiness | PARTIAL | 29 | G27-G30 | `apps/api/src/app.ts`, `apps/api/src/server.ts`, `apps/api/src/runtime-config.ts`, `apps/api/src/worker.ts`, `scripts/database-backup.mjs`, `scripts/database-restore.mjs`, `scripts/database-integrity.mjs`, `scripts/database-backup-crypto.mjs`, `scripts/run-database-backup-drill.mjs`, Compose/configuration | API separates liveness (`/api/health`) from authoritative PostgreSQL readiness (`/api/ready`); operational scripts produce authenticated AES-256-GCM encrypted custom-format backups with SHA-256 tamper-evident manifests and explicitly confirmed destructive restore; API and worker startup enforce production secrets, HTTPS CORS, database TLS, private-storage/scanner configuration, and non-local Redis TLS; HTTP shutdown closes the listener before Prisma disconnect and handles SIGINT/SIGTERM idempotently; frontend messaging reflects certified-provider boundaries | Backup-integrity/crypto tests pass 4/4; runtime config tests pass 12/12; script syntax checks, API/web typechecks, builds, lint, and diff checks pass; Prisma schema validation and live migration status verify 41 migrations are applied; disposable HTTP fixture proves `/api/health` and `/api/ready` behavior against live PostgreSQL; non-destructive Docker backup drill passes, including custom dump, AES-256-GCM encryption, SHA-256 manifest, decryption, and `pg_restore --list` readability; live deployment, drain, rollback, destructive restore drills remain gated | Docker/runtime topology and production operational evidence |
| 32 — Final Autonomous E2E Certification | IN_PROGRESS | 24 | G0-G31 | full system + certification report | A deterministic local Chromium-to-PostgreSQL certification slice exercises provider form fill, exact fixture upload, submit interaction, confirmation parsing, independent verification, durable evidence, final confirmed state, authenticated applications readback, analytics readback, durable notification-outbox consumption, a real CAPTCHA page routed into durable HumanVerification, explicit acknowledgement/resume at the recorded safe checkpoint, and the shared provider submission service against a persisted executing authorization; it is explicitly not the complete 30-step certification | Live disposable PostgreSQL + Chromium certification fixture passes 4/4, including notification/dashboard readback, CAPTCHA handoff, safe resume/replay, and authorized provider-service submit/confirmation; no external submission claimed and no CAPTCHA/MFA bypass | Full discovery→dashboard workflow, resilience/security matrix, and certification report |

## Immediate Execution Record

1. Goal 0 static baseline passed after truthfulness fixes: lint, seven workspace typechecks, 24/24 tests, dependency audit, and production builds.
2. WSL 2.7.13.0/kernel 6.18.33.2-2 and Docker Desktop 4.90.0/Engine 29.7.2/Compose 5.5.1 are operational after a clean Docker process-tree restart.
3. PostgreSQL 16.15 is healthy and reachable; `pg_isready`, an authenticated SQL query, and Prisma connectivity passed. Prisma correctly reports that no migration history exists; Goal 1 owns the initial baseline migration.
4. Fake Header notifications now use the authenticated API and mark-all-read backend; misleading resume, queue, and AI telemetry labels were corrected. Focused API tests pass.
5. A rollback snapshot was stored outside the project root with SHA-256 `7B04F071A626788F0A0E02A9A173D14FE12241D49BE13EE424241BE34EE2B86C`; Git history remains absent.
6. Goal 0 is COMPLETE. Next: implement and verify Goal 1 additive migrations, constraints, ownership/RLS, seed, restore, and migration CI. No final certification will be authored until Goal 32 passes.
7. Goal 1 is COMPLETE. The pinned pgvector PostgreSQL 16 image is healthy; the reviewed initial migration applies cleanly with pgcrypto/pgvector, typed lifecycle enums, durable execution/evidence models, database checks, ownership triggers, indexes, restricted-role RLS, and optimistic versions.
8. Deterministic synthetic seed is repeatable; `db:migrate:deploy`, `db:migrate:status`, and `db:migrate:verify` are scripted. Seven live PostgreSQL integration tests verify extensions, migration history, seed idempotency, constraints, lease invariants, restricted-role cross-tenant denial, ownership consistency, and transaction-scoped tenant context.
9. Backup/restore drill passed into a clean database with restored user rows and `pgcrypto`, `plpgsql`, and `vector`; full `npm run check` passes (24 standard tests, 7 database tests separately), production builds pass, and dependency audit remains at zero vulnerabilities. Next: Goal 2 application state machine.
10. Goal 2 is COMPLETE. The API status mutation now uses one typed transition service with an explicit allowed graph, optimistic expected-version compare-and-set, actor/reason/correlation/idempotency inputs, and atomic application/history/audit/outbox writes. `CONFIRMED` requires the independent verifier and submission moves through `SUBMISSION_PENDING → UNCONFIRMED` rather than asserting success.
11. Four graph unit tests and four live PostgreSQL transaction tests pass, including idempotent replay, rollback-safe invalid transitions, stale-write denial, and a two-writer concurrency race with exactly one winner. Full validation passes with 28 standard tests, 11 live database tests separately, all seven workspace typechecks, lint, API build, and web production build. Next: Goal 3 outbox and idempotency delivery semantics.
12. Goal 3 is COMPLETE. Versioned additive migrations add outbox leases, terminal failure state, claim-ready indexes, invariant checks, service-role cross-tenant publication policy, and restricted service-role table grants. The publisher claims with `FOR UPDATE SKIP LOCKED`, uses expiring leases, bounded exponential retry, terminal failure accounting, and stable transport idempotency keys; a send-before-mark crash deliberately redelivers rather than losing an event.
13. Durable command and consumer idempotency now use canonical SHA-256 request identity and one PostgreSQL transaction for key reservation, side effects, and stored response. Replays return the prior response, altered requests conflict, failed commands roll back both reservation and effects, and repeated consumer deliveries execute their effect once.
14. Goal 3 validation passes: two focused unit tests; 18 live PostgreSQL tests across foundation, transition, concurrent claim, retry exhaustion, crash/redelivery, consumer dedupe, conflict, and rollback scenarios; full `npm run check` with 30 standard tests, all seven workspace typechecks, ESLint, API build, and 1,503-module web build; dependency audit remains at zero vulnerabilities. Next: Goal 4 AutomationJob execution model.
15. Goal 4 is COMPLETE. The AutomationJob service creates versioned jobs with strict idempotent replay, serializes concurrent creation, claims by descending priority through `FOR UPDATE SKIP LOCKED`, increments attempts on every lease, renews only live owner leases, completes with compare-and-set ownership, retries with delayed availability, dead-letters exhausted failures and expired final-attempt leases, and safely cancels queued or leased work.
16. A versioned migration tightens exact lease and terminal timestamp invariants, adds a claim-ready priority index, and permits only the restricted service role to coordinate jobs across tenants while retaining tenant isolation for the application role. Nine live AutomationJob tests cover create/replay/conflict, concurrent creation and claims, priority, renewal, stale ownership, success, lease expiry/recovery, delayed retry, dead-letter exhaustion, database invariants, and cancellation.
17. Goal 4 validation passes: 27 live PostgreSQL tests across four files; full `npm run check` with 31 standard tests, all seven workspace typechecks, ESLint, API build, and 1,503-module web build; migration status is current and dependency audit remains at zero vulnerabilities. A mini forensic audit was initiated after Goals 0-4 as required; any concrete finding must be remediated before Goal 5 completion.
18. The Goals 2-4 mini forensic audit found ten defects: caller-forged verifier identity; a stranded final outbox lease; cancellation overwriting completion; cross-tenant automation-run attachment; three globally scoped idempotency identities; concurrent transition replay races; inert idempotency expiry; incomplete transition replay identity; and ambiguous omitted AutomationJob scheduling. All ten were remediated before Goal 5.
19. Trusted boundaries now force public transition actors to `USER`; transition replay locks and compares the complete request; transition, command, and AutomationJob keys are tenant-scoped; expired command records are replaceable; final expired outbox leases fail terminally; AutomationJob cancellation uses compare-and-set; automation runs require tenant ownership; and immutable `requestedAvailableAt` preserves creation identity independently from retry scheduling.
20. Post-remediation evidence passes on 2026-09-09: 34/34 live PostgreSQL tests across four files, including tenant reuse, expiry, crash recovery, concurrency, altered replay fields, scheduling identity, ownership, and cancellation/completion races; 32/32 standard tests across eight files; seven workspace typechecks; ESLint; API build; 1,503-module web build; six migrations current and schema valid; zero high-level dependency vulnerabilities.
21. Goal 5 is COMPLETE. BullMQ 6.3.4 and ioredis 5.11.1 coordinate six named queues while PostgreSQL remains authoritative. Attempt-specific queue identity, generation-fenced crash redispatch, lease renewal, stale-delivery rejection, deterministic bounded retry jitter, final DLQ inspection, one-time audited replay identity, cancellation, pause/resume, configured concurrency/rate limiting, queue/database metrics, graceful drain, and dispatcher reconciliation/overlap/close behavior are implemented.
22. Goal 5 fault evidence passes on 2026-09-10: 13/13 live Redis/PostgreSQL worker integration tests cover duplicate dispatch/two-worker execution, priority, delay, concurrency, rate limiting, pause/resume, cancellation, lease-expiry crash recovery, stale delivery, graceful drain, retry exhaustion, and DLQ inspection; an isolated disposable Redis container is stopped and restarted while PostgreSQL state remains authoritative, then the job dispatches and executes exactly once.
23. Goal 5 validation passes: 18/18 focused queue/runtime unit tests; 38/38 live PostgreSQL tests across five files; all eight additive migrations current and schema valid; full `npm run check` with 50/50 standard tests, seven workspace typechecks, ESLint, API/queue builds, and 1,503-module web build; zero dependency vulnerabilities. Authenticated `/metrics` now exposes only tenant-scoped PostgreSQL job counts rather than global transient queue totals. Next: Goals 6 and 7 in dependency order; the existing `ANALYZE_JOB` handler remains infrastructure evidence only and is not a Goal 8 completion claim.
24. Goal 6 is COMPLETE. Master-resume upload now deterministically extracts citation-backed candidate facts, stores exact source text and character ranges with SHA-256 identity, leaves every parser result unapproved, and replaces prior pending parser output without changing approved facts. Uploading a new master demotes the previous master atomically before creation.
25. Candidate fact reads and approve/reject decisions run in tenant-scoped PostgreSQL transactions. Approval stamps the authenticated user and timestamp; rejection removes only pending output; both decisions write durable audit records. The reusable claim gate accepts only normalized exact matches to approved fact values and rejects pending or absent claims, so parser or AI output cannot self-approve or invent candidate facts.
26. Goal 7 is COMPLETE. Greenhouse, Lever, and Ashby public posting adapters normalize fixture-backed HTTPS-only public data with paged/cursor-safe execution, compliant injected-fetch tests, source provenance, account-qualified durable identity/fingerprint dedupe, classified retry semantics, authoritative `DISCOVER_JOBS` jobs, tenant-scoped run APIs, cancellation, and normalized-item rejection records. Goal 7 evidence on 2026-09-10: 25/25 focused discovery contract/database tests, 30/30 discovery API/UI tests, 74/74 live PostgreSQL integration tests, 14/14 live Redis/BullMQ worker tests, a Redis outage/recovery test, `npm run db:migrate:verify`, full `npm run check` (187 passed, 78 separately gated), and zero high-severity dependency vulnerabilities. Next: Goal 8 job analysis AI.
27. Goal 8 is COMPLETE. `ANALYZE_JOB` now treats every job description as untrusted external data, isolates it in explicit delimiters, instructs the model to ignore embedded instructions, receives raw JSON, and rejects malformed, unexpected, hash-mismatched, or schema-invalid output before persistence. The validated versioned envelope persists analysis/schema/prompt/provider/model versions, input/output hashes, latency, token/cost accounting, confidence, and trust-boundary metadata in PostgreSQL. Evidence on 2026-09-10: 7/7 schema/agent contract tests including prompt injection, malformed JSON, and hash mismatch; 2/2 live PostgreSQL worker-handler tests; all eight workspace typechecks; and `db:migrate:verify` with 13 applied migrations. Next: Goal 9 deterministic job matching.
28. Goal 9 is COMPLETE. Matching is now a versioned deterministic weighted calculation rather than an LLM judgment. It persists all bounded dimensions, tier, matched/missing skills and technologies, reproducible profile/job hashes, and a dimension-level evidence record; a tenant-scoped `MATCH_JOB` automation job is idempotently queued from the job API and worker-executed with heartbeats. Evidence on 2026-09-10: 3/3 deterministic matching vectors (repeatability, gaps, missing data), 18/18 focused route/handler tests, 1/1 live PostgreSQL persistence test, `db:migrate:verify` with 14 applied migrations, and full `npm run check` with 194 passed tests and all builds/typechecks/lint passing. Next: Goal 10 resume tailoring engine.
29. Goal 10 is COMPLETE for durable truthfulness and versioning. `TAILOR_RESUME` is a tenant-scoped, idempotent automation job that requires approved source facts, treats job descriptions as untrusted data, strictly validates cited model output, rejects uncited/pending/altered dates, companies, credentials, metrics, and skills, and persists a PostgreSQL `ResumeVersion` with HTML content, job-description hash, claim provenance, and audit evidence. External binary/object-file storage remains explicitly deferred to Goal 19. Evidence on 2026-09-10: resume-tailoring contract/injection/malformed-output tests, existing claim-verifier adversarial fixtures, durable live PostgreSQL tailoring persistence/isolation fixtures (gated), 196 standard tests passing (79 integration-gated), all workspace typechecks, ESLint, production builds, and `db:migrate:verify` with 14 applied migrations. Next: Goal 11 ATS validation.
30. Goal 11 is COMPLETE. The former model-backed ATS analyzer has been replaced with a versioned deterministic scanner that produces bounded, repeatable keyword, required-skill, title, experience, formatting, section, parsing, contact, date, quantitative-achievement, readability, and keyword-stuffing evidence. `EVALUATE_ATS` is tenant-scoped, idempotently queueable, heartbeat-protected, persists the full evidence into `ResumeVersion`, and writes a durable audit event. Evidence on 2026-09-10: 3/3 ATS golden/stability/format fixtures, full suite at 199 passing standard tests (82 gated), workspace typechecks, ESLint, production builds, and `db:migrate:verify` with 14 migrations current. Next: Goal 12 application quality and policy gate.
31. Goal 12 is COMPLETE. Deterministic versioned quality evaluation now persists immutable PASS/FAIL/SKIPPED evidence, fails closed on missing job/resume/ATS/match/provenance evidence, applies profile and user-rule policy, uses same-user/day advisory locks with durable application-bound budget reservations, and transitions DISCOVERED applications through the audited state machine. Evidence on 2026-09-10: 4/4 quality rule-matrix unit tests, 3/3 live PostgreSQL quality integration tests including daily-cap concurrency, 203 standard tests passed with 85 integration-gated skips, and schema/migration/typecheck/lint/API/queue/web build verification.
32. Goal 13 is COMPLETE. Authenticated `POST /api/applications` now creates exactly one tenant-owned user/job application from an active job, tenant-owned fact-verified resume version, deterministic ATS evidence, job match, and active search profile. It serializes by tenant/job and idempotency key, atomically creates the audit/outbox evidence plus a versioned `EVALUATE_APPLICATION_QUALITY` durable command, and safely replays only an identical request. Evidence on 2026-09-10: 3/3 API boundary tests, 3/3 live PostgreSQL creation/replay/concurrency/ownership tests, full standard suite (205 passed, 88 integration-gated skips), full database suite (77 passed), Prisma migration verification, typecheck, lint, and all workspace builds. Next: Goal 14 human verification system.

33. Goal 14 is COMPLETE. Tenant-scoped CAPTCHA/MFA/anti-bot/auth verification records are durable and idempotent, expire safely, pause applications through the audited state machine, and can be acknowledged or cancelled only by their owner. Acknowledgement persists no credential material, creates exactly one versioned `RESUME_APPLICATION_AFTER_VERIFICATION` command, and resumes only to the immutable safe `FORM_FILLED` or `READY_TO_SUBMIT` handoff status; it never bypasses the verification itself. Authenticated read/list/resolve/cancel APIs are available at `/api/human-verifications`; requests remain trusted worker-only service operations. Evidence on 2026-09-11: 3/3 live PostgreSQL lifecycle/replay/tenant/expiry tests, focused handler tests, 207 standard tests passing with 91 integration-gated skips, 80/80 PostgreSQL integration tests, 16 applied migrations, all workspace typechecks, ESLint, and production builds. Existing application `WAITING_FOR_USER` transition notification remains the notification delivery mechanism; verification-specific notification/UI delivery is deferred to Goal 23. Next: Goal 15 browser automation foundation.

34. Goal 15 is COMPLETE. The trusted-worker-only browser session manager uses a reusable Playwright browser with a fresh context per session, no downloads, no granted permissions, and a one-hour bounded lifetime. It persists tenant-idempotent opaque session references, application ownership linkage, correlation/provenance, lifecycle audit records, and outbox events; shutdown closes live contexts and records durable closure. Every requested URL is HTTPS/allowlist/credential/literal-IP checked through the shared browser policy and DNS-resolved before launch, explicit navigation, and every browser-originated request (including redirects and subresources), rejecting empty, unavailable, private, loopback, link-local, reserved, multicast, and unspecified results. Startup failures now close with a durable `STARTUP_FAILED` reason rather than falsely marking expiry. No public browser-start route, credential persistence, CAPTCHA/MFA bypass, or form/submission behavior was added. Evidence on 2026-09-11: 3/3 isolated unit tests for context isolation, private-DNS denial, hostile request denial, explicit navigation revalidation, and startup cleanup; 2/2 PostgreSQL persistence/replay/ownership/lifecycle tests; 17 migrations deployed and verified; full unit suite (210 passed, 93 gated), PostgreSQL suite (82 passed), ESLint, API typecheck, and workspace production builds passed. Next: Goal 16 Greenhouse automation adapter.
35. Goal 18 milestone: the provider-neutral form layer was tightened without rebuilding provider adapters. Stable identity now prefers extracted semantic keys and optional stable occurrence keys, excludes DOM control ids, and supports re-rendered/reordered forms. Approved answers require owner/question identity, positive version, explicit approval timestamp, and non-cover-letter provenance; approved narrative/control answers can be resolved, while AI suggestions remain ineligible. The shared fill loop now handles approved select, combobox, multi-select, radio, checkbox, and text values, excludes hidden/dynamic-invisible controls, and blocks advancement on any unfilled required control. Hostile credential-oriented labels remain in the existing human-verification path. Evidence: 28/28 focused form-intelligence, Greenhouse, and Lever tests pass; workspace typecheck is still running/being finalized. G18 remains PARTIAL pending durable integration evidence and G19-backed upload flow.
36. Goal 18 follow-through: API orchestration now exposes a provider-neutral `ProviderApplicationService`; Greenhouse remains a compatibility entry point and Lever uses the shared orchestration with its own Lever DOM port. This removes the Lever service's direct dependency on a Greenhouse-named class without duplicating policy logic. Goal 19 inspection found existing S3/object metadata, integrity, scan, signed URL, and retention implementation; corrected S3-managed encryption to use AES256 while customer-managed keys use KMS. Evidence: 38/38 focused form and document-storage tests pass; API/job-engine typechecks and ESLint pass. PostgreSQL-backed adapter tests remain environment-gated because localhost:5432 is unreachable, and Docker cannot connect to its daemon; no integration result is claimed.
37. Goal 17/G19 incremental milestone: Lever extraction now recognizes radio, combobox, and multi-select controls and resolves accessible names via `aria-labelledby`; shared orchestration remains provider-neutral. Document reads, deletes, and signed URLs now reject metadata pointing at a bucket other than the configured private bucket, adding a storage-boundary check. Focused validation remains the evidence source; full G17/G19 completion is not claimed until provider fixtures and disposable S3/PostgreSQL integration run.
38. G18/G19 continuation: shared assessments now expose deterministic supported capabilities (`FILL_TEXT`, `SELECT_OPTION`, `SET_CHECKED`, `UPLOAD_DOCUMENT`, `REVIEW_ONLY`) and merge snapshot/DOM validation errors before step advancement. Added additive migration `20260914100000_goal19_exact_document_version` linking `ObjectMetadata` to an immutable `ResumeVersion`; submission preflight and provider execution prefer that exact linked object. Added disposable MinIO plus private bucket initialization to local Compose and documented `S3_ENDPOINT`. Evidence: Prisma schema valid, 40/40 focused form/Lever/document tests pass, API/job-engine typechecks pass. The attempted combined database typecheck command correctly exposed that the database workspace has no typecheck script; this is a command-surface issue, not a failed TypeScript check. Docker/PostgreSQL/S3 integration remains unverified because the Docker daemon is unavailable.
39. G19 continuation: `DocumentStorage.storeArtifact` now supports the required artifact classes (approved/tailored resumes, cover letters, evidence, screenshots, receipts) under tenant-scoped private prefixes while `storeResume` remains backward-compatible. Evidence: 13 document-focused tests and 2 shared-form tests pass; API/job-engine typechecks pass. No object-store integration claim is made.
40. G19 evidence hardening: stored object references must use the private prefix and a valid SHA-256 checksum; configured-bucket boundaries are enforced for reads, deletes, and signed URLs. Resume persistence now writes a `DOCUMENT_UPLOADED` audit event, and retention deletion writes `DOCUMENT_DELETED_RETENTION` evidence. Focused storage/retention/form tests pass 37/37 and API/job-engine typechecks pass. Integration remains gated by the unavailable Docker daemon/PostgreSQL/S3 runtime.
41. G19 duplicate/selection hardening: artifact object keys are now content-addressed by tenant, artifact kind, and SHA-256, and PostgreSQL enforces one metadata row per owner/kind/checksum. Submission preflight and provider execution require a private key, valid checksum, clean approved resume artifact, matching owner, and exact `ResumeVersion` linkage (with the existing source-resume compatibility path). Evidence: provider-submission plus document-storage tests pass 17/17 executed (3 integration tests skipped/gated), API typecheck and lint pass. No S3 or PostgreSQL integration result is claimed.
42. G19 metadata persistence: added tenant-scoped `persistDocumentMetadata`, which rejects objects whose content-addressed key is owned by another user, stores only object metadata in PostgreSQL, records provenance/audit evidence, and safely replays an existing owner/kind/checksum identity. Focused storage/retention/form tests pass 35/35; API typecheck and lint pass. Real duplicate-race and S3/DB behavior remain environment-gated.
43. G19 concurrency hardening: unique-identity races are now re-read in a fresh tenant transaction after the losing transaction exits, avoiding queries against an aborted transaction and converging concurrent metadata persistence safely. Evidence: 14/14 storage/retention tests and API typecheck pass; infrastructure integration remains unverified.
44. G19 upload-path consolidation: resume upload persistence now calls the shared `persistDocumentMetadataInTransaction` helper inside the existing tenant transaction, eliminating the prior duplicate metadata-write implementation while retaining atomic resume/object metadata creation and audit evidence. API typecheck and lint pass; focused storage/application route tests had 15/15 executable tests pass, while the Greenhouse database test remains gated by localhost:5432 being unreachable.
45. G19 metadata-boundary hardening: the shared transaction helper now validates the tenant path, private object shape, artifact kind, MIME/extension agreement, bounded size, and SHA-256 format before any metadata write. Provider/document-storage/retention focused tests pass 19/19; API typecheck and lint pass. S3/PostgreSQL integration is still unverified because Docker remains unavailable.
46. G19 application reference hardening: added `ApplicationDocument.objectMetadataId` with an additive foreign key/index and a transaction-scoped attachment helper that requires owner, clean scan status, exact application resume-version identity, and idempotent replay. Document metadata persistence is now used by the resume upload transaction. Evidence: document-storage tests pass 14/14; API typecheck and ESLint pass. Real DB/S3 integration remains gated.
47. G16/G19 integration milestone: Greenhouse form processing now loads the exact application `ResumeVersion` object, rejects legacy/non-exact objects for file controls, re-verifies clean/non-expired metadata, reads through `DocumentStorage.readVerified`, and uploads bytes only through the provider form port. Evidence: 26/26 executable provider/storage/adapter tests pass, API typecheck and lint pass. PostgreSQL-backed service execution remains gated by unavailable localhost:5432.
48. G19 object-integrity milestone: S3 uploads now include the base64 SHA-256 `ChecksumSHA256` header in addition to persisted checksum metadata and verified reads. Focused document/provider/form tests pass 40/40; API typecheck and ESLint pass. S3 integration remains unverified because Docker is unavailable.
49. G19 integration coverage: added an environment-gated real S3-compatible suite covering upload, checksum-verified readback, concurrent content-addressed duplicate identity, and deletion. Current execution reports 14 passed unit tests and 2 S3 tests skipped because `S3_INTEGRATION`/endpoint configuration is unavailable; skipped tests are not counted as passed. API typecheck and ESLint pass.
50. G19 lifecycle hardening: added `storeAndPersistArtifact`, which supports every artifact kind with optional resume/version provenance, persists authoritative metadata, returns replay state, and compensating-deletes the object if metadata persistence fails. Evidence: storage/provider/retention tests pass 21/21; API typecheck and ESLint pass. Real database/object-store execution remains gated.
51. G16 exact-document upload coverage: exported the provider-orchestration helper and added focused tests proving only a clean object linked to the selected `ResumeVersion` is uploaded, while wrong-version, pending/deleted, and missing references fail closed. Evidence: 5/5 focused tests pass; API typecheck and ESLint pass. This does not change G16/G19 completion status; real PostgreSQL/S3 execution remains gated.
52. G17 Lever browser-fixture milestone: added independent local Chromium fixtures for approved profile fields, select/radio controls, hostile custom questions, and CAPTCHA handoff. Fixed Lever DOM extraction so its field-kind classifier is serializable inside Playwright page callbacks. Evidence: 4/4 real Chromium fixtures pass; Lever port unit tests pass. PostgreSQL-backed service execution and exact document upload remain gated/deferred.
53. G19 private-reference hardening: deletion and signed-download operations now reject non-private/non-content-addressed keys before touching storage, closing an authorization boundary that previously protected reads but not destructive or signed-access paths. Evidence: combined storage, exact-upload, and Lever-fixture tests pass 24/24; API typecheck and ESLint pass. S3/PostgreSQL integration remains environment-gated.
54. G20 inspection milestone: the existing submission implementation is materially beyond `NOT_STARTED`—it performs explicit user authorization, durable preflight checks, exact approved-document verification, provider execution, idempotent claiming, and deliberately stops at `UNCONFIRMED`. The plan now records G20 as PARTIAL 55%; G21 remains unimplemented, so no submission is treated as confirmed.
55. G18 duplicate identity hardening: the shared `FieldExtractor` now assigns deterministic occurrence keys only when otherwise-identical semantic controls repeat, so durable question identities remain distinct without relying on DOM ids or global field indexes. Evidence: 22/22 form-intelligence tests pass; API typecheck and ESLint pass. G18 remains PARTIAL because cross-provider durable integration and document-backed controls are still G19-dependent.
56. G19 authorization boundary: added `DocumentStorage.readAuthorized`, which validates the authenticated owner against the content-addressed private object path before issuing a storage read; mismatched tenant/user references fail without touching S3. Evidence: combined form-intelligence and document tests pass 38/38; API typecheck and ESLint pass. S3/PostgreSQL integration remains gated by the unavailable Docker daemon.
57. G21 verification seam: added a verifier-only `verifySubmission` service that rejects incomplete, mismatched, unhashed, or unversioned evidence; accepts only `UNCONFIRMED` applications; transitions through the existing `VERIFIER` state-machine boundary; stores confirmation identity and durable audit evidence; and idempotently replays the same confirmation. Evidence: 5/5 validation tests pass; live PostgreSQL transition/reconciliation evidence remains gated.
58. G21 build verification: the new independent verification service compiles with the API and the full workspace production build passes, while no live confirmation is claimed because PostgreSQL integration remains unavailable.
59. G21 parser hardening: added bounded confirmation-page parsing for Greenhouse/Lever evidence markers and identifiers, hashing raw page text without persisting it; hostile or marker-only content is rejected. Evidence: 9/9 parser/evidence tests pass; API typecheck and lint pass. Live database reconciliation remains gated.
60. G21 provenance hardening: verification now requires the evidence provider to match the application job source and rejects future-dated evidence beyond a five-minute clock-skew allowance before any state transition. Evidence: 10/10 verification tests pass; API typecheck and lint pass.
61. G21 evidence durability: added the tenant-owned `SubmissionVerificationEvidence` model and additive migration with application/hash uniqueness; `verifySubmission` now records structured evidence in the same transaction as the verifier-only confirmation transition and audit event. Evidence: Prisma schema validation, API typecheck, lint, and full workspace build pass; live migration/reconciliation remains gated.
62. G21 tenant isolation: added RLS and restricted-role grants for `submission_verification_evidence`, matching the existing tenant boundary so the new evidence store cannot bypass PostgreSQL isolation. Evidence: Prisma schema validation, 10/10 verification tests, API typecheck, and lint pass; live migration/RLS checks remain gated.
63. G21 immutable evidence replay hardening: verification now compares every immutable evidence attribute when the same application/hash is replayed and rejects metadata drift instead of silently accepting it. Evidence: 10/10 verification tests pass; API typecheck and lint pass. Live database race/replay evidence remains gated.
64. G22 scheduler kernel: added deterministic next-run calculation for ONCE/HOURLY/DAILY/WEEKLY/CUSTOM schedules with validated five-field cron, bounded search, and IANA timezone matching. Evidence: 6/6 scheduler tests pass; API typecheck and lint pass. Durable schedule persistence and restart integration are not yet implemented.
65. G22 durable slot state: added SearchProfile `timeZone`, `nextRunAt`, and `lastRunAt` fields plus an additive migration; `claimDueSearchSchedules` claims due profiles transactionally under tenant scope and advisory locks, advances recurring slots, and deactivates one-shot schedules. Evidence: Prisma generation/validation, API typecheck, lint, and 6/6 scheduler tests pass. Discovery-run/job creation and live persistence remain incomplete.
66. G22 build verification: the durable schedule migration/schema and scheduler service compile with the API, full workspace production builds pass, Prisma schema validation passes, and `git diff --check` is clean. Provider account configuration and live scheduler persistence remain incomplete.
67. G22 discovery orchestration: added validated `discoveryAccounts` to SearchProfile and wired due schedule claims to atomically create provider-specific discovery runs and authoritative `DISCOVER_JOBS` automation jobs using the existing discovery transaction path. Malformed or account-less profiles fail closed without advancing their schedule. API typecheck, lint, Prisma validation, and diff checks pass; live scheduler concurrency/restart evidence remains gated.
68. G22 worker runtime: wired durable schedule polling into the existing automation worker with configurable bounded interval, non-overlapping ticks, error reporting, and graceful shutdown before dispatcher/worker closure. API typecheck, lint, and full workspace build pass; live multi-instance scheduler evidence remains gated.
69. G22 input boundary: SearchProfile validation now constrains schedule names, CUSTOM cron requirements, non-CUSTOM cron usage, IANA timezone values, and provider/account configuration before persistence. API typecheck and lint pass; no existing search-profile focused test file was present, so no test result is claimed for that nonexistent target.
70. G22 validation coverage: added focused SearchProfile tests for valid provider-account/custom-timezone configuration and invalid schedule, cron, timezone, and provider-account inputs. Evidence: 6/6 validation tests pass; API typecheck and lint pass.
71. G23 notification coverage: extended the existing outbox consumer to map `READY_TO_SUBMIT`, `UNCONFIRMED`, and `human-verification.requested` events to deterministic durable notifications, adding explicit application-ready, submission-uncertain, CAPTCHA/MFA, and generic human-verification types. Evidence: notification consumer tests pass 15/15; API typecheck and lint pass. Live outbox/SSE delivery remains unverified.
72. G23 SSE replay: the authenticated SSE endpoint now resolves `Last-Event-ID` within the requesting tenant, replays durable notifications in timestamp/id order, emits SSE event/id fields, and reports replay unavailability explicitly without fabricating events. Full workspace build, API typecheck, and lint pass; live reconnect/multi-instance evidence remains gated.
73. G23 replay ordering hardening: SSE connections now buffer live broadcasts while durable replay is in progress, then flush the buffer after the replay window, preventing interleaving/loss during reconnect. API typecheck, lint, and full workspace build pass; live multi-instance delivery remains unverified.
74. G24 email outcome boundary: added a tenant-owned `EmailOutcome` model with message-id uniqueness, application ownership linkage, indexes, RLS, and restricted-role grants. The ingestion service stores only SHA-256 hashes plus parser metadata, applies bounded deterministic classification for receipt/rejection/assessment/interview/recruiter/offer/withdrawal outcomes, rejects invalid tenant/application references, and audits accepted ingestion; unknown or hostile instruction content remains `UNCLASSIFIED` and does not alter application state. Evidence: 7/7 focused tests pass, Prisma generation/validation, API typecheck, and lint pass. OAuth/provider consent, live mailbox ingestion, and lifecycle transition integration remain unimplemented/gated.
75. G25 lifecycle boundary: added explicit tenant-scoped `recordInterview` and `recordOffer` services using the existing audited application state machine, requiring confirmed applications for interviews and source-event idempotency for replay-safe records. Added nullable source-event identities, indexes, and RLS/grants for interview/offer tables. Evidence: Prisma generation/validation, API typecheck, lint, and workspace build pass. Email-driven linking, remaining lifecycle states, API/UI surfaces, and live PostgreSQL concurrency evidence remain incomplete/gated.
76. G26 analytics boundary: corrected the dashboard's discovered-job count to use tenant-owned discovery items instead of globally counting active jobs, and added authoritative lifecycle status counts plus provider totals/confirmed/failed metrics from application/job state. API typecheck and ESLint pass; live SQL/UI coverage and richer historical dimensions remain incomplete/gated.
77. G28 logging boundary: request logging now emits structured JSON with a bounded trusted correlation header or generated UUID, response status, route path without query parameters, and duration; request payloads and credentials are not logged. API typecheck and ESLint pass; centralized metrics/tracing and alert drills remain unimplemented.
78. G27 credential isolation: the existing encrypted credential vault now requires a bounded owner identity on every store/retrieve/update/delete/list operation and denies cross-owner access, preventing identifier-only secret access. Evidence: 27/27 security/browser/untrusted-content tests pass, security typecheck, and ESLint pass. The vault remains process-local and is not production-ready until durable audited storage and rotation/revocation are implemented.
79. G30 suite boundary/evidence: two database-dependent provider service tests were corrected to use the repository's `DATABASE_INTEGRATION=1` gate, eliminating false failures when PostgreSQL is unavailable while preserving execution in integration mode. Full `npm test` now reports 46 passed files, 25 explicitly skipped files, 319 passed tests, and 107 skipped tests; skipped integration coverage remains unverified.
80. G31 readiness boundary: added `/api/ready` as a real PostgreSQL probe returning 503 with `not_ready` when the authoritative database is unavailable, while retaining `/api/health` as liveness. API typecheck and ESLint pass; deployment/runtime readiness and backup/restore evidence remain gated by unavailable Docker/PostgreSQL infrastructure.
81. G24 API boundary: added authenticated `/api/email-outcomes` list and ingest endpoints with bounded message fields, valid timestamp enforcement, explicit tenant ownership, and response minimization. Evidence: 12/12 focused classifier/API tests pass, API typecheck, and ESLint pass. OAuth consent/revocation, mailbox provider adapters, and live database execution remain incomplete/gated.
82. G25 API boundary: added authenticated `/api/applications/:id/interviews` and `/offers` endpoints requiring idempotency keys and bounded validated fields; application detail now returns offers. Routes call the existing audited lifecycle services, so interviews still require independent application confirmation. Evidence: 8/8 focused route tests pass, API typecheck, and ESLint pass. Live lifecycle persistence/concurrency and remaining outcome states remain incomplete/gated.
83. G21 API boundary: exposed authenticated `POST /api/applications/:id/verify-submission` for structured provider evidence. The route delegates to the existing verifier-only service, so it cannot confirm non-`UNCONFIRMED` applications, accepts no page text, and preserves provider/hash/timestamp/tenant checks and idempotent replay. API typecheck and ESLint pass; live PostgreSQL reconciliation remains gated.
84. G24 consent boundary: added tenant-owned `EmailConnection` persistence with provider/account uniqueness, scope bounds, active/revoked status, credential-reference-only storage, revocation that clears the reference, audit events, RLS, and restricted-role grants. Evidence: Prisma generation/validation, API typecheck, lint, and 12/12 email boundary tests pass. Provider OAuth flows and live mailbox synchronization remain unimplemented.
85. G24 consent API: exposed authenticated `/api/email-connections` list/grant/revoke endpoints; responses omit credential references and revocation clears them transactionally with audit evidence. API typecheck, lint, Prisma generation/validation, and 12/12 email boundary tests pass. OAuth authorization-code exchange and live mailbox synchronization remain unimplemented.
86. G26 dashboard wiring: extended the existing analytics view/types to render backend-provided lifecycle status counts and provider total/confirmed/failed metrics, preserving empty-state behavior when dimensions are absent. Web typecheck, production build, API lint, and full ESLint pass; live SQL/UI and scale evidence remain incomplete.
87. G24 reference hardening: email consent now rejects slash/token-like credential material and accepts only bounded opaque references, with focused validation coverage. Evidence: 15/15 focused classifier/API/consent tests pass, API typecheck, and ESLint pass. OAuth exchange and live mailbox synchronization remain unimplemented.
88. G25 frontend evidence: application detail now renders persisted interviews and offers, and only HTTPS meeting URLs are exposed as links. Web typecheck, production build, API lint, and diff checks pass; lifecycle mutation controls and live persistence tests remain incomplete.
89. G27 durable credential path: added encrypted PostgreSQL-backed `CredentialRecord` storage with tenant RLS, owner/name uniqueness, versioned replacement, trusted-worker-only retrieval, revocation that clears encrypted material, and audited operations. Opaque owner/reference validation prevents raw token-shaped references. Evidence: 29/29 focused security/credential tests pass, Prisma generation/validation, API typecheck, lint, and diff checks pass. Live database concurrency and production key-management evidence remain gated.
90. G30 current-suite verification: after the latest email/credential/schema changes, `npm test` completed with 49 files passed, 25 explicitly skipped, 329 tests passed, and 107 skipped. No unexpected failures remain; the skipped set remains environment-gated integration coverage and is not treated as certification evidence.
91. G31 operational scaffolding: added `db:backup` and guarded `db:restore` scripts using PostgreSQL custom-format dumps; restore requires `CONFIRM_DATABASE_RESTORE=YES`, while credentials are supplied through `PGPASSWORD` rather than persisted in files. `node --check`, API typecheck, lint, and diff checks pass. No backup/restore execution is claimed because PostgreSQL/Docker is unavailable and restore is destructive.
92. G25 offer decisions: added an explicit offer decision endpoint for ACCEPTED, DECLINED, WITHDRAWN, or EXPIRED outcomes. Decisions are tenant-scoped, require an existing pending offer, transition the application through the existing state machine, and audit the mutation; email classification does not invoke this path. API typecheck, lint, and 3/3 application route tests pass. Live persistence/concurrency evidence remains gated.
93. G31 shutdown hardening: `server.ts` now exports a managed HTTP startup path that closes the listener before disconnecting Prisma, handles duplicate SIGINT/SIGTERM signals idempotently, and does not bind a port in test mode. API typecheck, ESLint, and diff checks pass; live drain and deployment evidence remain gated.
94. G23 frontend handoff: replaced the static Human Review Room disclaimer with authenticated loading of durable human-verification records and an acknowledge-completion action routed through the existing API; copy explicitly states CAPTCHA/MFA are never bypassed. Web typecheck, production build, ESLint, and diff checks pass; live verification notification/resume tests remain gated.
95. G25/G26 frontend data-path correction: the application-detail API mapper now preserves persisted interviews and offers instead of silently discarding those fields before rendering; a frontend API test covers both records. Evidence: 5/5 web API tests pass, web typecheck, and ESLint pass.
96. G24 explicit linking: added authenticated `PATCH /api/email-outcomes/:id/link` for reviewer-controlled application mapping or unlinking. It validates tenant ownership, audits previous/new identities, and carries an explicit `lifecycleMutation: false` marker; no email classification can change application status. API typecheck, lint, and 12 focused email tests pass; live database linking remains gated.
97. G20 durable attempt evidence: authorized submission claims now create a tenant/application-scoped `ApplicationAttempt`; provider activation, unknown outcome, and deterministic failure paths update that attempt with bounded structured evidence, timestamps, correlation/authorization identifiers, and terminal status. The application still remains `UNCONFIRMED` until the independent verifier confirms it. API typecheck passes; live PostgreSQL attempt/recovery execution remains gated.
98. G17 provider orchestration separation: submission execution now invokes `LeverApplicationAdapter` for Lever and `GreenhouseApplicationAdapter` for Greenhouse while retaining the same provider-neutral intelligence and fail-closed policy. Focused Lever/provider tests report 11 passed and 1 explicitly skipped fixture; API typecheck and root lint pass.
99. G19 signed-download authorization: added an owner-bound signed URL service boundary that validates private content-addressed identity, checksum, size, MIME, and authenticated owner before presigning; the resume route now supplies the complete verified metadata to that boundary. Document-storage tests pass 16/16, API typecheck and root lint pass; live S3 authorization/expiry evidence remains gated.
100. Regression verification: full configured suite completed with 49 files passed, 25 explicitly skipped, 330 tests passed, and 107 skipped; workspace typechecks, production builds, root lint, and `git diff --check` pass. Skipped tests remain PostgreSQL/Redis/S3/Docker-gated and are not certification evidence.
101. G18 provenance boundary: shared `ApprovedApplicationAnswer` now supports explicitly approved AI suggestions while requiring non-array provenance for every resolved non-profile answer; unapproved drafts still resolve to nothing. Focused form-intelligence tests pass 22/22, job-engine typecheck and root lint pass. Durable database-backed cross-provider execution remains gated.
102. G19 authorization test coverage: added focused coverage proving owner-bound signed-download requests reject cross-tenant references before presigning. Combined G18/G19 focused tests pass 39/39; API and job-engine typechecks pass. Docker/PostgreSQL/S3 integration remains unavailable and explicitly gated.
103. G16/G17 adapter boundary: the durable provider application loop now selects `LeverApplicationAdapter` for Lever and `GreenhouseApplicationAdapter` for Greenhouse while retaining shared document/validation/review orchestration. Focused provider tests pass 4/4 with 2 database-backed files explicitly skipped; API typecheck, root lint, and diff checks pass.
104. Increment build verification: after the provider-boundary and provenance changes, all configured workspace production builds pass and `git diff --check` is clean. This is static evidence only; G16/G17/G19 remain partial/in-progress pending live database/object-storage and full recovery/submission evidence.
105. G16 exact-version hardening: Greenhouse and authorized provider submission paths now refuse the mutable master-resume fallback and require the object metadata directly attached to the selected `ResumeVersion`, with matching version identity before browser upload. Focused provider/document tests pass 10/10 (2 database-backed files skipped); API typecheck, root lint, and diff checks pass. Live PostgreSQL/S3 execution remains gated.
106. G20 authorization consistency: `authorizeSubmission` now enforces the same immutable `ResumeVersion.objectMetadata` requirement as provider execution, eliminating a preflight/execution mismatch that could otherwise authorize a master-resume fallback. Focused provider/document tests pass 10/10 with 3 submission integration tests explicitly skipped; API typecheck, root lint, and diff checks pass.
107. G19 destructive-access boundary: added `deleteAuthorized`, requiring authenticated owner, private content-addressed reference, checksum, MIME, and size validation before object deletion; retention continues using the separate trusted system deletion path. Document/provider focused tests pass 23/23; API typecheck, root lint, and diff checks pass. Live S3 authorization remains gated.
108. G20 multi-step execution: authorized provider submission now processes up to ten shared-adapter form steps, rejects unsafe/non-advancing/over-limit steps, handles a single exact resume upload across the flow, fails closed on validation or human-verification controls, then requires a valid final submit control. Provider submission tests pass 6/6; API typecheck, root lint, and diff checks pass. Live provider/database recovery evidence remains gated.
109. G21 replay integrity: already-confirmed applications now replay verification only when a persisted evidence record matches every immutable field, including evidence hash, parser version, source, provider, and observed timestamp; same confirmation IDs without matching evidence now conflict. Executable verification/API tests pass 13/13; API typecheck, root lint, and diff checks pass. Live PostgreSQL reconciliation remains gated.
110. G23/G24 email notification path: classified email outcomes now emit an idempotent `email.outcome.detected` outbox event (unknown/hostile content emits no notification), and the existing notification consumer maps it to a tenant-scoped review notification without mutating application lifecycle state. Focused email/notification tests pass 23/23; API/types typechecks, root lint, and diff checks pass.
111. G24 tenant/idempotency hardening: email outcome ingestion now runs through `withTenant`, applying the same RLS transaction boundary as adjacent services, and safely re-reads an existing message after a unique-constraint race instead of surfacing a duplicate-delivery failure. Focused email/notification tests pass 23/23; API typecheck, root lint, and diff checks pass.
112. G26 analytics expansion: dashboard analytics now derive tenant-scoped failure-reason counts and per-`ResumeVersion` application/confirmed/failed performance from PostgreSQL state, and the web dashboard renders both dimensions without placeholder metrics. API/web typechecks, web production build, root lint, and diff checks pass; live SQL/UI evidence remains gated.
113. G26 quality/time dimensions: analytics now derive ATS score buckets and average time from `appliedAt` to valid independent `confirmedAt` timestamps, and the dashboard renders those values from the backend response. API/web typechecks, web production build, root lint, and diff checks pass; live SQL/UI evidence remains gated.
114. G26 analytics coverage expansion: backend and dashboard now expose authoritative ATS score buckets and average time-to-confirmation in addition to lifecycle, provider, failure, and resume-version dimensions. API/web typechecks, web production build, root lint, and diff checks pass; live SQL/UI evidence remains gated.
115. G27 token revocation boundary: added durable tenant-isolated refresh-token sessions storing only SHA-256 token hashes, rotating sessions on refresh, rejecting revoked/expired/unknown refresh tokens, and exposing authenticated logout with audit evidence. The web session now retains the refresh token only in session storage and revokes it on sign-out. Prisma generation, workspace typechecks, and root lint pass; live PostgreSQL migration/concurrency/revocation evidence remains gated because local PostgreSQL/Docker is unavailable.
116. G28 metrics expansion: the authenticated automation metrics endpoint now exposes tenant-scoped durable automation-job, application-attempt, failure-category/code, and browser-session status counts from PostgreSQL. API typecheck, root lint, full tests (49 passed files; 25 explicitly skipped; 334 passed tests; 107 skipped), and workspace build pass; live SQL/queue/browser metric evidence remains gated.
117. G28 RLS boundary: moved the expanded automation metrics aggregation into `withTenant`, so all job/attempt/failure/browser-session groupings execute under the same PostgreSQL tenant context as the surrounding durable services. API typecheck and root lint pass; live cross-tenant/RLS metric evidence remains gated.
118. G24/G25 reviewed lifecycle boundary: added an explicit authenticated `POST /api/email-outcomes/:id/apply` action requiring a linked outcome, expected application version, idempotency key, correlation ID, and the audited state machine; only reviewed rejection/interview/offer/withdrawal classifications can mutate lifecycle state, while receipt/assessment/recruiter/unknown outcomes remain non-mutating. Focused mapping tests pass; live lifecycle persistence remains gated.
119. G23/G25 dashboard evidence: application detail now loads and renders the latest tenant-owned email outcome classifications, confidence, and timestamps from the backend alongside existing attempts, interviews, and offers; no synthetic lifecycle status is introduced.
120. G23/G25 UI verification: after replacing an unsupported `replaceAll` call with the workspace-compatible string operation, web/API typechecks, root lint, focused web/email tests (19/19), and `git diff --check` pass. Live email/lifecycle persistence remains gated.
121. G19 filename hardening: stored-object metadata now rejects empty, oversized, and control-character filenames, while signed download headers use a sanitized basename to prevent response-header injection/path leakage. Focused storage tests cover unsafe names; S3/DB integration remains gated.
122. G19 filename verification: replaced the control-character regex with explicit character-code validation to satisfy the repository lint policy. Document-storage tests pass 21/21; API typecheck, root lint, and `git diff --check` pass. Live S3/DB evidence remains gated.
123. G31 production configuration gate: HTTP startup now validates the port universally and fails closed in production when PostgreSQL, JWT/encryption secrets, private object storage, or malware scanning configuration is missing/weak. Added focused runtime-configuration tests; deployment/runtime drills remain gated.
124. G31 configuration verification: corrected the startup log to use the validated port variable. Runtime-configuration tests pass 4/4; API typecheck, root lint, and `git diff --check` pass. Live deployment/readiness and shutdown drills remain gated.
125. G31 worker configuration gate: worker startup now fails closed in production unless PostgreSQL, Redis, private object storage, and malware scanning are configured, and validates the production Redis URL before creating workers. Added focused worker configuration tests; live runtime/deployment drills remain gated.
126. G31 worker configuration verification: worker/server focused tests pass 7/7; API typecheck, root lint, and `git diff --check` pass. No live worker/Redis or deployment readiness claim is made because infrastructure remains unavailable.
127. G29 submission crash recovery: added a bounded, advisory-lock-protected reconciler for stale `EXECUTING` submission authorizations. It records failure/audit evidence, moves the application to `UNCONFIRMED` when still pending, marks the attempt/authorization `OUTCOME_UNKNOWN`, and forbids automatic retry or false confirmation; dispatcher runs it before queue dispatch. Focused dispatcher tests and static verification remain required; live crash/DB evidence is gated.
128. G29 recovery verification: dispatcher runtime tests pass 4/4; API typecheck, root lint, and `git diff --check` pass after stale-submission reconciliation. Live PostgreSQL/Redis crash-drill evidence remains gated.
129. G21 evidence-boundary hardening: verification now validates provider/source enums and bounded confirmation/parser identities at runtime, then rejects evidence observed before the persisted submission timestamp. Focused verification tests cover malformed boundary values; live verifier/database evidence remains gated.
130. G21 verification evidence: submission-verification tests pass 13/13; API typecheck, root lint, and `git diff --check` pass after runtime evidence hardening. Live independent-provider/database reconciliation remains gated.
131. G27 refresh-session hardening: refresh now requires an active user, detects expired/revoked sessions, and revokes the entire refresh-token family with audit evidence when a revoked token is replayed. Existing token formats remain unchanged; live rotation/reuse concurrency evidence remains gated.
132. G27 token verification: security JWT tests pass 5/5; API typecheck, root lint, and `git diff --check` pass after refresh-session hardening. Live database-backed rotation/reuse evidence remains gated.
133. G21/G23 uncertain-outcome notification boundary: provider unknown outcomes and stale worker reconciliation now emit an idempotent `submission.outcome.unknown` outbox event when no lifecycle transition event exists; the tenant notification consumer maps it to an explicit `SUBMISSION_UNCERTAIN` notification. Notification consumer tests pass 16/16; workspace typechecks, root lint, and `git diff --check` pass. Live outbox delivery and crash-drill evidence remain PostgreSQL/Redis-gated.
134. Regression verification: full configured suite passes with 51 files passed, 25 explicitly skipped, 356 tests passed, and 107 skipped; workspace production build and `git diff --check` pass. Skipped database/Redis/S3/Docker suites remain gated and are not treated as certification evidence.
135. G18 identity hardening: the provider-neutral field extractor now derives an identity for every visible field and assigns duplicate occurrence keys by deterministic semantic ordering rather than DOM position, preserving identities across reordered controls. Focused form-intelligence tests pass 23/23; job-engine typecheck, root lint, and `git diff --check` pass. Cross-provider browser/database persistence remains gated.
136. G18 approved-answer boundary: application-answer persistence now validates answer sources and provenance shape at runtime, and forces the authoritative source tag to win over caller-supplied provenance fields. AI suggestions remain unapproved until explicit user approval. API typecheck, root lint, and `git diff --check` pass; database approval integration remains gated.
137. G18 workflow API: added authenticated tenant-scoped GET/POST/PATCH application-answer endpoints for reviewing questions, saving unapproved drafts, and explicitly approving or rejecting drafts with runtime value/source validation and optimistic version checks. Route tests pass 3/3; API typecheck, root lint, and `git diff --check` pass. Database-backed approval and cross-provider browser evidence remain gated.
138. G17 Lever extraction: Lever’s provider-specific DOM port now preserves native input types and stable `data-field`/autocomplete semantic keys for shared classification and identity resolution. Lever port tests pass 2/2; API/job-engine typechecks, root lint, and `git diff --check` pass. Full Lever browser/document/database execution remains gated.
139. G17/G18 regression: Lever semantic extraction remains green after the approved-answer API addition; focused Lever tests pass 2/2, API typecheck and root lint pass, and no live provider submission or storage evidence is claimed.
140. G22 schedule contract alignment: durable scheduling and profile validation now support the already-advertised `EVERY_3_HOURS` schedule, calculated as the next three-hour boundary in the configured IANA timezone. Scheduler/profile tests pass 12/12; API typecheck, root lint, and `git diff --check` pass. PostgreSQL persistence/concurrency and restart evidence remain gated.
141. G23 durable delivery runtime: the worker now starts a bounded notification outbox loop that claims events through the existing leased publisher, delivers through the tenant-idempotent notification consumer, suppresses overlapping ticks, and drains cleanly on shutdown. Runtime tests pass 2/2; API typecheck, root lint, and `git diff --check` pass. Live PostgreSQL/Redis outbox delivery remains gated.
142. G31 runtime configuration documentation: documented scheduler, notification-outbox, and worker concurrency tuning variables in `.env.example`, matching the bounded worker startup controls. Static validation remains green; deployment/runtime drills remain gated.
143. G25 lifecycle validation: interview and offer services now enforce bounded text, valid dates/rounds/salary, and HTTPS-only meeting URLs at the service boundary, protecting non-HTTP callers as well as API routes. Focused lifecycle validation tests pass 4/4; API typecheck, root lint, and `git diff --check` pass. Database lifecycle concurrency evidence remains gated.
144. G25 offer-decision idempotency: offer-decision API calls now require a bounded `Idempotency-Key`, and the key is incorporated into the audited state-machine transition boundary. Lifecycle/route tests pass 8/8; API typecheck, root lint, and `git diff --check` pass. Database concurrency/replay evidence remains gated.
145. G25 frontend lifecycle controls: the authenticated dashboard now exposes real Accept/Decline controls for pending offers, sends an idempotent decision request, and refreshes the application from the backend before rendering the resulting lifecycle state. Web API tests pass 5/5; web typecheck, production build, root lint, and `git diff --check` pass. Live lifecycle persistence remains gated.
146. G25 reviewed email-outcome controls: the dashboard now exposes an authenticated action for linked actionable email outcomes, sends the persisted application version plus idempotency/correlation metadata, and refreshes authoritative application state after the audited mutation. The application detail contract now includes version and outcome ownership identity. Web API tests pass 6/6; web/API typechecks, web production build, root lint, and `git diff --check` pass. Live PostgreSQL lifecycle persistence and concurrency evidence remain gated.
147. Regression verification: after the G25 reviewed email-outcome UI integration, the full configured Vitest suite completed with 53 files passed, 25 explicitly skipped, 365 tests passed, and 107 skipped. No unexpected failures occurred; skipped PostgreSQL/Redis/S3/Docker coverage remains gated and is not certification evidence.
148. G25 lifecycle client contract: added authenticated web API methods for recording interview and offer events, each carrying a fresh idempotency key and preserving backend-returned records. Focused web API tests pass 7/7; web typecheck, root lint, and `git diff --check` pass. Live lifecycle persistence remains gated.
149. G25 lifecycle recording UI: application detail now provides bounded user-entered interview and offer forms, invokes the existing audited tenant-scoped endpoints, and reloads persisted state after success. Web typecheck, production build, root lint, and `git diff --check` pass; live PostgreSQL lifecycle persistence/concurrency evidence remains gated.
150. G20/G25 explicit authorization UI: the authenticated application dashboard now exposes the irreversible submission authorization only for `READY_TO_SUBMIT` records, binds the request to the persisted application version, sends idempotency/correlation metadata, and refreshes state after the backend accepts it. Web API tests pass 8/8; web typecheck, production build, root lint, and `git diff --check` pass. Live submission execution and PostgreSQL/Redis evidence remain gated.
151. G17 native radio transport: Greenhouse and Lever Playwright ports now fall back from `selectOption` to checking the exact named/value radio control, allowing the shared adapter’s supported radio classification to execute safely without provider duplication. Provider port/fixture tests pass 10/10; API/job-engine typechecks, root lint, and `git diff --check` pass. Live provider/document/database execution remains gated.
152. G26 outcome-rate analytics: the tenant-scoped analytics endpoint now derives offer, rejection, and independently confirmed-submission rates from persisted application statuses, and the dashboard renders those authoritative rates. API/web typechecks, web production build, root lint, and `git diff --check` pass. Live SQL/UI and scale evidence remain gated.
153. G26/G28 verification: outcome-rate analytics changes pass API and web typechecks, web production build, root lint, and `git diff --check`; no live SQL or scale evidence is claimed.
154. G22 schedule controls: the authenticated settings UI now loads tenant-owned SearchProfile schedule state and pauses/resumes profiles through the existing persisted `isActive` API path, displaying the server-provided next-run timestamp. Web API tests pass 9/9; web typecheck, production build, root lint, and `git diff --check` pass. Live scheduler persistence/concurrency remains gated.
155. G22 schedule cancellation: settings now exposes an explicit tenant-scoped cancellation action for persisted SearchProfile schedules, removing the profile only after the backend confirms deletion. Web API tests pass 10/10; web typecheck, production build, root lint, and `git diff --check` pass. Live scheduler persistence/concurrency remains gated.
156. G22 schedule creation: settings now creates provider-backed SearchProfiles with bounded name, role, city, provider account, and schedule inputs through the existing authenticated API, then renders the persisted record. Web API tests pass 11/11; web typecheck, production build, root lint, and `git diff --check` pass. Live scheduler persistence/concurrency remains gated.
157. G19 upload-boundary hardening: document upload validation now rejects empty, oversized, or control-character filenames before scanning or object storage, aligning direct artifact uploads with persisted metadata safety. Document-storage tests pass 25/25; API typecheck, root lint, and `git diff --check` pass. Live S3/DB integration remains gated.
158. G19 artifact coverage: the shared private storage validator now supports PNG and JPEG magic-byte validation for screenshot and receipt artifacts while retaining the same checksum, size, malware-scan, encryption, and private-reference controls. Document-storage tests pass 27/27; API typecheck, root lint, and `git diff --check` pass. Live S3/DB integration remains gated.
159. G19 artifact upload API: added authenticated `/api/documents/upload` for approved cover-letter, application-evidence, screenshot, and receipt kinds. The route validates/scans/stores through the existing private storage service, persists metadata transactionally, optionally binds evidence to an owned application, audits the binding, and returns no binary content. API typecheck, root lint, and `git diff --check` pass; live S3/DB execution remains gated.
160. G19 multipart boundary verification: wrapped the artifact upload middleware with explicit 413 handling for oversized multipart documents and generic malformed-upload rejection. Focused document/storage and artifact-route tests pass 30/30; API typecheck, root lint, and `git diff --check` pass. Live S3/DB authorization, persistence, and object-integrity execution remain gated by unavailable Docker/PostgreSQL/S3 infrastructure.
161. G19 signed-download API: added authenticated `GET /api/documents/:id/download-url`, filtering metadata by owner, clean scan status, and non-deleted state before invoking the existing checksum/owner-bound presigning service. Artifact-route tests pass 5/5; combined document tests pass 32/32; API typecheck, root lint, and `git diff --check` pass. Live S3/DB authorization and signed-URL expiry remain gated.
162. G19 owner deletion API: added authenticated `DELETE /api/documents/:id`, requiring owner-scoped live metadata and the existing authorized storage deletion boundary before recording a durable `deletedAt` tombstone and audit event. Artifact-route tests pass 6/6; combined document tests pass 33/33; API typecheck, root lint, and `git diff --check` pass. Live S3/DB deletion and retention execution remain gated.
163. G19 RLS enforcement: generic artifact download and owner deletion metadata reads/updates now execute through the existing `withTenant` transaction context, preserving application-level ownership checks while reusing database tenant isolation. Artifact-route tests pass 6/6; API typecheck, root lint, and `git diff --check` pass. Live cross-tenant RLS and object-store deletion evidence remain gated.
164. G19 deletion failure verification: added route coverage proving a failed object-store deletion does not update metadata or emit a deletion audit event. Artifact-route tests pass 7/7; combined document tests pass 34/34; API typecheck, root lint, and `git diff --check` pass. Live S3/DB failure recovery remains gated.
165. G19 duplicate replay verification: added artifact-route coverage proving a duplicate content-addressed upload returns the existing immutable application-document reference with HTTP 200 and does not create a second binding. Artifact-route tests pass 8/8; combined document tests pass 35/35; API typecheck, root lint, and `git diff --check` pass. Live concurrent S3/DB duplicate execution remains gated.
166. G19 concurrent-upload correction: changed the artifact route to use the existing race-safe `persistDocumentMetadata` boundary and to validate application ownership before object storage, preventing a unique-identity race from deleting a shared immutable object. Artifact-route tests pass 8/8; API typecheck, root lint, and `git diff --check` pass. Live concurrent S3/DB execution remains gated.
167. G19 exact-binding hardening: application-document attachment now validates the runtime artifact-kind/document-type pairing, full private metadata integrity, owner scope, and expiry before creating the binding. Storage and route tests pass 35/35; API typecheck, root lint, and `git diff --check` pass. Live database/RLS and S3 evidence remain gated.
168. G19 exact-binding test verification: added focused tests for artifact/document-type mismatch and expired document references; both fail closed before application-document creation. Combined storage and artifact-route tests pass 37/37; API typecheck, root lint, and `git diff --check` pass. Live database/RLS and S3 evidence remain gated.
169. G16/G19 evidence integration: Greenhouse form-step audit records now include the exact ResumeVersion-linked object metadata identity, checksum, kind, filename, MIME, size, scan state, and version binding without storing binary content. Greenhouse tests report 5 passed and 1 explicitly skipped integration file; API typecheck, root lint, and `git diff --check` pass. Live durable evidence persistence remains database-gated.
170. G19 immutable deletion hardening: metadata persistence now rejects reusing a previously deleted content-addressed identity, preventing a reupload from resurrecting stale metadata or returning a deleted reference. Combined storage and artifact-route tests pass 38/38; API typecheck, root lint, and `git diff --check` pass. Live concurrent S3/DB verification remains gated.
171. G19 race-winner integrity: the concurrent unique-identity replay path now compares the winning metadata’s bucket, key, version, MIME, size, checksum, and clean scan state before replaying; mismatched or deleted winners fail closed. Combined storage and artifact-route tests pass 38/38; API typecheck, root lint, and `git diff --check` pass. Live concurrent S3/DB verification remains gated.
172. G20 exact-document authorization evidence: submission authorization now persists the immutable object ID, artifact kind, private bucket/key/version, MIME, size, checksum, and scan state; provider execution refuses authorization if any recorded reference differs from the current ResumeVersion object. Provider submission tests pass 7/7; API typecheck, root lint, and `git diff --check` pass. Live database mutation/recovery remains gated.
173. G20 attempt evidence propagation: durable execution-start and provider-activation attempt logs now carry the bounded exact document reference from authorization, preserving auditability without storing binary content. Provider submission tests pass 7/7 with 3 database-backed tests explicitly skipped; API typecheck, root lint, and `git diff --check` pass. Live attempt persistence remains PostgreSQL-gated.
174. G20 failure-path evidence: failed and unknown/stale submission attempt logs, failure records, and audit details now retain the same bounded exact document reference, preserving recovery traceability without binary content. Provider/dispatcher tests pass 11/11; API typecheck, root lint, and `git diff --check` pass. Live attempt persistence and crash recovery remain PostgreSQL/Redis-gated.
175. G20 shared metadata validation: authorization preflight and provider execution now invoke the central stored-document validator for approved resume artifact kinds, rejecting invalid MIME/size/private-reference metadata before authorization or browser startup. Provider/storage tests pass 37/37; API typecheck, root lint, and `git diff --check` pass. Live database/S3 execution remains gated.
176. G20 validator regression: provider submission coverage now proves matching authorization/current-object references are still rejected when their MIME declaration is internally invalid, before any browser session starts. Provider tests pass 8/8; API typecheck, root lint, and `git diff --check` pass. Live database/S3 execution remains gated.
177. G21 submission-timestamp gate: independent verification now refuses to confirm an `UNCONFIRMED` application that lacks a persisted submission attempt timestamp, preventing standalone provider evidence from creating a false submission lifecycle. Submission-verification tests pass 13/13; API typecheck, root lint, and `git diff --check` pass. Live database verification remains gated.
178. G18 accessible-name support: the shared field contract, stable identity, profile mapping, duplicate occurrence handling, and hostile-content fingerprinting now support explicit ARIA/native accessible names when visible labels are absent. Form-intelligence tests pass 24/24; combined provider/intelligence tests pass 31/31; job-engine typecheck, root lint, and `git diff --check` pass. Cross-provider persistence remains gated.
179. G18 provider propagation: Greenhouse and Lever DOM ports now preserve explicit `aria-labelledby`/`aria-label` values as `accessibleName` while retaining semantic label fallback, so shared identity and mapping receive accessibility data from real provider extraction. Focused port/intelligence tests pass 30/30; API/job-engine typechecks, root lint, and `git diff --check` pass. Cross-provider browser persistence remains gated.
180. G18 accessible extraction verification: provider-port fixtures now assert ARIA accessible names survive Greenhouse and Lever DOM normalization into the shared field contract. Focused port/intelligence tests pass 30/30; API/job-engine typechecks, root lint, and `git diff --check` pass. Live Chromium/provider persistence remains gated.
181. G18 accessible-name identity verification: shared question identity now prefers a stable ARIA accessible name over a volatile visible-label wrapper, while retaining label fallback; provider DOM ports propagate `aria-label`/`aria-labelledby` semantics into the shared field contract. Focused form-intelligence, Greenhouse, and Lever tests pass 32/32; job-engine typecheck, root lint, and `git diff --check` pass. Cross-provider durable persistence remains gated.
182. G17/G18 custom-combobox extraction: Greenhouse and Lever DOM ports now include ARIA combobox controls, preserve `data-field` identities for interaction, and extract currently rendered listbox option text into the provider-neutral `COMBOBOX` contract. Focused provider/intelligence/adapter tests pass 33/33; API and job-engine typechecks, root lint, and `git diff --check` pass. Live Chromium/provider execution remains gated.
183. G21 attempt-bound evidence: independent verification now deterministically binds each confirmation to the latest completed durable `UNCONFIRMED`/`OUTCOME_UNKNOWN` execution attempt that precedes the observed evidence, persists that attempt identity, and rejects immutable evidence-hash replays with a different attempt binding. Submission-verification tests pass 19/19; Prisma schema validation, API typecheck, root lint, and `git diff --check` pass. Live migration/transition and provider reconciliation remain PostgreSQL-gated.
184. G17 Lever dynamic-control hardening: Lever DOM extraction now records explicit hidden state and zero-layout controls, allowing shared form intelligence to exclude dynamic controls that are not currently actionable while retaining their semantic identity for later reinspection. Lever port and shared intelligence tests pass 34/34; API/job-engine typechecks and diff checks pass. Live provider execution remains fixture/infrastructure-gated.
185. G17 Lever document transport coverage: the provider-specific Lever Playwright port now has executable regression coverage proving the exact filename, MIME type, checksum-bearing payload, and bytes reach the native file control boundary. Lever port tests pass 7/7; API/job-engine typechecks and `git diff --check` pass. Live provider execution remains fixture/infrastructure-gated.
183. G17 custom-combobox fixture verification: Lever coverage now exercises a rendered ARIA combobox with `data-field` identity and listbox options, confirming the provider port emits a selectable shared control without relying on DOM position. Focused provider/intelligence/adapter tests pass 34/34; API and job-engine typechecks plus `git diff --check` pass. Live Chromium/provider execution remains gated.
184. G17 local Chromium fixture coverage: added a real local Lever browser fixture for an ARIA combobox with a multi-step-compatible semantic identity and rendered listbox options; the shared adapter leaves the unknown custom choice for review and blocks required advancement. Fixture suite passes 5/5; Docker/PostgreSQL/Redis-backed provider persistence remains gated.
185. G16/G17 upload-selector hardening: exact resume upload now recognizes provider `data-field` controls, while all provider interaction selectors restrict that fallback to actual form controls so validation-error containers cannot create strict-mode collisions. Greenhouse/Lever Chromium fixtures and provider-submission tests pass 26/26; API typecheck, root lint, and `git diff --check` pass. Live document/browser persistence remains gated.
186. G18 dynamic-option identity hardening: stable question identity no longer embeds rendered option text, so lazy/dynamic option changes do not create a new question identity; duplicate occurrence handling remains semantic and deterministic. Shared form-intelligence tests pass 26/26; job-engine typecheck, root lint, and `git diff --check` pass. Durable cross-provider approval persistence remains gated.
187. Regression verification: after the dynamic identity and provider-selector changes, the full configured suite passes with 54 files passed, 25 explicitly skipped, 396 tests passed, and 107 skipped; all workspace production builds and `git diff --check` pass. PostgreSQL/Redis/S3/Docker-gated tests remain unverified and are not counted as certification evidence.
188. G19 direct-upload owner boundary: document storage now validates owner identifiers before malware scanning or S3 access, rejecting whitespace/path-separator/control-character identities before any external side effect. Document-storage tests pass 31/31; API typecheck, root lint, and `git diff --check` pass. Live S3/DB authorization remains gated by the unavailable Docker daemon.
189. G21 verification-request boundary: independent submission verification now validates owner, application, and bounded correlation identifiers before entering the tenant transaction, preventing malformed request metadata from reaching durable state transitions. Submission-verification tests pass 14/14; API typecheck, root lint, and `git diff --check` pass. Live database reconciliation remains gated.
190. G19/G20 worker document authorization: provider submission now reads the exact ResumeVersion-linked object through the owner-bound `readAuthorized` storage contract, re-enforcing authenticated ownership at the binary-read boundary before browser upload. Provider-submission tests pass 8/8; API typecheck, root lint, and `git diff --check` pass. Live S3/DB execution remains gated.
191. G20 authorization binding: provider execution now scopes the `EXECUTING` authorization lookup to both the authenticated owner and the supplied application ID, preventing a valid authorization from being reused against another application. Provider-submission tests pass 8/8; API typecheck, root lint, and `git diff --check` pass. Live database authorization/concurrency evidence remains gated.
192. G16/G19 Greenhouse worker boundary: exact approved resume uploads now use the owner-bound `readAuthorized` storage contract before handing bytes to the Greenhouse/Lever form port, aligning form completion with the same tenant authorization enforced by provider submission. Greenhouse document/application focused tests report 5 passed and 2 database tests skipped; API typecheck, root lint, and `git diff --check` pass. Live persistence remains gated.
193. Regression verification: after routing both provider form-completion and submission-worker document reads through owner authorization, the full configured suite passes with 54 files passed, 25 explicitly skipped, 398 tests passed, and 107 skipped; all workspace production builds and `git diff --check` pass. PostgreSQL/Redis/S3/Docker-gated tests remain unverified.
194. G16/G19 artifact-kind enforcement: Greenhouse exact resume upload now accepts only `RESUME_SOURCE`, `RESUME_APPROVED`, or `RESUME_TAILORED` metadata kinds in addition to owner, scan, expiry, and ResumeVersion checks; unsupported artifacts fail closed before storage reads. Greenhouse document-upload tests pass 6/6; API typecheck, root lint, and `git diff --check` pass. Live persistence remains gated.
195. G20 repeated-step protection: provider submission now tracks semantic `stepIdentity` values and fails closed when a provider re-renders the same step without advancing, preventing duplicate field processing and repeated submission-side effects. Provider-submission tests pass 9/9; API typecheck, root lint, and `git diff --check` pass. Live provider recovery evidence remains gated.
196. G22 scheduler input hardening: durable schedule claims now reject oversized account payloads, cap total provider accounts, and deduplicate repeated provider accounts before creating discovery work. Scheduler tests pass 7/7; API typecheck, root lint, and `git diff --check` pass. Live multi-tenant scheduler concurrency remains gated.
197. G23 durable job-discovery notifications: successful/partial discovery completion now atomically upserts an idempotent `job.discovery.completed` outbox event when jobs are created or updated; the existing tenant notification consumer maps it to `JOB_DISCOVERED` without synthetic activity. Job-discovery and notification tests pass 27/27; API typecheck, root lint, and `git diff --check` pass. Live outbox delivery remains PostgreSQL/Redis-gated.
198. G23 review-required notification coverage: existing provider `application.review-required` outbox events now map to durable `USER_APPROVAL_REQUIRED` notifications with tenant, application, provider, blocking-field, and validation-error context; malformed payloads fail closed. Notification tests pass 18/18; API typecheck, root lint, and `git diff --check` pass. Live outbox delivery remains PostgreSQL/Redis-gated.
199. G21 durable-attempt verification: independent submission verification now requires a completed persisted `ApplicationAttempt` in `UNCONFIRMED` or `OUTCOME_UNKNOWN` state, timestamped no later than observed provider evidence, in addition to `appliedAt`; synthetic lifecycle timestamps cannot create confirmation. Submission-verification tests pass 15/15; API typecheck, root lint, and `git diff --check` pass. Live database reconciliation remains gated.
200. G24 mailbox synchronization boundary: added a provider-neutral connector-injected sync runner that requires active tenant-owned consent, matches the consented provider, bounds and validates opaque cursors/messages, ingests through the existing hash-only idempotent classifier, and persists the next cursor plus audit evidence. Email-sync boundary tests pass 5/5; live OAuth/provider mailbox synchronization and PostgreSQL cursor persistence remain gated.
201. Build verification: the API, web production bundle, and queue package all build successfully after the G24 synchronization boundary; live PostgreSQL/Redis/S3 and external mailbox-provider evidence remains gated by the unavailable local infrastructure and absent provider credentials.
202. G25 duplicate-delivery hardening: interview and offer recording now replay the tenant/application-bound existing record after a concurrent unique source-event race, while unrelated database errors and cross-application collisions remain fail-closed. Lifecycle boundary tests, API typecheck, root lint, and production build remain required verification.
203. G25 verification: lifecycle/route boundary tests pass 9/9, API and web production builds plus typechecks pass, root lint and `git diff --check` pass. PostgreSQL-backed concurrent lifecycle evidence remains gated and is not counted as complete.
204. G26 historical funnel analytics: added a tenant-scoped `/api/analytics/applications-funnel` endpoint and pure UTC date/status aggregation, preserving only counts and lifecycle labels rather than application content. Analytics aggregation tests pass 2/2; API typecheck, root lint, and `git diff --check` remain required, while live SQL/UI analytics evidence is gated.
205. G26 build verification: API, web production bundle, and queue package build successfully with the historical funnel endpoint; live database query, scale, and dashboard rendering evidence remain gated.
206. G26 dashboard integration: web API/types and Analytics view now load and render the persisted historical lifecycle funnel alongside existing real metrics. Web API coverage adds the authenticated funnel contract; live SQL/UI rendering evidence remains gated.
207. G26 frontend verification: web API tests pass 12/12, web typecheck and production build pass, root lint and `git diff --check` pass; live browser/database rendering evidence remains gated.
208. G26 metric correctness: response-rate aggregation now includes persisted rejection, interview, offer, accepted, and withdrawal lifecycle outcomes; interview/offer rates include progressed terminal states where appropriate. Analytics policy tests pass 3/3; live SQL/UI evidence remains gated.
209. G26 metric verification: analytics policy tests pass 3/3, API typecheck and root lint pass, and `git diff --check` is clean. Live SQL/UI evidence remains gated; no completion claim is made.
210. G29 retry safety: shared BullMQ backoff now validates positive attempt/base inputs, normalizes non-finite jitter, caps exponential growth at 24 hours, and guarantees a finite positive delay. Queue semantics tests pass 15/15; live Redis restart/recovery evidence remains gated.
211. G29 verification: queue typecheck and build pass, root lint and `git diff --check` pass, and queue semantics tests pass 15/15. Redis restart, worker crash, and end-to-end recovery evidence remain gated.
212. G31 backup integrity: database backup now emits a SHA-256 sidecar manifest, and destructive restore requires and verifies the matching manifest before invoking `pg_restore`; tampered or mismatched artifacts fail closed. Backup-integrity tests are added under the configured Vitest roots; live backup/restore drills remain gated pending execution.
213. G31 verification: backup-integrity tests pass 2/2, backup/restore scripts pass Node syntax checks, API typecheck and root lint pass, and `git diff --check` is clean. No live backup or destructive restore was executed.
214. G31/G32 UI truthfulness: removed stale frontend claims that form submission is unavailable; dashboard and jobs/application empty states now describe the implemented certified-provider, explicit-approval, exact-document, and human-verification boundaries without claiming unrestricted submission. Web typecheck/build and UI test verification remain required.
215. G31/G32 UI verification: web typecheck and production build pass, root lint and `git diff --check` pass after correcting stale submission messaging. Live browser/dashboard E2E remains gated.
216. Regression verification: configured full Vitest suite passes with 57 files passed, 25 explicitly skipped, 417 tests passed, and 107 skipped. The skipped PostgreSQL/Redis/S3/Docker integration set remains unverified and is not counted as certification evidence.
217. Completion-ledger reconciliation: G30/G31 rows now reflect the latest 57/417 full-suite evidence, current backup-manifest controls, and truthful frontend/provider readiness boundaries; `git diff --check` passes. G32 remains NOT_STARTED pending the complete infrastructure-backed E2E certification.
218. G31 secure endpoint gate: production runtime configuration now rejects explicitly configured non-HTTPS S3 endpoints while preserving development/MinIO operation; focused runtime configuration tests pass 5/5, with live deployment validation still gated.
219. G31 verification: runtime configuration tests pass 5/5, API typecheck and root lint pass, and `git diff --check` is clean. Production deployment and live object-storage TLS evidence remain gated.
220. G31 database transport gate: production startup now requires explicit PostgreSQL TLS mode for non-local database hosts, while local development/test databases remain usable; focused runtime configuration tests now cover database and object-storage transport safety.
221. G31 transport verification: runtime configuration tests pass 6/6, API typecheck and root lint pass, and `git diff --check` is clean. Live deployment validation remains gated.
222. G20/G16 approved-answer execution: provider submission now loads only owner-scoped, approved, timestamped normalized application answers with valid provenance/value shapes and passes them into the shared Greenhouse/Lever resolver; unapproved/invalid records are omitted and therefore remain blocking. Provider-submission tests cover the wiring; live database execution remains gated.
223. G20/G16 verification: provider-submission tests pass 10/10, API typecheck and root lint pass, and `git diff --check` is clean. Live normalized-answer persistence and browser submission evidence remain gated.
224. G16/G20 cover-letter execution: approved `{source:"coverLetter"}` answer markers now resolve only through the owner-linked application CoverLetter content before entering the shared provider resolver; unrelated or unapproved markers remain omitted. Provider-submission coverage now includes owner-approved cover-letter text; live persistence/browser evidence remains gated.
225. G16/G20 cover-letter verification: provider-submission tests pass 10/10, API typecheck and root lint pass, and `git diff --check` is clean. Live cover-letter persistence and browser submission evidence remain gated.
226. G17/G16 provider-boundary ownership: Greenhouse and Lever adapters now own their HTTPS application-host allowlist helpers; submission execution dispatches through those provider contracts instead of embedding provider host rules in the API service. Adapter tests cover cross-provider, insecure, and invalid hosts; live browser/network evidence remains gated.
227. G17/G16 provider-boundary verification: Greenhouse/Lever adapter and provider-submission tests pass 19/19; job-engine/API typechecks, root lint, and `git diff --check` pass. Live browser/network and database evidence remain gated.
228. G27/G17 provider allowlist hardening: provider application URLs now accept only explicit certified Greenhouse hosts (`boards.greenhouse.io`, `job-boards.greenhouse.io`) and Lever host (`jobs.lever.co`), rejecting attacker-controlled subdomains while requiring HTTPS. Hostile-subdomain coverage added; live DNS/browser validation remains gated.
229. G17/G16 verification: provider/adapter tests pass 19/19, job-engine/API typechecks and root lint pass, and `git diff --check` is clean. Live DNS, browser, and database evidence remain gated.
230. G24 durable email worker path: registered an `EMAIL_OUTCOME` automation handler that validates bounded payloads/timestamps, honors cancellation heartbeats, and delegates provider-fetched messages to the existing tenant-scoped hash-only idempotent ingestion service; it performs no mailbox access or credential handling. Handler tests cover valid and malformed payloads; live queue/mailbox delivery remains gated.
231. G24 worker verification: automation handler tests pass 8/8, API typecheck and root lint pass, and `git diff --check` is clean. Live queue/Redis and mailbox-provider delivery remain gated.
232. Regression fix: updated the production handler-map contract test to include the newly registered `EMAIL_OUTCOME` worker handler; the prior full-suite failure was an expected-key mismatch, not a runtime failure.
233. Final regression verification: configured full Vitest suite passes with 57 files passed, 25 explicitly skipped, 424 tests passed, and 107 skipped after provider-host allowlists, approved-answer/cover-letter wiring, and the durable email-outcome worker registration. Submission verification now also closes the durable attempt as `CONFIRMED` when independently verified. PostgreSQL/Redis/S3/Docker integration and live provider evidence remain gated and are not certification evidence.
234. G21 attempt finalization: independent verification now selects the latest durable unconfirmed/unknown attempt and closes it as `CONFIRMED` in the same tenant transaction as the application confirmation, keeping durable attempt history consistent with the independently verified lifecycle state. Submission-verification tests pass 15/15; API typecheck, root lint, and `git diff --check` pass. Database-backed transition evidence remains gated.
235. G21 replay safety: confirmation replay is now evaluated against immutable persisted verification evidence before the durable-attempt gate, so a previously confirmed application remains idempotently replayable after its attempt is finalized as `CONFIRMED`; conflicting evidence still fails closed. Submission-verification tests pass 15/15; API typecheck, root lint, and `git diff --check` pass.
236. Cross-goal focused verification: provider-neutral form intelligence, Greenhouse/Lever adapter contracts, private document validation/retention, and independent submission verification pass 83/83 focused tests. Live PostgreSQL/S3-backed persistence and browser upload evidence remain gated by unavailable local Docker infrastructure.
237. G19 signed-access retention gate: authenticated document download URL creation now excludes expired metadata at the tenant query boundary in addition to deleted and non-clean objects, preventing signed access to retained/expired artifacts. Document route tests pass 8/8; API typecheck and `git diff --check` pass. Live S3 signed-URL expiry remains gated.
238. G19 immutable object-write race: document uploads now use a conditional S3-compatible put with a content-hash metadata marker; a failed conditional write is reconciled only when `HeadObject` proves the exact same checksum, MIME, and size, preventing concurrent writers from replacing immutable content. Document-storage tests pass 32/32; API typecheck, root lint, and `git diff --check` pass. Live S3 concurrency evidence remains gated.
239. G16/G18 accessible-name integration: Greenhouse resume, cover-letter, and deferred document classification now includes the provider-extracted ARIA/native accessible name, so labels hidden behind accessible semantics are handled consistently with shared form intelligence. Chromium Greenhouse fixtures and document-upload tests pass 19/19; API typecheck, root lint, and `git diff --check` pass.
240. G19 service-level signed-access gate: `signedDownloadUrlAuthorized` now independently rejects infected, deleted, or expired metadata before presigning, preserving fail-closed behavior even when callers bypass the HTTP route query. Document storage and route tests pass 43/43; API typecheck, root lint, and `git diff --check` pass.
241. G19 retention tenant boundary: expired-object retention now performs metadata tombstoning and retention audit writes inside the owner’s `withTenant` transaction, with an explicit owner predicate on the update; object-store deletion failures still leave metadata recoverable. Retention tests pass 2/2; API typecheck and `git diff --check` pass. Live retention execution remains gated.
242. G17 Lever combobox transport: Lever’s provider port now activates custom ARIA comboboxes and selects only an exact rendered option after native-select fallback fails; multi-value/unknown-option cases remain fail-closed. Lever port tests pass 5/5 and local Chromium fixtures pass 5/5; API typecheck, root lint, and `git diff --check` pass.
243. G16/G20 document-control integration: provider submission resume detection now includes accessible names, allowing opaque file controls to be matched only when their semantic accessible name identifies a resume/CV; provider-submission tests pass 10/10 with this path covered. API typecheck, root lint, and `git diff --check` pass.
244. G20 provider-port upload boundary: authorized submission now routes the exact verified document through the selected Greenhouse/Lever form port’s `uploadDocument` capability instead of directly manipulating the page, and fails closed when that capability is absent. Provider-submission tests pass 10/10; API typecheck, root lint, and `git diff --check` pass. Live provider execution remains gated.
245. G20 provider-port verification: the exact-document upload path remains covered after moving transport into the provider port; provider-submission tests pass 10/10, and the adjacent Lever/Greenhouse transport and storage suites remain green. Live authorized browser submission and database state transitions remain gated.
246. G20 provider-port architecture audit: scheduler-created discovery jobs remain handed off to the existing durable automation dispatcher, while authorized provider submissions now share the same provider-owned upload transport and exact-document boundary. No new in-memory submission path was introduced; live queue/browser execution remains gated.
247. G25 assessment lifecycle: added durable `ApplicationStatus.ASSESSMENT`, legal transitions from independently confirmed applications into assessment and onward to interview/offer/rejection/withdrawal, and reviewed email-outcome mapping for `ASSESSMENT`. Prisma client generation and all workspace typechecks pass; lifecycle/email tests pass 25/25. Migration `20260914180000_goal25_assessment_status` is ready, but applying it is PostgreSQL-gated.
248. Full regression after assessment-state and provider-port changes: configured Vitest suite passes with 57 files passed, 25 explicitly skipped, 429 tests passed, and 107 skipped. The first run exposed and the focused rerun fixed only a stale Greenhouse test double; no production test failure remains. PostgreSQL/Redis/S3/Docker integration and migration application remain gated.
249. G23 assessment notification: added the durable `ASSESSMENT_DETECTED` notification mapping to the existing application transition outbox consumer, so reviewed assessment lifecycle transitions are surfaced without introducing a second notification path. Notification/state-machine tests pass 23/23; all workspace typechecks and root lint pass.
250. G28 structured traceability: added one shared structured-log boundary and enriched HTTP request/error records with correlation and authenticated user identifiers; worker failures now emit bounded job, correlation, type, attempt, retry, and worker context. API typecheck, root lint, and worker tests pass 9/9; live log shipping/metrics/alert validation remains gated.
251. G29 lease observability: queue timer-driven lease-renewal failures now invoke an explicit operational callback instead of being silently discarded; the API emits a structured lease-renewal failure event with job/correlation/worker context. Queue worker tests pass 4/4, queue typecheck, root lint, and `git diff --check` pass; Redis outage/restart recovery remains gated.
252. Workspace verification: all workspace typechecks and production builds pass after the G28/G29 changes; root lint and `git diff --check` pass. PostgreSQL/Redis/S3/Docker integration, live telemetry shipping, and final E2E certification remain gated.
253. G28 request correlation: HTTP request events now safely include validated route `applicationId`, `automationJobId`, and `provider` identifiers when present, without logging query strings or answer/document content. API typecheck, root lint, and `git diff --check` pass; live log/metric sink validation remains gated.
254. G19 worker-read boundary: owner-bound document reads now independently reject infected, deleted, or expired metadata before any object-store request, matching the signed-download fail-closed policy. Storage/provider/upload tests pass 54/54; API typecheck and root lint pass. Live PostgreSQL/S3 execution remains gated.
255. G19 compensating cleanup: `storeAndPersistArtifact` now surfaces a dedicated storage failure when metadata persistence fails and object deletion cannot be confirmed, instead of suppressing the cleanup error and leaving an untracked object ambiguous. Storage/retention tests pass 40/40; API typecheck and root lint pass. Live PostgreSQL/S3 failure-recovery evidence remains gated.
256. G19 artifact binding: authenticated uploads now persist `COVER_LETTER` application attachments with the explicit `cover_letter` document type; cleanup failures in the route upload path also return an explicit storage error instead of being suppressed. Document route/storage tests pass 47/47; API typecheck and root lint pass. Live PostgreSQL/S3 transaction and object-store evidence remains gated.
257. G16/G17 combobox safety: Greenhouse and Lever custom ARIA combobox transports now match only an exact normalized option label, preventing substring selection of a different employer answer. Provider ports and local Chromium fixtures pass 26/26; API typecheck and root lint pass. Live provider execution remains gated.
258. G19 upload transactionality: authenticated artifact uploads now persist object metadata and, when requested, the application binding inside one tenant transaction; a failed binding triggers compensating object cleanup before returning failure. Document route tests pass 9/9; API typecheck and root lint pass. Live PostgreSQL transaction and S3 cleanup evidence remain gated.
259. G19 content-address integrity: stored-reference validation now requires the final object-key digest to equal the recorded SHA-256 checksum, preventing metadata from pointing at a different content-addressed object. Storage/provider/route tests pass 58/58; API typecheck and root lint pass. Live S3 tamper/retrieval evidence remains gated.
260. G16/G17 combobox regression evidence: added an explicit Lever test proving the custom ARIA matcher accepts `Engineer` but rejects substring `Senior Engineer`; the focused port suite passes 5/5, API typecheck, root lint, and `git diff --check` pass. Live provider execution remains gated.
261. G19 application-binding idempotency: added a unique `(applicationId, objectMetadataId, type)` database invariant and replay handling for concurrent binding races, preventing duplicate attachments without duplicating audit events. Prisma generation, document-storage tests (40/40), API typecheck, and root lint pass; applying the migration and live race evidence remain PostgreSQL-gated.
262. G19 deletion authorization: owner-bound document tombstoning now includes the authenticated `userId` in its post-delete update predicate, closing a TOCTOU ownership gap. Document route tests pass 9/9; API typecheck, root lint, and `git diff --check` pass. Live PostgreSQL/RLS evidence remains gated.
263. G18/G19 artifact identity: stored metadata validation now binds the private object-path artifact segment to the recorded artifact `kind`, preventing a resume metadata record from referencing a screenshot namespace. Storage/route tests pass 50/50; API typecheck and root lint pass. Live PostgreSQL/S3/RLS evidence remains gated.
264. G16/G20 exact-document evidence: submission authorization now records filename and encryption-key reference alongside the immutable object identity, requires an encryption reference for new authorizations, and provider execution compares both fields before browser access. Provider-submission tests pass 10/10; API typecheck and root lint pass. Live PostgreSQL-backed authorization/replay evidence remains gated.
265. G18 answer provenance bounds: application-answer provenance now requires a JSON-serializable object of at most 8 KB in addition to the existing key-count/type checks, limiting unnecessary sensitive metadata retention while preserving explicit approval semantics. Provenance unit test passes 1/1; API typecheck and root lint pass. Live database persistence remains gated.
266. G17 provider host hardening: shared Greenhouse/Lever application orchestration now delegates host validation to the certified adapter allowlists, removing the prior arbitrary-subdomain acceptance. Job-engine adapter tests pass 9/9 and API typecheck/lint pass. A combined Chromium fixture run exposed two environment-level timeouts after 16 fixture tests passed; those timeouts are not counted as passing evidence and require follow-up.
267. Chromium fixture recovery: Greenhouse browser fixtures now reuse one Chromium process with isolated contexts and preserve a separate crash/recovery launch check, eliminating repeated per-test process accumulation. The full local Greenhouse fixture suite passes 13/13; API typecheck and root lint pass. Provider integration remains fixture-only and external submission remains disabled.
268. Lever fixture recovery: Lever Chromium fixtures now use one shared browser with isolated contexts, matching the Greenhouse lifecycle fix and reducing process accumulation during provider verification. Full Lever fixture suite passes 5/5; API typecheck and root lint pass. External provider execution remains disabled.
269. Full regression: configured workspace Vitest now passes 58 files with 438 tests passed and 25 files/107 tests explicitly skipped. The previously timing-sensitive Greenhouse fixtures pass under the shared-browser lifecycle; skipped PostgreSQL/Redis/S3/Docker integration remains unverified and is not certification evidence.
270. Integration runner gate: `run-database-integration.mjs` now performs a Docker-daemon preflight and exits with an explicit gated result before launching PostgreSQL suites when Docker is unavailable. Current execution reports `GATED` with no integration tests run; script syntax, root lint, and `git diff --check` pass. PostgreSQL/RLS/S3 evidence remains unavailable.
271. Queue integration gate: `run-queue-integration.mjs` now verifies Redis reachability and writeability plus PostgreSQL reachability before launching BullMQ integration suites. Current execution reports `GATED` because Redis is write-blocked by `MISCONF`; no queue integration tests were run. Redis outage runner also reports `GATED` when Docker is unavailable. Integration evidence remains unavailable.
272. G19/G20/G21 focused regression: document storage, authenticated document routes, provider submission, and independent verification pass 75/75 tests after the integration-runner changes; workspace typecheck, root lint, and `git diff --check` pass. PostgreSQL, writable Redis, S3, Docker, and live submission evidence remain gated.
273. G20 stale-execution evidence: stale submission reconciliation now retains the same bounded exact-document reference in its durable failure record as the attempt/audit evidence. Focused G19–G21 regression remains 75/75; workspace production build passes. Live reconciliation remains PostgreSQL-gated.
274. Regression caution: a fresh parallel full-suite run exposed a bcrypt password-test timeout under shared worker load; the isolated security auth suite passes 5/5. Full-suite success is not re-claimed from this run; the failure requires controlled rerun/resource investigation before updating the regression evidence.
275. Greenhouse fixture recovery hardening: the crash-recovery fixture now recycles the shared Chromium process before relaunching, avoiding competing browser processes while preserving recovery coverage. Greenhouse fixtures pass 13/13 and combined Greenhouse/Lever fixtures pass 18/18. Full-suite rerun is pending.
276. Full regression restored: after the Chromium lifecycle fix, `npm test` passes 58 files and 438 tests, with 25 files/107 tests explicitly skipped. The skipped PostgreSQL/Redis/S3/Docker suites remain unverified and are not counted as passing integration evidence.
277. Regression verification: the full suite was rerun after the recovery fix and again passes 58 files/438 tests; the prior auth timeout and Greenhouse recovery timeout did not recur. Integration-dependent skips remain explicitly gated and do not certify G19/G20/G21 live behavior.
278. G19 integration harness: added `npm run test:s3`, which uses the existing MinIO Compose services with explicit local credentials and runs the real S3 integration suite only after a Docker-daemon preflight. Current execution reports `GATED` because the Docker engine pipe is unavailable; no S3 tests were run and no integration pass is claimed.
279. Post-harness verification: workspace typechecks, root lint, script syntax, and `git diff --check` pass after adding the S3 runner and Chromium recovery fix. PostgreSQL, writable Redis, MinIO, and live provider execution remain gated by unavailable runtime services.
280. G21 verifier boundary: added a bounded provider-confirmation verification helper that parses confirmation evidence before invoking the existing tenant-scoped verifier; oversized page content is rejected before hashing. Submission-verification tests pass 16/16 and all workspace typechecks pass. Live provider/database verification remains gated.
281. Durable verification handler: registered `VERIFY_SUBMISSION_CONFIRMATION` in the production automation handler map with bounded provider/page/timestamp validation, heartbeats, and delegation to the independent verifier. Focused handler/discovery/verifier tests pass 36/36; the handler-list contract was updated for the new command. Live queue/database execution remains gated.
282. Full regression after durable verification wiring: `npm test` passes 58 files and 441 tests, with 25 files/107 tests explicitly skipped. Integration-dependent PostgreSQL/Redis/S3/Docker execution and live provider verification remain unverified.
283. Regression rerun after handler-contract repair: `npm test` again passes 58 files and 441 tests, with 25 files/107 tests explicitly skipped. No live submission or infrastructure integration evidence is inferred from the unit/fixture suite.
284. G21 durable payload minimization: `VERIFY_SUBMISSION_CONFIRMATION` jobs now carry only normalized confirmation identity, hash, parser, source, provider, and timestamp fields; raw confirmation page text is kept at the capture boundary and is never placed in the durable job payload. Handler/verifier tests pass 26/26 and workspace typechecks pass.
285. Full regression after payload minimization: `npm test` passes 58 files and 441 tests, with 25 files/107 tests explicitly skipped. Infrastructure-dependent and live provider evidence remains unverified.
286. G21 durable producer: added `queueProviderConfirmationVerification`, which parses trusted browser confirmation content in memory and creates an idempotent `VERIFY_SUBMISSION_CONFIRMATION` job containing only normalized evidence metadata. Focused tests pass 27/27, workspace typechecks pass, and the full suite passes 58 files/442 tests with 25 files/107 skips. Live queue/database evidence remains gated.
287. G21 payload-security verification: the durable producer/consumer boundary was reviewed and confirmed to keep raw confirmation content out of persisted job payloads; normalized producer and handler tests pass 27/27, and all workspace typechecks pass. Queue/database execution remains gated.
288. G22 schedule API contract: the typed web client now exposes the backend’s validated `customCron` field for durable custom schedules without changing existing schedule behavior. Scheduler/API focused tests pass 19/19; web typecheck and root lint pass. The compressed Settings JSX still needs a deliberate UI edit before claiming full custom-cron dashboard coverage.
289. G22 custom-cron UI: reformatted the Settings schedule form and added a required conditional custom-cron input with bounded client length, while preserving durable profile pause/resume/cancel and rule controls. Scheduler/web API tests pass 20/20; web typecheck, production build, and root lint pass.
290. G22 timezone UI: durable schedule creation now exposes an editable IANA timezone field defaulted from the browser and sends it through the validated profile API. Scheduler/web API tests pass 20/20; web typecheck, production build, and root lint pass.
291. G22 schedule-boundary validation: custom cron expressions are now parsed at the authenticated profile API boundary using the shared scheduler kernel, preventing invalid schedules from being persisted. Search-profile/scheduler/web API tests pass 27/27; API/web typechecks and root lint pass.
292. G22 schedule coverage: the Settings UI now exposes the backend-supported weekly schedule in addition to once/recurring/custom options. Web typecheck, production build, root lint, and `git diff --check` pass.
293. G22 daily-limit UI: Settings now exposes the persisted daily application limit with integer bounds and sends it through the typed profile client; server validation remains authoritative. Scheduler/profile/web API tests pass 27/27; web typecheck, production build, and root lint pass.
294. G22 persisted-settings visibility: the schedule list now renders the saved IANA timezone and daily application limit from backend profile state, keeping the dashboard truthful after creation. Web typecheck, production build, root lint, and diff checks pass.
295. G23 notification refresh: the authenticated header now refreshes durable notifications on mount and every 30 seconds, while retaining the existing open-dropdown fetch and mark-all-read flow. Web typecheck, root lint, and diff checks pass; live outbox/SSE delivery remains unverified.
296. G23 individual acknowledgement: notification items in the authenticated header now call the owner-scoped read endpoint and update local durable state after success; API coverage includes the PATCH request. Web notification API test passes 14/14; web typecheck, production build, root lint, and diff checks pass.
297. G28 runtime log unification: scheduler ticks, notification-outbox failures, dispatcher failures, worker startup, and worker startup failures now emit the shared bounded structured-log records instead of free-form console output. Dispatcher/worker tests pass 13/13; API typecheck, root lint, and diff checks pass. External log shipping/alert drills remain gated.
298. G28 HTTP lifecycle logging: API startup and shutdown failures now use the shared structured-log boundary with bounded event/error fields, removing the remaining free-form server lifecycle output. Server, dispatcher, and worker tests pass 16/16; API typecheck, root lint, and `git diff --check` pass. External log shipping, metrics, and alert drills remain gated.
299. G29/G28 queue error boundary: the shared BullMQ worker now exposes unscoped connection/worker errors through an explicit callback, and the API worker records them as structured queue events with worker/queue context. Queue and API worker tests pass 10/10; queue/API typechecks and root lint pass. Redis outage/restart recovery and external telemetry remain gated.
300. Regression evidence update: the latest full Vitest run had 426 passing tests, 1 bcrypt timeout, and two Chromium cleanup-hook timeouts under shared load; isolated auth passed 5/5 and the provider fixtures executed all 18 assertions but Lever cleanup still exceeded the hook timeout. This run is not counted as a full-suite pass; infrastructure and resource-sensitive browser evidence remain gated.
301. G28 log-safety boundary: the shared logger now redacts credential/content-like keys, bounds strings/arrays/object depth, and preserves correlation fields before emission. Focused observability/worker/queue tests pass 12/12; API and queue typechecks, root lint, and diff checks pass. External log shipping and alert validation remain gated.
302. G24 email provenance: email outcomes now persist a bounded ingestion `source` alongside the existing message identity, hashes, classifier evidence, and audit event; mailbox synchronization records the consented connector provider and durable worker ingestion defaults to `AUTOMATION`. Prisma generation, email/route/sync/handler tests pass 36/36; API typecheck and root lint pass. Migration application and live mailbox-provider evidence remain PostgreSQL/OAuth-gated.
303. G24 schema verification: the email-source migration is syntactically represented in Prisma and `prisma validate` passes. A workspace build attempt ended at the runner pipe without a reliable exit code, so no build pass is claimed; the previously verified API/queue typechecks and focused tests remain the current evidence.
304. Build evidence correction: the web production build was rerun independently and completed successfully (Vite transformed 1,503 modules; emitted the production bundle). The earlier aggregate workspace build remains unverified because its runner pipe closed before reporting an exit status.
305. G29/G31 worker shutdown correctness: worker SIGINT/SIGTERM handling now exits nonzero and emits structured failure evidence when durable shutdown/drain fails; a successful drain still exits zero. Worker/server tests pass 9/9, API build/typecheck, root lint, and diff checks pass. Live shutdown/drain and infrastructure recovery drills remain gated.
306. G29 queue-error evidence: added executable coverage for forwarding an unscoped BullMQ error through the shared worker callback; queue/API worker tests pass 11/11, queue/API typechecks, root lint, and diff checks pass. Redis outage/restart recovery remains gated.
307. G30 deterministic regression execution: Vitest now runs test files serially because Chromium and bcrypt suites share host resources; the controlled full suite passes 59 files and 448 tests, with 25 files/107 tests explicitly skipped. PostgreSQL/Redis/S3/Docker integration and live provider evidence remain gated and are not counted as passing.
308. Workspace verification: all configured workspace typechecks and the API build pass after the G29/G30 changes; diff checks pass. The web build was independently verified earlier. Integration services remain unavailable, so no live database/queue/object-storage or final E2E certification is claimed.
309. G31 readiness-document correction: updated `PRODUCTION_READINESS.md` to reflect the implemented durable execution, provider/form, storage, verification, scheduling, notification, analytics, security, and recovery boundaries, while explicitly retaining the unavailable-infrastructure, live-provider, telemetry, DR, and final-E2E release blockers. No readiness claim was upgraded.
310. G28 route-log unification: authenticated route failure paths now use the shared bounded/redacted structured logger with correlation and tenant context instead of free-form console output; no route response behavior changed. API observability/server/worker tests pass 11/11, API typecheck, root lint, and diff checks pass.
311. Full regression after route-log unification: controlled serial Vitest passes 59 files and 448 tests, with 25 files/107 tests explicitly skipped. Skipped integration suites remain PostgreSQL/Redis/S3/Docker-gated and no live provider or final E2E evidence is inferred.
312. G18 stable identity hardening: provider-neutral question identity now prefers explicit semantic keys and browser-standard autocomplete semantics before volatile DOM names, while preserving deterministic occurrence keys for duplicates; dynamic DOM-renaming coverage passes in the form-intelligence suite (27/27), job-engine typecheck, root lint, and diff checks pass.
313. G18 approved-answer fail-closed decision handling: the durable answer service now rejects unknown approval decisions and invalid expected versions before any database work, preventing an unrecognized command from falling through to approval. Application-answer tests pass 2/2; API typecheck, root lint, and diff checks pass.
314. G27 credential boundary: the legacy process-local `CredentialVault` is now explicitly disabled when `NODE_ENV=production`, directing production callers to the durable tenant-scoped credential service; security tests pass 12/12, package typecheck, root lint, and diff checks pass.
315. G18/G20 approval-state audit: reviewed the durable answer and submission transition boundaries; unknown answer decisions now fail closed, submission authorization remains explicit and version-bound, and `CONFIRMED` remains verifier-only. No new lifecycle mutation was introduced; focused approval/state tests and static checks remain green.
316. Full regression after G18/G27 hardening: controlled serial Vitest passes 59 files and 451 tests, with 25 files/107 tests explicitly skipped. The additional tests cover production-disabled process-local credentials and invalid answer decisions; live infrastructure and provider/E2E evidence remain gated.
317. G17 Lever multi-step extraction: Lever’s provider-specific form port now reads `data-step`, `aria-posinset`, and `data-step-index` safely, defaulting to step 1 only when no marker exists; added an accessible multi-step Chromium fixture. Lever port/fixture tests pass 11/11, API typecheck, root lint, and diff checks pass.
318. Full regression after Lever multi-step extraction: controlled serial Vitest passes 59 files and 452 tests, with 25 files/107 tests explicitly skipped. The added provider fixture executes successfully; live provider, infrastructure, and final E2E evidence remain gated.
319. G19 retention audit hardening: retention deletion audit records now include the immutable SHA-256 checksum used to identify the deleted object, matching owner-deletion evidence and preserving tamper/audit traceability without retaining binary content. Retention tests pass with explicit audit assertions; live S3/PostgreSQL retention execution remains Docker/database-gated.
320. G20 provider-failure finalization: ordinary provider submission failures now atomically finalize the executing authorization as `FAILED`, transition a still-pending application to `FAILED`, and retain bounded attempt/failure/audit evidence. The existing authoritative application lifecycle outbox event supplies failure notification without a duplicate submission-specific notification. Live database/provider execution remains gated.
321. G20 failure-path coverage: added a database integration scenario asserting ordinary provider rejection produces `FAILED` application/authorization/attempt state rather than a stranded executing authorization. The scenario is currently skipped because PostgreSQL is unavailable; no integration pass is claimed.
322. G20 retry classification: deterministic provider rejection errors are now recorded as non-retryable failure records, while unexpected execution errors remain retryable only at the failure-record classification boundary; provider exceptions that become `UNKNOWN_OUTCOME` still remain blocked from automatic retry. API typecheck and focused provider tests remain required evidence.
323. G29 scheduler shutdown hardening: the durable scheduler timer is now unref’d and close has a validated bounded shutdown timeout, surfacing a shutdown failure instead of waiting forever on a hung database tick. API typecheck, focused scheduler tests, root lint, and diff checks remain required evidence.
324. G28 execution timing: the API automation worker now emits bounded structured duration records for each leased handler execution, including job, correlation, worker, type, attempt, and elapsed milliseconds without payload content. Worker-focused tests, API typecheck, root lint, and diff checks remain required evidence.
325. G23 SSE replay correctness: durable notification replay now uses the stable `(createdAt, id)` cursor ordering, preventing same-timestamp notifications from being skipped after reconnect while retaining tenant-scoped cursor lookup. API route tests, typecheck, lint, and diff checks remain required evidence.
326. G24 lifecycle replay idempotency: reapplying an already-recorded email outcome now returns the state-machine replay result without writing a second email-lifecycle audit event. Focused lifecycle tests, API typecheck, root lint, and diff checks remain required evidence.
327. G24 review provenance: explicit email-outcome lifecycle application now persists reviewer identity and timestamp on the outcome and exposes them with application state; replay paths do not mutate review metadata or duplicate audit records. Prisma generation, focused lifecycle tests, API/web typechecks, lint, and migration application remain required evidence.
328. G24 dashboard provenance: application detail cards now render the persisted reviewed timestamp/reviewer or an explicit human-review-needed state, keeping email outcome status truthful in the UI. Web typecheck, production build, lint, and diff checks remain required evidence.
329. G23 SSE memory safety: pending live events are now bounded during durable replay; an overloaded/reconnecting client is closed and removed instead of allowing unbounded per-client memory growth. API typecheck, lint, and diff checks remain required evidence.
330. G23 SSE serialization safety: live and replayed event payloads now require bounded JSON serialization; circular or oversized untrusted payloads close and remove the affected client instead of throwing through replay cleanup or writing uncontrolled response data. API typecheck, lint, and diff checks remain required evidence.
331. G23 SSE focused coverage: added provider-independent unit coverage for normal, circular, and oversized SSE payload serialization without requiring live database infrastructure. SSE boundary tests pass; live replay/connection evidence remains gated.
332. G27 refresh rotation race hardening: refresh-token rotation now serializes concurrent requests for the same tenant/token with a PostgreSQL transaction advisory lock, ensuring one-time use is enforced before the second request observes the revoked session and revokes its family. Auth tests, API typecheck, lint, and database concurrency evidence remain required; live PostgreSQL evidence is gated.
333. G31 production CORS configuration: production runtime validation now requires an explicit HTTPS origin for credentialed frontend CORS, rejecting HTTP, paths, queries, fragments, and missing values before the API listens. Runtime configuration tests, API typecheck, lint, and diff checks remain required evidence.
334. G31 runtime validation testability: isolated production runtime configuration validation into the dependency-free `apps/api/src/runtime-config.ts`, allowing HTTPS CORS, TLS database, secret, scanner, and object-storage checks to execute without importing the listening server or initializing database dependencies. Runtime configuration tests pass 7/7; API typecheck, workspace lint, production build, and diff checks pass. Live infrastructure and final production-readiness evidence remain gated.
335. Regression boundary: the aggregate `npm test` invocation was started after the runtime-config refactor but produced no file/test progress and was terminated after the bounded wait; it is not counted as a pass. The focused runtime-config suite remains green at 7/7, with API typecheck, workspace lint, production build, and diff checks passing. Resource-sensitive aggregate execution and all live infrastructure/provider evidence remain gated.
336. G18 evidence completeness: provider-neutral form persistence now adds a bounded audit projection for each persisted answer containing answer/question identity, owner, source, provenance, approval state, approval timestamp, and version, while intentionally excluding answer content. Focused form-intelligence tests pass 27/27 and API typecheck plus diff checks pass; provider persistence integration remains PostgreSQL-gated.
337. G18/G20 approval-actor hardening: provider submission preparation and the shared `AnswerResolver` now require the durable answer's explicit owner approval actor in addition to approval state/timestamp and provenance; mismatched reviewers are rejected and cannot reach provider controls. Form-intelligence tests pass 27/27, API and job-engine typechecks pass, root lint and diff checks pass. Live database enforcement remains PostgreSQL-gated.
338. Workspace build verification: after approval-actor hardening, the API, web, and queue workspace production builds completed successfully; the focused form-intelligence suite remains 27/27 and live PostgreSQL/provider execution is still not claimed.
339. G18 identity contract alignment: normalized the provider-neutral form field output to expose the same derived stable identity as both `identity` and `questionIdentity`, including duplicate-field occurrence keys and dynamic/multi-step reinspection. This closes the adapter-to-durable-question naming seam without using DOM indexes or control IDs. Form-intelligence tests pass 27/27; job-engine/API typechecks, root lint, and diff checks pass.
340. G21 hostile-input boundary: independent submission verification now validates runtime string/Date shapes before calling string/date methods, and confirmation parsing rejects non-text payloads with `SubmissionVerificationError` instead of leaking native `TypeError`. Verification tests pass 18/18; API typecheck and root lint pass. Live verifier/database evidence remains gated.
341. G21 malformed-payload coverage: added regression cases for non-Date evidence timestamps and non-string provider confirmation content, verifying both fail closed through the domain error boundary. Verification tests pass 18/18; API typecheck and root lint pass. Live verifier/database evidence remains gated.
342. G22 scheduler fail-closed validation: `nextScheduledRun` now validates schedule names at runtime before interpreting cron state, protecting the durable scheduler from corrupted or legacy persisted values that could otherwise fall through as custom schedules. Scheduler tests pass 8/8; API typecheck and root lint pass.
343. G28 correlation safety: centralized bounded correlation-ID normalization and applied it to both request lifecycle logs and error-handler logs, preventing raw untrusted headers from entering structured observability records. Correlation boundary tests pass 5/5; API typecheck, root lint, and diff checks pass. Aggregate Vitest completion and external telemetry remain gated.
344. G19 scanner resilience: the ClamAV document-scanner integration now has a validated bounded timeout, terminates a hung scanner process, and returns fail-closed `ERROR` evidence instead of blocking uploads indefinitely. Document-storage tests pass 42/42; API typecheck, root lint, and diff checks pass. Live scanner/S3/database execution remains gated.
345. G19 scanner configuration coverage: added executable validation for rejecting sub-second/unbounded scanner timeouts before process creation. Document-storage tests pass 42/42; API typecheck, root lint, and diff checks pass. Live scanner/S3/database execution remains gated.
346. G18/G16 Greenhouse multi-select capability: the Greenhouse DOM port now detects native `multiple` selects as provider-neutral `MULTISELECT` controls and safely handles array selections, while preserving exact option validation and single-value combobox/radio behavior. Greenhouse-port plus shared form tests pass 31/31; API/job-engine typechecks and root lint pass.
347. G18/G16 provider radio capability: Greenhouse and Lever DOM ports now extract grouped native radio values into the shared field `options` contract, enabling deterministic approved-answer option validation instead of silently treating radio questions as opaque controls. Greenhouse/Lever port plus shared form tests pass 37/37; API/job-engine typechecks and root lint pass.
348. G18/G16 radio-group normalization: Greenhouse and Lever DOM ports now emit one field per native radio `name` group, with the complete value set and fieldset legend, preventing duplicate durable questions/answers for a single radio question. Provider-port tests pass 10/10; API/job-engine typechecks and root lint pass. Chromium fixture assertions ran 29/29 but both browser `afterAll` shutdown hooks exceeded the 10-second timeout, so fixture suites are not claimed as passing.
349. Browser fixture lifecycle: increased only the Greenhouse and Lever Chromium `afterAll` cleanup hook budget to the existing 30-second fixture budget, allowing host-load-sensitive browser shutdown to complete without changing application behavior. Both real local Chromium fixture suites pass 19/19; API typecheck, root lint, and diff checks pass.
350. G20/G27 authorization-boundary correction: restored the provider-submission authorization lookup predicates for exact authorization ID, application ID, authenticated owner, and `EXECUTING` status after a focused aggregate regression exposed their absence. Provider-submission tests pass 10/10; API typecheck, root lint, and diff checks pass. Live database execution remains gated.
351. Full regression after provider authorization and radio-group fixes: controlled serial Vitest completes with 62 files passed, 25 explicitly skipped, 466 tests passed, and 108 skipped. Skipped database/Redis/S3/Docker integration and live-provider suites remain gated; no final E2E or production-readiness claim is inferred.
352. G21/G20 verification handoff: provider submission now opportunistically parses only bounded trusted-browser confirmation text into normalized evidence, and the submission transaction enqueues a single durable `VERIFY_SUBMISSION_CONFIRMATION` job after persisting the `UNCONFIRMED` attempt. Missing or unparsable markers remain unconfirmed; raw page text is never queued or persisted. Submission/verification tests pass 28/28 executable (4 database tests skipped), API typecheck and root lint pass.
353. Full regression after verification-job wiring: controlled serial Vitest completes with 62 files passed, 25 explicitly skipped, 466 tests passed, and 108 skipped. The executable provider, worker, verification, and browser-fixture coverage remains green; PostgreSQL/Redis/S3/Docker integration, live providers, and final E2E remain gated.
354. G20/G21 positive evidence coverage: provider-submission tests now exercise a real confirmation-page text path, asserting normalized confirmation metadata is returned and raw page text is excluded from the evidence object. Provider-submission tests pass 11/11; API typecheck, root lint, and diff checks pass.
355. Regression confirmation: the controlled full Vitest run after durable verification-job wiring remains green at 62 files/466 tests passed with 25 files/108 explicitly skipped; the later positive confirmation-evidence test also passes. Infrastructure-gated suites remain unverified and no final E2E certification is claimed.
356. Integration-gate recheck: the repository runners report PostgreSQL as Docker-daemon-gated, queue integration as PostgreSQL-gated, and S3 as Docker-daemon-gated; no infrastructure tests were run or counted. The executable suite remains green, and implementation continues without claiming live integration.
357. G20/G21 timestamp ordering: the provider submission path now captures one post-click attempt timestamp and reuses it as confirmation evidence `observedAt`, ensuring valid provider evidence cannot be rejected because parsing completed after the attempt timestamp. Submission/verification tests pass 29/29; API typecheck, root lint, and diff checks pass. Live database/S3/provider evidence remains gated.
358. G20 timestamp fail-closed boundary: removed the impossible-path synthetic timestamp fallback; provider execution now raises an explicit unknown-outcome error if a successful return could ever lack the post-click timestamp. Submission/verification tests remain 29/29, API typecheck and diff checks pass, and no live integration evidence is inferred.
359. G19 signed-access tamper boundary: authenticated signed-download issuance now verifies the referenced object’s bytes, checksum, size, and magic bytes before presigning, preventing a tampered object behind clean metadata from receiving browser access. Document-storage tests pass 43/43; API typecheck, root lint, and diff checks pass. Live S3 authorization and tamper tests remain Docker-gated.
360. G16/G17 approved-answer integration: provider application preparation now loads only tenant-owned, explicitly owner-approved, provenance-bearing durable answers, passes them through the shared Greenhouse/Lever form adapters, and records bounded answer identity/provenance audit projections without persisting answer content in form evidence. Greenhouse/Lever fixture tests pass 25/26 (one database test skipped), API typecheck, root lint, and diff checks pass. Live provider/database execution remains gated.
361. G16/G17 policy handoff hardening: provider orchestration now requests human verification for any unresolved verification control, and unresolved ambiguous, sensitive, or high-risk fields trigger review even when optional; explicitly approved answers may proceed through the shared resolver. Fixture/form-intelligence tests pass 46/46, API typecheck, root lint, and diff checks pass. Live provider/database execution remains gated.
362. G16/G17 replay idempotency: repeated provider form inspection no longer increments an approved profile/cover-letter answer version when value, owner, approval actor, and provenance are unchanged; changed evidence still creates the normal versioned update. Provider/storage focused tests pass 71/71, API typecheck, root lint, and diff checks pass. Live provider/database execution remains gated.
363. G21 trust-boundary correction: the public authenticated verify-submission mutation can no longer promote caller-supplied confirmation IDs/hashes into independent proof; durable verification now requires the internal trusted verifier boundary, while the worker confirmation path supplies that assertion. Verification/handler/document-route tests pass 37/37; API typecheck, root lint, and diff checks pass. Live verifier/database evidence remains gated.
364. G21 route hardening build verification: after restricting durable confirmation writes to the trusted verifier boundary, the API workspace typecheck, lint, and focused verification/handler tests remain green at 37/37; no live submission confirmation or database result is claimed.
365. G22 scheduler input boundary: the cron kernel now rejects malformed runtime dates and blank timezones before calculation, preventing corrupted persisted schedule values from producing native exceptions or undefined scheduling behavior. Scheduler tests pass 9/9; API typecheck, root lint, and diff checks pass.
366. G23 notification runtime resilience: durable notification-outbox shutdown now has a validated bounded timeout and reports a stuck publisher instead of waiting indefinitely; runtime-boundary coverage passes 4/4, API typecheck, root lint, and diff checks pass. Live notification delivery remains database/outbox-gated.
367. G24 email identity hardening: duplicate message delivery is now replayed only when source, normalized sender/subject/body hashes, and received timestamp all match; conflicting reuse of a tenant message identity fails closed. Email classifier/lifecycle tests pass 9/9, API typecheck, root lint, and diff checks pass. Live mailbox/database ingestion remains gated.
368. G25 lifecycle identity hardening: interview and offer source-event replays now compare the complete normalized event payload before returning an existing record; conflicting reuse of a source event fails closed instead of silently discarding changed lifecycle data. Lifecycle/email tests pass 16/16, API typecheck, root lint, and diff checks pass. Live lifecycle/database evidence remains gated.
369. G26 analytics completeness: dashboard analytics now expose tenant-scoped provider success rates and role-level totals/confirmed/failed rates derived from persisted applications and job records, without fabricating metrics. Analytics tests pass 4/4; API typecheck, root lint, and diff checks pass. Live database aggregation evidence remains gated.
370. G26 dashboard alignment: the web analytics contract and view now render persisted provider and role success rates alongside totals and outcomes, keeping the frontend aligned with the real analytics payload. Web typecheck, analytics tests 4/4, root lint, and diff checks pass; live database metrics remain gated.
371. G27 browser-session input hardening: browser startup now validates runtime text, expiry, and allowlist shapes before invoking policy or DNS resolution, converting malformed inputs into bounded domain errors. Browser-session tests pass 4/4; API typecheck, root lint, and diff checks pass. Live browser/provider execution remains gated.
372. G28 worker trace correlation: automation execution duration records now derive bounded application and provider identifiers from the authoritative job payload, preserving job/correlation/tenant context without logging payload contents. Observability and worker tests pass 8/8; API typecheck, root lint, and diff checks pass. External telemetry, alert drills, and live infrastructure recovery remain gated.
373. G28 trace-correlation regression coverage: added a worker-focused assertion that application/provider identifiers are emitted while answer content remains absent from execution logs. Queue, observability, and API worker tests pass 14/14; API typecheck, root lint, and diff checks pass. External telemetry and infrastructure recovery remain gated.
374. G29 dispatcher shutdown resilience: in-flight automation dispatch now has a validated bounded shutdown timeout, and the queue registry is closed even when dispatch hangs or fails. Dispatcher tests pass 5/5, API typecheck, root lint, and diff checks pass. Redis restart/crash recovery remains infrastructure-gated.
375. Regression boundary: a fresh serial `npm test -- --run` invocation produced only Vitest startup output during three bounded polling windows and was terminated; it is not counted as a pass. Focused G28/G29 evidence remains green, while aggregate resource-sensitive execution and infrastructure/provider suites remain gated.
376. Workspace build verification: API, web, and queue production builds completed successfully after dispatcher shutdown hardening; `git diff --check` passed. The aggregate test run remains unverified, and PostgreSQL/Redis/S3/live-provider/final-E2E evidence remains gated.
377. G18 validation-state extraction: the provider-neutral `FieldExtractor` now merges bounded snapshot-level validation errors into matching field records with duplicate-message suppression, preserving validation state for shared mappers/adapters. Form-intelligence and Greenhouse/Lever adapter tests pass 37/37; job-engine/API typechecks, root lint, and diff checks pass. Durable provider persistence remains PostgreSQL-gated.
378. G19 retention runtime: added a bounded, coalescing document-retention runtime and wired it into API worker startup/shutdown so expired non-held private objects are actually purged and audited on a durable worker cadence. Storage/retention/worker tests pass 56/56; API typecheck, root lint, and diff checks pass. Live S3/PostgreSQL retention execution remains Docker-gated.
379. G19 build verification: API production build completed successfully after retention-runtime wiring and `.env.example` documents `DOCUMENT_RETENTION_INTERVAL_MS`; diff checks pass. Live object-storage, database, scanner, and final E2E evidence remain gated.
380. G19/G31 runtime configuration hardening: production worker validation now rejects malformed `DOCUMENT_RETENTION_INTERVAL_MS` values before runtime startup; worker and retention-runtime tests pass 8/8, API typecheck, root lint, and diff checks pass. Live retention execution remains Docker/S3/PostgreSQL-gated.
381. G20/G21 downstream verification: provider submission, submission verification, and exact approved-document upload focused coverage passes 35/35; API typecheck and diff checks pass. PostgreSQL-backed authorization, provider execution, and independent confirmation remain gated.
382. G19 retention input hardening: `purgeExpiredDocuments` now rejects invalid `Date` values before querying PostgreSQL, preventing malformed scheduler input from driving retention decisions. Retention/runtime tests pass 7/7; API typecheck, root lint, and diff checks pass. Live retention execution remains Docker/S3/PostgreSQL-gated.
383. G30 test-runner hardening: Vitest now has explicit 30-second test and 45-second hook timeouts while retaining serial file execution for resource-sensitive browser/bcrypt suites, preventing indefinite aggregate hangs.
384. G28 logger correctness: structured logging now omits undefined optional fields instead of serializing the literal string `"undefined"`; observability/worker tests pass 10/10, API typecheck, root lint, and diff checks pass.
385. G30 aggregate regression: the bounded serial suite completes with 63 files passed, 25 explicitly skipped, 482 tests passed, and 108 skipped. Skipped PostgreSQL/Redis/S3/Docker/live-provider suites remain gated; no final E2E certification is inferred.
386. G20 unknown-outcome hardening: authorized submission execution now treats any persistence failure after the provider executor returns as an unknown outcome, preventing a post-click database error from being misclassified as a normal provider rejection. Provider/verification/handler tests pass 39/39; API typecheck, root lint, and diff checks pass. Database-backed post-click failure recovery remains gated.
387. G20/G29 submission input hardening: submission authorization/execution and stale-reconciliation paths now reject malformed identifiers and invalid timestamps with bounded domain errors before database work. Provider/verification/dispatcher tests pass 34/34; API typecheck, root lint, and diff checks pass. Database-backed recovery remains gated.
388. G18 durable answer-source contract: `ApplicationAnswer.source` is now a first-class tenant-owned field, backfilled from validated provenance by an additive migration; draft/profile/cover-letter persistence writes it explicitly, and provider preparation rejects records whose source column and provenance disagree. Focused shared/answer/provider tests pass 48/48; Prisma generation/validation, API typecheck, and `git diff --check` pass. Live migration and cross-provider persistence remain PostgreSQL-gated.
389. G19 storage regression: document storage, retention, runtime, and authenticated document-route tests pass 59/59 after the answer/migration changes; root lint and `git diff --check` pass. The S3 runner was executed and returned `GATED` because no reachable Docker daemon is available; no S3 integration result is claimed.
390. G16/G18 provider visibility: Greenhouse DOM extraction now preserves explicit hidden/zero-layout state like the Lever port, allowing the shared extractor to exclude non-actionable dynamic controls while retaining them for reinspection. Greenhouse/Lever fixtures plus shared intelligence tests pass 60/60; API/job-engine typechecks, root lint, and `git diff --check` pass. Durable provider persistence and submission remain infrastructure-gated.
391. G22 scheduler input hardening: durable due-slot claiming now rejects invalid scheduler timestamps before any Prisma query, with a regression test proving malformed runtime dates produce a bounded `SchedulerError`. Scheduler tests pass 10/10; API typecheck and `git diff --check` pass.
392. G24 email-ingestion hardening: direct connector/service ingestion now validates runtime types for identity, source, sender, subject, body, and timestamps before hashing or persistence, preventing malformed payloads from leaking raw `TypeError` failures. Email outcome/sync tests pass 14/14; API typecheck and `git diff --check` pass.
393. G23 notification-envelope hardening: notification mapping now rejects malformed tenant/event identifiers, unsupported schema versions, invalid timestamps, and oversized correlation/idempotency values before any durable notification upsert. Notification consumer tests pass 24/24; API typecheck and `git diff --check` pass.
394. G25 lifecycle input hardening: interview, offer, and offer-decision commands now validate tenant/application/source-event/offer identities before trimming or entering a database transaction. Lifecycle and email boundary tests pass 19/19; API typecheck and `git diff --check` pass.
395. G26 analytics aggregation hardening: the shared date/status funnel now ignores malformed runtime rows instead of throwing or fabricating counts. Analytics tests pass 5/5; API typecheck and `git diff --check` pass.
396. G27 credential-boundary hardening: durable credential store/retrieve/revoke paths now validate runtime identity and value types before regex/trim/database access, preserving tenant fail-closed behavior without leaking raw `TypeError`. Credential/security tests pass 16/16; API typecheck and `git diff --check` pass.
397. G28 logging hardening: bounded structured-log serialization now detects circular metadata and emits a safe `[CIRCULAR]` marker instead of throwing while handling an original failure. Observability tests pass 4/4; API typecheck and `git diff --check` pass.
398. G29 worker failure hardening: automation failure handling now normalizes non-`Error` queue rejections before logging and durable retry/dead-letter persistence, preventing the failure handler itself from masking recovery. Automation-worker tests pass 8/8; API typecheck and `git diff --check` pass.
399. G31 production dependency transport hardening: worker startup now validates PostgreSQL URL protocol/TLS and requires `rediss://` for non-local production Redis before opening queues, matching the API’s database transport boundary and failing closed before durable execution begins. Server/worker runtime tests pass 12/12; API typecheck, root lint, and `git diff --check` pass. Live infrastructure startup and deployment evidence remain gated.
400. G19 private-bucket metadata hardening: stored document-reference validation now rejects empty bucket identifiers and any bucket that differs from the configured private document bucket, preventing a private-looking object key from being authorized against an unintended bucket. Document storage/retention/authenticated-route tests pass 56/56; API typecheck, root lint, and `git diff --check` pass. S3/PostgreSQL integration remains gated because Docker/MinIO are unavailable.
401. G20 approval preflight hardening: required application answers now need owner-matching answer ownership, explicit `approvedAt`, owner-matching `approvedBy`, and valid provenance before submission authorization can be created; a forged `approved` flag alone cannot pass preflight. Direct approval-boundary tests pass 2/2; submission/provider-focused tests pass 30/30, API typecheck, root lint, and `git diff --check` pass. Live PostgreSQL authorization remains gated.
402. G18 resolver hardening: the provider-neutral `AnswerResolver` now validates approved-answer runtime identity, version, approval timestamp, owner, provenance, and JSON answer shape before returning a value, failing closed on malformed persisted data rather than trusting TypeScript-only types. Form-intelligence tests pass 28/28; job-engine/API typechecks, root lint, and `git diff --check` pass. Durable cross-provider persistence remains PostgreSQL-gated.
403. G18 source/provenance consistency: the shared `AnswerResolver` now requires a supported answer source and an exact matching `provenance.source`, preventing source laundering between user input and AI suggestions or other approval classes. Form-intelligence tests pass 28/28; job-engine/API typechecks, root lint, and `git diff --check` pass. Durable cross-provider persistence remains PostgreSQL-gated.
405. G18 dynamic-field reinspection: the shared `ApplicationFormAdapter` now re-detects and re-extracts after fills before computing required blockers or advancing, so conditional controls rendered by a selection cannot be skipped. Form-intelligence tests pass 29/29; job-engine/API typecheck and `git diff --check` pass. Provider persistence remains PostgreSQL-gated.
406. G16/G18 Greenhouse contract parity: Greenhouse extraction now emits native `inputType` and provider-neutral `semanticKey` metadata alongside its existing accessible-name/kind extraction, matching the shared identity and semantic-mapping contract used by Lever. Greenhouse and form-intelligence tests, API/job-engine typechecks, root lint, and diff checks are required evidence; live provider/persistence execution remains gated.
407. G18 hostile-instruction boundary: provider-neutral field assessment now rejects explicit webpage instruction patterns before profile-derived mapping, including attempts to ignore policy, reveal credentials, bypass verification, fabricate answers, or upload unauthorized content. Form-intelligence tests pass 29/29; API/job-engine typechecks, root lint, and diff checks pass. Live provider execution remains gated.
408. G18 dynamic-removal correctness: post-fill form results now treat the refreshed DOM as authoritative, preventing conditional controls removed during interaction from remaining as stale required blockers. Form-intelligence tests pass 30/30; job-engine/API typechecks, root lint, and `git diff --check` pass. Live provider execution remains gated.
409. Aggregate regression verification: bounded serial `npm test -- --run` completed with 64 files and 508 tests passed, plus 25 files and 108 explicitly skipped. Skipped PostgreSQL/Redis/S3/Docker/live-provider suites remain gated; no integration or final E2E completion is inferred.
410. G16/G17 local browser verification: Greenhouse and Lever Chromium fixture suites completed with 19/19 tests passed after shared dynamic-field and semantic metadata changes. These are deterministic local fixtures only; live provider, PostgreSQL, Redis, S3, and final E2E evidence remain gated.
411. Infrastructure gate recheck: database and queue integration runners were executed; PostgreSQL returned `GATED` because Docker is unavailable, and Redis returned `GATED` because no writable Redis endpoint is available. No integration tests were run or counted as passed.
412. G18 control-transport hardening: the shared form adapter now validates runtime option value shapes and rejects multi-value answers for single-value comboboxes before invoking provider transport. Form-intelligence tests pass 31/31; job-engine typecheck, root lint, and `git diff --check` pass. Live provider execution remains gated.
413. Provider/form regression verification: the shared form-intelligence suite and local Greenhouse/Lever Chromium fixtures completed with 50/50 tests passed; root lint and diff checks also pass. Live provider, database, queue, S3, and final E2E evidence remain gated.
414. Workspace build verification: API, web, and queue production builds completed successfully after the latest form-intelligence and provider changes; Vite transformed 1503 modules and `git diff --check` passed. Infrastructure, live provider, and final E2E evidence remain gated.
415. G20/G21 unknown-outcome state hardening: executor-level indeterminate submission outcomes now transition a `SUBMISSION_PENDING` application to `UNCONFIRMED` atomically before persisting `OUTCOME_UNKNOWN`, making the record eligible for independent verification instead of stranding it outside the verifier state graph. The gated integration expectation was updated; live PostgreSQL execution remains unavailable.
416. Aggregate regression after unknown-outcome hardening: bounded serial `npm test -- --run` completed with 64 files and 509 tests passed, plus 25 files and 108 explicitly skipped. PostgreSQL/Redis/S3/Docker/live-provider suites remain environment-gated; no skipped integration test is counted as passed.
417. G20 approval-boundary consistency: submission authorization now rejects reviewed answers whose persisted source is unsupported, whose provenance source disagrees, or whose version is malformed, closing a preflight-to-provider source-laundering gap. Boundary tests pass 2/2 and API typecheck/diff checks pass; durable PostgreSQL execution remains gated.
418. Workspace regression after G20 hardening: root lint and all workspace builds completed successfully, including API, web (Vite 1503 modules), and queue packages. The readiness report now records the current aggregate evidence of 64 files/509 passed and 25 files/108 explicitly gated.
419. G18 current-value-aware required controls: the shared form contract now carries bounded current control values, and both Greenhouse and Lever DOM ports extract selected options, checked state, selected radio values, and existing text. Required blockers now respect safe values already present in the live form. Focused shared/provider-port tests pass 45/45; job-engine/API typechecks, lint, and diff checks pass. Durable cross-provider persistence remains gated.
420. G19/G20 exact cover-letter artifact transport: provider submission now selects at most one application-attached `COVER_LETTER` object, validates tenant ownership, clean scan state, expiry, private metadata, and integrity through the existing storage boundary, then uploads that exact object when a provider exposes a cover-letter file control. Missing/ambiguous attachments fail closed. Provider-submission tests pass 12/12; API typecheck and diff checks pass. Live S3/PostgreSQL evidence remains gated.
421. G19 storage regression and environment audit: document storage, retention, and authenticated artifact-route tests pass 56/56; API typecheck, root lint, and diff checks pass. The repository Compose file contains PostgreSQL, Redis, MinIO, and private-bucket initialization, but Docker cannot connect to its local daemon; PostgreSQL/MinIO ports are unavailable and Redis is reachable but remains non-writable for the queue gate. No infrastructure integration result is counted as passed.
422. Aggregate regression after exact cover-letter transport: bounded serial `npm test -- --run` completed with 64 files and 511 tests passed, plus 25 files and 108 explicitly skipped. No PostgreSQL/Redis/S3/live-provider result was inferred from the aggregate run.
423. G19/G20 immutable cover-letter binding: submission authorization now records the exact attached cover-letter object reference (or explicit absence), and provider execution rejects attachment drift before opening a browser. Cover-letter transport and submission-boundary tests pass 14/14; API typecheck, lint, and diff checks pass. Live persistence remains gated.
424. G16 provider-form integration: the existing shared Greenhouse/Lever application orchestration now loads at most one application-attached immutable `COVER_LETTER` object and uses the existing authenticated storage read boundary to upload it when a provider exposes a cover-letter file control; absent/invalid artifacts remain review-blocking rather than falling back to mutable content. API typecheck passes; the application-service integration files are explicitly skipped because PostgreSQL/Docker is unavailable.
425. G16/G17 workspace verification after cover-letter integration: root lint, API/web/queue production builds, and `git diff --check` completed successfully; web Vite transformed 1503 modules. Application-service integration tests remain explicitly skipped and are not counted as provider/database evidence.
426. G16 cover-letter transport unit coverage: added non-database tests for clean exact-artifact upload and fail-closed behavior when the approved artifact is absent; the focused file passes 2 tests with its 1 PostgreSQL-gated test explicitly skipped. API typecheck and diff checks pass.
427. G16/G17 provider-fixture regression: local Chromium Greenhouse/Lever fixtures plus shared form intelligence and both DOM-port suites completed with 64/64 tests passed. These are deterministic local fixtures; PostgreSQL/Redis/S3 and external-provider evidence remain gated.
428. G16/G17 duplicate-upload safety: pre-submission resume and cover-letter transport now fail closed when more than one matching file control is detected, preserving the required blocker and performing no storage read or upload. New focused tests pass 3/3 with 1 PostgreSQL-gated test skipped; API typecheck, lint, and diff checks pass.
429. G16 duplicate-text safety: shared provider orchestration now fails closed when more than one cover-letter textarea is detected, preserving all matching blockers for human review instead of writing identical content to multiple controls. Focused application-service tests pass 4/4 with 1 PostgreSQL-gated test skipped; API typecheck, lint, and diff checks pass.
430. G16 durable evidence parity: form-step audit evidence now records the exact immutable cover-letter object reference alongside the resume reference, including object identity, checksum, type, MIME, size, and scan status. Provider/submission regression tests pass 16/16 with 1 PostgreSQL-gated test skipped; API typecheck, lint, and diff checks pass.
404. G19 integration gate recheck: the repository S3 runner was executed and returned `GATED` because no reachable Docker daemon is available; no S3 integration tests were run or counted as passed. Unit/storage integrity evidence remains valid, but private-object integration, authorization, and retention evidence remain open.
431. G19 encryption metadata boundary: authorized document reads, signed access, and deletion now carry the configured encryption reference through route-selected metadata, while stored metadata validation rejects explicitly missing or malformed encryption references. Document-storage and authenticated-route tests pass 54/54; API typecheck and root lint pass. S3/PostgreSQL integration remains gated because Docker and MinIO are unavailable.
432. G16/G20 durable document evidence parity: submission execution, provider activation, failure, unknown-outcome, and stale-recovery records now project both the immutable resume and cover-letter references from authorization preflight evidence. Provider/submission tests pass 12/12 with 4 PostgreSQL-gated; API typecheck and diff checks pass. Durable database verification remains gated.
433. Workspace build regression after evidence hardening: API, web, and queue production builds pass; Vite transforms 1503 modules. No infrastructure or external-provider completion status is inferred from this build.
434. G20 provider boundary hardening: `ProviderSubmissionService.execute` now rejects malformed worker, tenant, application, authorization, and correlation identifiers before tenant/database or browser work. Focused provider tests pass 13/13; API typecheck and diff checks pass. Durable submission execution remains PostgreSQL/provider-integration gated.
435. G20 provider preparation regression: malformed execution identifiers are covered at the provider boundary with no tenant, database, or browser invocation; provider tests pass 13/13 and API typecheck/diff checks pass. Live submission and independent verification remain infrastructure/provider gated.
436. G19 duplicate-upload integrity: immutable document replay now requires the stored encryption reference to match the upload configuration, preventing same-checksum metadata convergence across encryption policies. Document-storage tests pass 46/46; API typecheck and diff checks pass. S3/PostgreSQL integration remains gated.
437. G19 authorized-storage encryption boundary: worker reads, signed URLs, and authorized deletion now fail closed when persisted encryption metadata is absent, before any object-store operation. Storage and authenticated-route tests pass 56/56; API typecheck and diff checks pass. S3/PostgreSQL integration remains gated.
438. G19 resume-download boundary: the authenticated resume download route now selects and forwards the persisted encryption reference into the authorized signed-URL path, preventing a metadata-incomplete resume from being presigned. API typecheck, root lint, and diff checks pass; route/database integration remains gated.
439. G19 retention/security regression: the shared authorized storage boundary now rejects missing encryption metadata before worker reads, signed access, or deletion, and the authenticated resume route supplies that metadata. Storage/route tests pass 56/56; API typecheck, lint, and diff checks pass. Docker/S3/PostgreSQL integration remains gated.
440. G19 retention authorization: expiry cleanup now uses the owner-bound `deleteAuthorized` boundary and selects complete immutable metadata, so malformed or cross-tenant records are reported as failed instead of being deleted through a weaker path. Retention/runtime tests pass 7/7; API typecheck and diff checks pass. PostgreSQL/S3 integration remains gated.
441. G22 scheduler boundary hardening: top-level due-schedule execution now validates timestamps and per-user limits before loading users from PostgreSQL. Scheduler tests pass 11/11; API typecheck and diff checks pass. Durable multi-user scheduling remains PostgreSQL-gated.
442. G19 retention runtime integration: the retention runtime and cleanup worker now require the owner-bound `deleteAuthorized` storage contract end-to-end, preventing expiry deletion from bypassing immutable metadata/encryption validation. Retention/runtime tests pass 7/7; API typecheck and diff checks pass. PostgreSQL/S3 execution remains gated.
443. G22 durable claim fail-closed behavior: per-user scheduler claims now reject malformed user identifiers and limits with explicit `SchedulerError` values instead of silently returning no work. Scheduler tests pass 12/12; API typecheck and diff checks pass. PostgreSQL multi-worker claim evidence remains gated.
444. G23 notification payload hardening: untrusted outbox payload fields persisted into notifications now require bounded, non-empty text for provider, classification, confidence, verification type, and application identifiers. Notification consumer tests pass 24/24; API typecheck and diff checks pass. Durable outbox/database delivery remains infrastructure-gated.
445. G24 email-outcome boundary hardening: optional application IDs and lifecycle-link identifiers now require bounded runtime-safe strings before tenant queries or state-machine transitions. Email outcome/lifecycle tests pass 10/10; API typecheck and diff checks pass. Mailbox/database integration remains infrastructure-gated.
446. G25 offer lifecycle integrity: durable offer recording now rejects expiry timestamps earlier than the offer start date, preventing temporally invalid offer records from entering the application pipeline. Lifecycle/email tests pass 12/12; API typecheck and diff checks pass. PostgreSQL lifecycle persistence remains gated.
447. G26 analytics integrity: ATS performance bucket aggregation now ignores non-finite and out-of-range scores instead of assigning fabricated performance metrics. Analytics tests pass 6/6; API typecheck and diff checks pass. Dashboard database aggregation remains infrastructure-gated.
448. G27 browser SSRF hardening: the existing Goal 15 navigation policy now rejects non-standard HTTPS ports on allowlisted hosts, while retaining per-request DNS resolution and blocked-address checks. Browser-policy tests pass 11/11; API typecheck, lint, and diff checks pass. Live browser/provider infrastructure remains gated.
449. G28 trace-identifier boundary: the shared structured logger now drops malformed correlation, application, automation-job, provider, and user identifiers even when called outside HTTP request normalization, preventing newline/path injection into trace records. Observability tests pass 5/5; API typecheck, root lint, and diff checks pass. External telemetry and alert-drill evidence remain gated.
450. G31 backup transport hardening: production database backup and guarded restore scripts now require an explicit TLS `sslmode` for non-local PostgreSQL targets and propagate the validated mode to `pg_dump`/`pg_restore` through `PGSSLMODE`. Script syntax, database-integrity tests 2/2, and diff checks pass; live backup/restore drills remain unavailable because PostgreSQL/Docker are offline.
451. G31 backup-role correctness: backup and restore now prefer the documented owner-scoped `DATABASE_ADMIN_URL`, falling back to `DATABASE_URL` only for existing development setups, preventing routine application credentials from being used for operational database jobs. Script syntax, database-integrity tests 2/2, and diff checks pass; live backup/restore remains infrastructure-gated.
452. G31 operational connection contract: extracted shared database URL selection, production TLS enforcement, and `PGSSLMODE` propagation for backup/restore, with executable coverage for admin-role preference, TLS rejection, and development fallback. Database connection/integrity tests pass 5/5; script syntax, API typecheck, lint, and diff checks pass. Live backup/restore remains PostgreSQL-gated.
453. G28 failure trace parity: automation failure events now reuse the authoritative leased job lookup to include bounded application/provider context while the structured logger continues to redact payload content. Automation-worker plus observability tests pass 14/14; API typecheck, root lint, and diff checks pass. External telemetry and alert-drill evidence remain gated.
454. G30 aggregate regression: the bounded serial workspace suite completed with 66 files and 528 tests passed, plus 24 files and 108 explicitly skipped. PostgreSQL/Redis/S3/Docker, live-provider, recovery-drill, and final-E2E suites remain environment-gated; skipped tests are not counted as passes.
455. G31 frontend truthfulness: application details no longer claim browser automation and retry controls are unimplemented when no attempts exist; the UI now reports the actual persisted state that no browser attempts have yet been recorded. Web typecheck, root lint, and diff checks pass.
456. G31 frontend capability messaging: Automation view now accurately describes persisted browser attempts, certified-provider boundaries, approved-data/exact-document controls, and human-verification pauses instead of claiming the backend never launches browser automation or submits applications. Web typecheck, lint, Vite build (1503 modules), and diff checks pass.
457. G31 frontend capability regression: audited remaining UI capability claims against the API; corrected Automation view’s stale blanket limitation while retaining the accurate distinction that starting an assisted run alone does not submit an application. Web typecheck, Vite build (1503 modules), root lint, and diff checks pass.
458. G31 readiness evidence correction: updated `PRODUCTION_READINESS.md` to the latest bounded aggregate result of 66 passing files/528 tests and 24 explicitly skipped files/108 tests, while preserving all live-infrastructure, recovery, telemetry, and final-E2E blockers. Stale-count search and `git diff --check` pass.
459. G28 metric integrity: the shared structured logger now drops non-finite or negative duration/retry metrics before emission, preventing invalid values from contaminating operational telemetry while preserving valid numeric metadata. Observability tests pass 6/6; API typecheck, root lint, and diff checks pass. External telemetry validation remains gated.
460. G31 build regression fix: added the declaration contract for the shared database-connection `.mjs` helper after the workspace build exposed an API `TS7016` failure. API, web (1503-module Vite build), and queue production builds now pass; root lint and diff checks pass. Live deployment and infrastructure evidence remain gated.
461. G31 runtime configuration completeness: `.env.example` now declares the production-required `REDIS_URL` and `BULLMQ_PREFIX`, matching the worker’s Redis/TLS validation and queue prefix contract. Worker/queue focused tests pass 20/20; API and queue typechecks, root lint, and diff checks pass. Live Redis execution remains gated.
462. G31 AI runtime gate: production API startup now requires `AI_API_KEY`, matching the actual AI provider constructor’s fail-closed credential requirement; focused runtime configuration tests pass 8/8, API typecheck, root lint, and diff checks pass. Live AI-provider invocation remains unverified.
463. G31 AI provider configuration gate: production startup now rejects unsupported `AI_PROVIDER` values before serving requests, matching the currently implemented OpenAI-only provider factory. Runtime configuration tests pass 9/9; API typecheck, root lint, and diff checks pass. Live AI-provider invocation remains unverified.
464. G31 AI configuration consistency: the OpenAI provider factory now trims and lowercases `AI_PROVIDER` exactly as startup validation does, preventing whitespace-padded valid configuration from failing on first use. Provider test passes 1/1; AI typecheck, root lint, and diff checks pass. Live provider invocation remains unverified.
465. G30 aggregate regression: after AI provider configuration hardening, the bounded serial workspace suite completed with 67 files and 532 tests passed, plus 24 files and 108 explicitly skipped. PostgreSQL/Redis/S3/Docker, live-provider, recovery-drill, and final-E2E evidence remain gated; skipped tests are not counted as passes.
466. G31 dependency audit: offline `npm audit --audit-level=high` reports 0 vulnerabilities from the available advisory cache; the online registry audit endpoint failed in this environment and is not represented as a live audit pass.
467. Workspace static gate: all configured workspace typechecks (API, web, AI, job-engine, queue, resume-engine, security, and types) completed successfully, and offline high-severity npm audit reports 0 vulnerabilities. This does not replace live infrastructure, provider, recovery, or final-E2E evidence.
468. G18 untrusted validation boundary: the provider-neutral form extractor now rejects non-string validation payloads, trims empty messages, and bounds retained error text before shared evidence or UI handling. Form-intelligence tests pass 33/33; job-engine/API typechecks, root lint, and diff checks pass. Durable provider persistence remains infrastructure-gated.
469. G18 malformed collection boundary: shared form extraction now treats non-array snapshot or field validation collections as empty rather than throwing, preserving fail-closed behavior for malformed provider adapter data. Form-intelligence tests pass 34/34; job-engine typecheck, root lint, and diff checks pass.
470. G18 field-collection boundary: provider-neutral extraction now treats a malformed non-array `snapshot.fields` value as an empty form, preserving the downstream fail-closed requirement for automatic submission. Form-intelligence tests pass 35/35; job-engine typecheck, root lint, and diff checks pass.
471. G18 record-shape boundary: provider-neutral extraction now ignores malformed individual field and validation records before property access, preventing hostile adapter payloads from throwing or entering shared evidence. Form-intelligence tests pass 36/36; job-engine typecheck, root lint, and diff checks pass.
472. G18 port-validation boundary: `ApplicationFormAdapter` now sanitizes `port.validate()` results through the shared bounded validation contract, preventing malformed provider validation payloads from throwing during step advancement. Form-intelligence tests pass 37/37; job-engine typecheck, root lint, and diff checks pass.
473. G18 identity-metadata boundary: stable question identity generation now normalizes invalid provider and step metadata to `UNKNOWN`/`default` instead of throwing on malformed dynamic provider snapshots. Form-intelligence tests pass 38/38; job-engine typecheck, root lint, and diff checks pass.
474. G18 snapshot boundary: the shared `ApplicationFormAdapter` now normalizes malformed detector snapshots before extraction, answer resolution, validation, or advancement. Invalid provider/step metadata, field collections, validation collections, and truthy non-boolean advancement flags fail closed to an empty non-advancing form. Form-intelligence tests pass 39/39; job-engine typecheck, root lint, and diff checks pass. Durable provider persistence remains infrastructure-gated.
475. G17/G19 offline regression gate: Lever adapter/service/port and local fixture coverage passes 16/16 runnable tests with 1 fixture test environment-gated; document storage, authorization routes, and retention runtime coverage passes 63/63. These fixture/unit results do not replace live PostgreSQL/Redis/S3 or external-provider evidence.
476. G30 aggregate regression: the latest bounded serial workspace suite completed with 67 files and 539 tests passed, plus 24 files and 108 explicitly skipped by infrastructure gates. No skipped test is counted as passed; PostgreSQL/Redis/S3/Docker, live-provider, recovery-drill, and final-E2E evidence remain unavailable.
477. G18 metadata hardening: provider-neutral snapshot normalization now rejects oversized step identities before they can expand durable question identities or audit payloads. Form-intelligence tests pass 40/40; job-engine typecheck, root lint, and diff checks pass. Live persistence remains infrastructure-gated.
478. G18 identity-contract hardening: `stableQuestionIdentity` now independently bounds and trims step metadata, protecting direct shared-layer callers as well as adapter-normalized snapshots. Form-intelligence tests pass 41/41; job-engine typecheck and diff checks pass. Live cross-provider persistence remains gated.
479. G18 untrusted collection limits: provider-neutral normalization and direct extraction now cap dynamic fields at 500 and validation records at 200 before identity, policy, or evidence processing. Form-intelligence tests pass 42/42; job-engine typecheck, root lint, and diff checks pass. Live cross-provider persistence remains gated.
480. G18 detector boundary: `defaultFormDetector` now normalizes provider snapshots itself, preventing direct detector consumers from bypassing malformed metadata, collection limits, or non-advancing fail-closed behavior. Form-intelligence tests pass 43/43; job-engine typecheck, root lint, and diff checks pass. Live persistence remains infrastructure-gated.
481. G17 provider-host boundary: the shared Greenhouse/Lever application-host helper now rejects embedded URL credentials and nonstandard ports before browser-session creation. Provider adapter tests pass 9/9; job-engine typecheck, root lint, and diff checks pass. Live provider execution remains gated.
482. G18 answer-provenance boundary: the provider-neutral approved-answer resolver now rejects oversized, over-keyed, or non-serializable provenance before an answer can influence form filling, matching the API’s bounded provenance contract. Form-intelligence tests pass 43/43; job-engine typecheck, root lint, and diff checks pass. Live persistence remains gated.
483. G18 validation-output boundary: field-level and merged provider validation results are now capped at the shared bounded error limit before required-field decisions, advancement, or evidence persistence. Form-intelligence tests pass 43/43; job-engine typecheck, root lint, and diff checks pass. Live persistence remains gated.
484. Infrastructure probe: a disposable native Redis instance on isolated port 6381 passed `PING`, writable `SET`, and `GET`, then was terminated cleanly. Repository queue integration remains gated because PostgreSQL is unavailable; no queue integration test is counted as passed.
485. G17 adapter isolation: `LeverApplicationAdapter` now rejects cross-provider or malformed snapshots at its detector boundary, preventing a Lever worker from applying shared fill logic to a non-Lever form. Lever plus shared intelligence tests pass 47/47; job-engine typecheck, root lint, and diff checks pass. Live provider execution remains gated.
486. G30 aggregate regression: after Lever adapter isolation, the bounded serial workspace suite completed with 67 files and 544 tests passed, plus 24 files and 108 explicitly skipped by infrastructure gates. No skipped test is counted as passed; PostgreSQL/Redis/S3/Docker, live-provider, recovery-drill, and final-E2E evidence remain unavailable.
487. G17 provider-neutral orchestration contract: exposed `ProviderApplicationOutcome`, `ProviderApplicationError`, and `ExecuteProviderApplicationInput` aliases while retaining Greenhouse compatibility, and updated the Lever service to consume the provider-neutral input contract. API/job-engine typechecks, provider tests (4 runnable, 1 database-gated), root lint, and diff checks pass.
488. G17 provider-boundary isolation: both Greenhouse and Lever adapters now reject explicit cross-provider snapshots while accepting legacy provider-omitted fixture snapshots through one normalized shared boundary. Provider plus shared intelligence tests pass 54/54; job-engine typecheck, root lint, and diff checks pass. Live provider execution remains gated.
489. G19 signed-access boundary: the low-level document signed-download method now requires an authenticated owner and complete immutable metadata before presigning, eliminating an owner-unbound public path while preserving the authorized route. Storage/route tests pass 56/56; API typecheck, root lint, and diff checks pass. Live S3 authorization remains gated.
490. G19 signed-access availability gate: low-level presigning now also requires clean scan status, non-deleted/non-expired metadata, and explicit availability fields, preventing unavailable objects from receiving signed access. Storage/route tests pass 56/56; API typecheck, root lint, and diff checks pass. Live S3 authorization remains gated.
491. G19 resume-download expiry boundary: the authenticated resume download route now carries `ObjectMetadata.expiresAt` into the owner-bound signed-access check, so expired resume objects cannot be presigned through that route. API typecheck, storage/route/retention tests pass 59/59, root lint, and `git diff --check` pass. Live S3/PostgreSQL authorization remains gated.
492. Workspace regression gate: API/web/queue production builds completed successfully; the bounded serial workspace suite completed with 67 files and 545 tests passed, plus 24 files and 108 explicitly skipped by infrastructure gates. No skipped test is counted as passed; PostgreSQL, S3/MinIO, live provider, recovery-drill, and final autonomous E2E evidence remain unavailable.
493. G19 application-binding hardening: authenticated cover-letter uploads that include an application ID now require and persist that application’s exact `resumeVersionId`; mismatched or omitted bindings are rejected before storage access. Document route tests pass 11/11; API typecheck, root lint, and `git diff --check` pass. Live PostgreSQL/S3 authorization remains gated.
494. G19 transaction-bound ownership hardening: `persistDocumentMetadataInTransaction` now verifies any supplied `resumeVersionId` and `resumeId` belong to the authenticated owner before creating immutable object metadata, closing non-HTTP worker/service bypasses. Document storage and route tests pass 58/58; API typecheck, root lint, and `git diff --check` pass. Live PostgreSQL/S3 authorization remains gated.
495. G16/G20 exact cover-letter binding: submission authorization and provider execution now require an attached cover-letter object to match the application’s exact `resumeVersionId`; the immutable preflight evidence and replay comparison retain and verify that binding. Provider-submission tests pass 13/13; API typecheck, root lint, and `git diff --check` pass. Database-backed replay and live provider evidence remain gated.
496. G16/G20 cover-letter binding regression: added a provider execution test proving a cover-letter object from a different resume version is rejected before browser/document reads. Provider-submission tests pass 14/14; API typecheck, root lint, and `git diff --check` pass. Database-backed replay and live provider evidence remain gated.
497. Workspace regression gate: after exact cover-letter binding changes, the bounded serial workspace suite completed with 67 files and 548 tests passed, plus 24 files and 108 explicitly skipped by infrastructure gates. No skipped test is counted as passed; PostgreSQL, S3/MinIO, live provider, recovery-drill, and final autonomous E2E evidence remain unavailable.
498. Infrastructure re-probe: the Docker client is installed but no active Docker server/services are available; PostgreSQL migration verification reaches `localhost:5432` but cannot establish the database integration gate; default Redis responds only with its existing `MISCONF` write-stop error; S3 port 9000 is closed. No infrastructure-dependent test is counted as passed.
499. G24 classifier safety: email outcome classification now aggregates matching rules and fails closed to `UNCLASSIFIED` when contradictory outcome classes are present, preventing rule-order bias from silently turning mixed content into an offer, rejection, or interview state. Email classifier tests pass 10/10; API typecheck, root lint, and `git diff --check` pass. Live mailbox/provider integration remains gated.
500. G28 log-redaction hardening: the existing structured-log sanitizer now redacts common cloud and bearer credential keys including `apiKey`, `accessKey`, `accessToken`, `refreshToken`, and `privateKey`. Observability tests pass 7/7; API typecheck, root lint, and `git diff --check` pass. External telemetry and production alert-drill evidence remain gated.
501. G19 immutable binding hardening: duplicate and concurrent object-metadata reconciliation now compares `resumeId` and `resumeVersionId` in addition to checksum, object, MIME, size, encryption, and scan metadata, preventing identical bytes from replaying under a different resume-version binding. Document-storage tests pass 48/48; API typecheck, root lint, and `git diff --check` pass. Live PostgreSQL/S3 concurrency evidence remains gated.
502. G28/G24 regression gate: contradictory email outcome handling remains fail-closed, and the structured logger now redacts common cloud/bearer credential keys. Observability tests pass 7/7; email classifier tests pass 10/10; API typecheck, root lint, and `git diff --check` pass. External mailbox/telemetry and infrastructure evidence remain gated.
503. G24/G25 review gate: `applyEmailOutcome` now requires durable `reviewedAt` and authenticated `reviewedBy` evidence before any email classification can mutate application lifecycle state. Unreviewed outcomes fail closed before transition; lifecycle/classifier tests pass 12/12; API typecheck, root lint, and `git diff --check` pass. Live mailbox and PostgreSQL lifecycle evidence remain gated.
504. G24/G25 relink-review integrity: relinking an email outcome to a different application now clears prior review metadata, forcing fresh human review before lifecycle mutation against the new target. Lifecycle tests pass 3/3; API typecheck, root lint, and `git diff --check` pass. Live mailbox and PostgreSQL lifecycle evidence remain gated.
505. G23 verification notification integrity: human-verification outbox events now carry the exact durable verification ID, and notification mapping preserves it while remaining backward-compatible with older events by using the aggregate ID. Notification consumer tests pass 24/24; API typecheck, root lint, and `git diff --check` pass. Live notification delivery and PostgreSQL outbox evidence remain gated.
506. G23 hostile-payload regression: notification mapping rejects an explicitly malformed verification ID and preserves aggregate-ID fallback for legacy human-verification events, preventing untrusted payloads from redirecting the user to an arbitrary verification record. Notification consumer tests pass 24/24; API typecheck, root lint, and `git diff --check` pass. Live notification delivery and PostgreSQL outbox evidence remain gated.
507. G23 frontend handoff: notification records now expose bounded structured data, and clicking a human-verification notification opens the existing Human Review Room only when its durable verification ID is valid; other notifications retain read-state behavior. Web typecheck/build, root lint, and `git diff --check` pass. Live SSE/outbox delivery and browser UI evidence remain gated.
508. Workspace regression gate: after the notification handoff changes, the bounded serial Vitest workspace run completed with 67 files and 553 tests passed, plus 24 files and 108 tests explicitly skipped by infrastructure gates. No skipped test is counted as passed; PostgreSQL, Redis/S3, live-provider, recovery-drill, and final autonomous-E2E evidence remain unavailable.
509. G23 SSE resource boundary: the authenticated live-event endpoint now caps concurrent connections per tenant at 10 and returns a bounded 429 response when capacity is exhausted; existing tenant-scoped replay and payload-size limits remain unchanged. SSE boundary tests pass 3/3; API typecheck, root lint, and `git diff --check` pass. Live multi-instance SSE evidence remains gated.
510. G21 verifier input hardening: submission-attempt selection now fails closed on malformed runtime timestamps, and independent-verification evidence rejects oversized application identities before database access. Submission-verification tests pass 21/21; API typecheck, root lint, and `git diff --check` pass. PostgreSQL transition/reconciliation and live-provider evidence remain gated.
511. G24/G25 explicit review workflow: added an authenticated, tenant-scoped `reviewEmailOutcome` action and `/email-outcomes/:id/review` endpoint that records `reviewedAt`/`reviewedBy`, audits the non-lifecycle review, and idempotently replays same-user review; the application UI now requires this explicit review before offering lifecycle application. Lifecycle/route/web API tests pass 32/32; API/web typechecks, root lint, and `git diff --check` pass. Live mailbox and PostgreSQL lifecycle evidence remain gated.
512. G22/G23 scheduler boundary audit: verified durable schedules enqueue real discovery jobs through PostgreSQL-backed `AutomationJob` records, while the existing application daily-budget reservation remains owned by the quality gate; no duplicate scheduler authority was introduced. No new implementation was warranted from this audit; live scheduler persistence and application-run integration remain gated.
513. G24/G25 review verification: the explicit email-outcome review workflow and lifecycle boundary pass 18/18 focused API tests; web production build, API/web typechecks, root lint, and `git diff --check` pass. Live mailbox, PostgreSQL lifecycle, and provider evidence remain gated.
514. G28 observability metrics: `/api/automation/metrics` now reports tenant-scoped retry totals/jobs-with-retries from durable `attemptCount` groups and bounded execution-duration sample/average/maximum from persisted application attempts; malformed or incomplete windows are excluded. Observability summary tests pass 2/2; API typecheck, root lint, and `git diff --check` pass. Live telemetry backend and database-scale evidence remain gated.
515. G28 browser-session observability: automation metrics now also report bounded completed browser-session duration sample/average/maximum using persisted session lifecycle timestamps, alongside status counts. Observability summary tests pass 2/2; API typecheck, root lint, and `git diff --check` pass. Live telemetry backend and database-scale evidence remain gated.
516. G28 retry-metric accuracy: retry summaries now derive `jobsWithRetries` from durable `attemptCount > 1` groups, avoiding false counts from pending jobs with zero attempts; the duration helper also validates both lifecycle timestamps before aggregation. Observability summary tests pass 2/2; API typecheck, root lint, and `git diff --check` pass. Live telemetry backend and database-scale evidence remain gated.
517. G28 browser-session duration surface: `/api/automation/metrics` now includes bounded completed browser-session duration summaries alongside job/attempt durations and retry metrics, all derived inside the authenticated tenant transaction. Observability summary tests pass 2/2; API typecheck, root lint, and `git diff --check` pass. Live telemetry backend and database-scale evidence remain gated.
518. G31 bounded API shutdown: added `closeHttpServer`, which bounds graceful listener drain, closes remaining connections on timeout, and always disconnects Prisma through the existing signal path. HTTP shutdown tests pass 11/11; API typecheck, root lint, and `git diff --check` pass. Live deployment/drain and rollback evidence remain gated.
519. G23 live-event delivery: the notification outbox runtime now broadcasts each committed, tenant-owned notification to connected SSE clients with the deterministic notification ID, allowing resumable cursors; unsupported/unconsumed events are not broadcast. Runtime/SSE tests pass 6/6; API typecheck, root lint, and `git diff --check` pass. Live multi-instance delivery and PostgreSQL outbox evidence remain gated.
520. G23 SSE cursor recovery: the web live-event client now sends a bounded `Last-Event-ID`, parses bounded SSE `id:` fields, and reconnects with exponential backoff while preserving the last durable cursor; authenticated failures stop retrying and aborted views clean up timers. Web API tests pass 14/14; workspace typechecks, lint, build, and `git diff --check` pass. Live multi-instance replay and PostgreSQL outbox evidence remain gated.
521. G23 initial replay race: the automation view now loads persisted event history and captures its newest bounded event ID before opening the first SSE connection, preventing the initial reconnect cursor from racing the history request. Web typecheck/build, root lint, focused web API tests (14/14), and `git diff --check` pass. Live multi-instance replay and PostgreSQL outbox evidence remain gated.
522. G23 client cursor tests: added focused web tests covering authenticated SSE cursor forwarding, event-ID parsing, and rejection of oversized cursor values. Web API tests pass 16/16; web typecheck, root lint, and `git diff --check` pass. Live multi-instance replay and PostgreSQL outbox evidence remain gated.
523. Workspace regression after SSE recovery: the bounded serial Vitest workspace run completed with 68 files and 564 tests passed, plus 24 files and 108 tests explicitly skipped by infrastructure gates. No skipped test is counted as passed; PostgreSQL, Redis/S3, live-provider, recovery-drill, and final autonomous-E2E evidence remain unavailable.
524. G23 multi-instance bridge: added an optional Redis pub/sub bridge for tenant-scoped SSE notifications, with bounded envelope validation, origin fencing, API/worker lifecycle cleanup, and isolation of pub/sub failures from durable outbox acknowledgements. Bridge/runtime/SSE tests pass 9/9; API typecheck/build, root lint, and `git diff --check` pass. Live Redis multi-instance delivery remains environment-gated.
525. G23 bridge hardening: Redis error events are observed, configured channel names are bounded/allowlisted, and serialization failures fail closed before publish. Bridge/runtime/SSE tests pass 9/9; API typecheck/build, root lint, and `git diff --check` pass. Live Redis failover and multi-instance delivery remain environment-gated.
526. G23 Redis recovery: the SSE subscriber now reissues its channel subscription on each Redis `ready` event, recovering from an initial or later connection outage without restarting the API. Bridge/runtime/SSE tests pass 6/6; API typecheck, root lint, and `git diff --check` pass. Live Redis failover remains environment-gated.
527. G31 runtime dependency contract: production API startup now requires and validates `REDIS_URL`, matching the Redis-backed multi-instance SSE bridge; existing runtime configuration tests pass 11/11, API typecheck/build, root lint, and `git diff --check` pass. Live Redis connectivity and deployment evidence remain gated.
528. G23 disposable Redis probe: an isolated native Redis instance on port 6381 accepted a real `publishSseEvent` call and a separate subscriber received the tenant/user/event identity intact; the instance was shut down with `redis-cli shutdown nosave`. This validates the publish path only; subscriber failover, API multi-instance delivery, PostgreSQL outbox coupling, and production topology remain unverified.
529. G23 subscriber integration: added an environment-gated real Redis test that waits for bridge readiness and verifies a publisher representing another API instance reaches the local tenant broadcaster; the isolated native Redis run passed 1/1 and was cleaned up. API typecheck, root lint, and `git diff --check` pass. PostgreSQL outbox coupling and production failover topology remain unverified.
530. G23 readiness contract: the Redis SSE bridge now exposes an explicit readiness promise, preventing startup publish races while allowing the API to start without blocking on an unavailable Redis service. Production-config tests pass 12/12; isolated Redis subscriber integration passes 1/1; API typecheck, root lint, and `git diff --check` pass. PostgreSQL outbox coupling and production failover topology remain unverified.
531. G31 Redis startup gate regression: added coverage proving production configuration rejects missing Redis, rejects non-local plaintext Redis, and accepts local Redis or TLS Redis endpoints. Runtime configuration tests pass 12/12; API typecheck, root lint, and `git diff --check` pass. Live deployment and failover evidence remain gated.
532. Workspace regression after Redis bridge/readiness changes: the bounded serial Vitest workspace run completed with 69 files and 568 tests passed, plus 25 files and 109 tests explicitly skipped by infrastructure gates. No skipped test is counted as passed; PostgreSQL, Redis/S3, live-provider, recovery-drill, and final autonomous-E2E evidence remain unavailable.
533. G24/G27 credential-reference ownership: email consent now verifies any supplied credential reference against the authenticated user’s active, unrevoked durable credential before activating or updating a mailbox connection; syntax-only references can no longer cross tenants or point at revoked material. Email consent boundary tests pass 3/3; API typecheck, root lint, and `git diff --check` pass. Live database ownership/RLS evidence remains gated.
534. G24 credential-consent regression: added service-level tests for foreign/revoked credential rejection before upsert and successful activation with an active owner-bound credential. Email consent tests pass 5/5; API typecheck, root lint, and `git diff --check` pass. Live database ownership/RLS evidence remains gated.
535. G27 credential revocation cascade: revoking a durable credential now atomically revokes active email connections bound to that credential, clears their reference, and audits the dependent-connection count. Durable credential and email-consent tests pass 10/10; API typecheck, root lint, and `git diff --check` pass. Live PostgreSQL/RLS cascade evidence remains gated.
536. G28 queue metrics surface: the authenticated automation metrics endpoint now includes BullMQ counts from a lifecycle-managed queue registry while retaining PostgreSQL metrics when Redis is unavailable; API shutdown closes the registry. Automation metrics/server tests pass 14/14; API typecheck/build, root lint, and `git diff --check` pass. Live Redis queue telemetry remains environment-gated.
537. G28 frontend metrics surface: the Automation Control view now loads and renders real queue waiting/active/failed counts and durable retry totals from `/api/automation/metrics`, while displaying queue telemetry as unavailable rather than zero when Redis is down. Web API tests pass 16/16; web typecheck/build, root lint, and `git diff --check` pass. Live authenticated UI/SSE evidence remains gated.
538. G28 frontend metrics contract: added API coverage for durable retry/duration metrics and explicitly unavailable queue telemetry. Web API tests pass 17/17; web typecheck, root lint, and `git diff --check` pass. Live authenticated UI/SSE evidence remains gated.
539. G29 focused recovery regression: automation worker retry/completion fencing, queue lease renewal and retry preparation, dispatcher reconciliation/overlap/bounded shutdown, and scheduler lifecycle tests pass 31/31. Web automation telemetry now also exposes delayed queue work; web typecheck/build, root lint, and `git diff --check` pass. PostgreSQL/Redis crash drills, browser restart, and integrated recovery evidence remain gated.
540. G24/G25 lifecycle regression: email classification, tenant-owned ingestion/replay, explicit review/application gating, email synchronization, interview/offer lifecycle transitions, and analytics policy helpers pass 36/36 focused tests. Durable PostgreSQL lifecycle/outbox evidence and mailbox-provider integration remain gated.
541. G19 storage verification: private document upload validation, content-addressed duplicate/concurrent reconciliation, ownership/tenant checks, verified reads, signed-access authorization, retention deletion, tamper detection, malware-scan failure modes, and compensating cleanup tests pass 55/55 focused tests. The real S3 integration runner correctly reports GATED because no Docker daemon is reachable; no S3 integration result is counted as passed.
542. G18 provider-neutral form regression: stable semantic identities, accessible-name/label mapping, duplicate and dynamic controls, validation and multi-step handling, deterministic policy classes, hostile webpage fail-closed behavior, approved-answer provenance, and safe control filling pass 43/43 form-intelligence tests; job-engine typecheck and `git diff --check` pass. Durable database-backed answer evidence remains environment-gated.
543. Workspace regression after G18/G19/G28/G29/G24/G25 increments: all configured workspace typechecks, API/web production builds, root ESLint, and `git diff --check` pass. This remains static/source evidence only; PostgreSQL, MinIO/S3, live providers, crash drills, telemetry shipping, and final autonomous E2E remain gated.
544. G16/G17 provider regression: shared form adapters, Greenhouse and Lever provider boundaries, exact approved resume upload, provider validation/review/human-verification handling, and local fixture flows pass 40/40 tests; one Chromium fixture file is explicitly skipped by its executable gate (2 tests), not counted as passed. Live provider submission and durable PostgreSQL/S3 evidence remain gated.
545. G28 queue-age observability: BullMQ metrics now include a bounded `oldestWaitingMs` value derived from the oldest waiting/prioritized job, with `null` when queue age is unavailable; the Automation Control view surfaces it without converting unavailable telemetry to zero. Queue metrics and web API tests pass 19/19; queue/web typechecks, root lint, and `git diff --check` pass. Live queue telemetry remains gated.
546. G21/G20 verification regression: independent-verification, submission-boundary, provider-submission, and the explicitly gated database integration suites were exercised; 37 focused tests pass and 4 database-dependent tests remain skipped. Confirmation parsing continues to persist only bounded hashed evidence and requires a completed durable attempt; live PostgreSQL reconciliation remains gated.
547. G21 provider-response evidence: added a bounded provider-response parser and queue handoff that accepts only explicit success statuses plus a bounded confirmation/application identifier, hashes normalized metadata, and excludes arbitrary response content from durable evidence. Submission-verification tests pass 25/25; API typecheck, root lint, and `git diff --check` pass. Live verifier/database reconciliation remains gated.
548. G24 mailbox-boundary hardening: malformed connector messages now fail with bounded domain errors before any property access or ingestion, including non-string sender/subject/body and blank identities/content. Email sync/outcome tests pass 18/18; API typecheck, root lint, and `git diff --check` pass. Live mailbox-provider integration and cursor persistence remain gated.
549. G24 concurrent-sync safety: mailbox cursor advancement now uses an owner/status/previous-cursor compare-and-set, so a slower concurrent page cannot regress durable progress; audit records are written only after winning the cursor update. Sync concurrency tests pass 10/10; API typecheck, root lint, and `git diff --check` pass. Live PostgreSQL cursor persistence remains gated.
550. G24 sync timestamp hardening: optional mailbox sync timestamps are now validated as finite dates within bounded clock skew before provider ingestion or persistence. Email sync/concurrency tests pass 14/14; API typecheck, root lint, and `git diff --check` pass. Live mailbox and PostgreSQL evidence remain gated.
551. G22 scheduler input hardening: persisted discovery account identifiers now reject control characters before being passed into provider discovery scheduling, preserving the existing source/account allowlist and bounded deduplication. Scheduler tests pass 12/12; API typecheck, root lint, and `git diff --check` pass. Live scheduler persistence/concurrency remains gated.
552. G28 human-verification observability: authenticated automation metrics now include tenant-scoped pending verification count plus bounded oldest/average age summaries, and Automation Control displays the pending review count. Observability/API and web contract tests pass 21/21; API/web typechecks, root lint, and `git diff --check` pass. Live database telemetry remains gated.
553. G31 object-storage startup hardening: production `S3_ENDPOINT` validation now rejects embedded usernames/passwords and query/hash data in addition to requiring HTTPS, preventing secret-bearing endpoint configuration from entering the storage client. Runtime configuration tests pass 12/12; API typecheck, root lint, and `git diff --check` pass. Deployment/runtime validation remains gated.
554. G28 human-review age telemetry: the authenticated automation metrics endpoint now reports bounded pending human-verification count and age summaries from tenant-owned durable records; the web contract includes the new metric and Automation Control displays the count. API/web metrics tests pass 21/21; API/web typechecks, root lint, and `git diff --check` pass. Live database telemetry remains gated.
555. G31 backup artifact hardening: backup integrity now rejects empty, directory, and symlink artifacts before hashing or restore, and backup creation validates that `pg_dump` produced a non-empty regular file before writing its sidecar manifest. Focused backup-integrity tests pass 3/3; all three database scripts pass Node syntax checks and `git diff --check` passes. No live backup/restore or encryption-at-rest evidence is claimed.
556. G31 encrypted backup envelope: operational database backups now encrypt the custom-format dump with an authenticated AES-256-GCM envelope, manifest the encrypted artifact, remove the plaintext dump after creation, and decrypt only into a bounded temporary restore directory that is always removed. Backup-integrity/crypto tests pass 4/4; script syntax and `git diff --check` pass. Live backup/restore rehearsal remains gated.
557. G31 build/infrastructure gate: API, web, and queue production builds completed successfully; PostgreSQL and S3 integration runners both correctly returned environment-gated status because no reachable Docker daemon exists. No unavailable integration test is counted as passed.
558. G31 backup preflight: encrypted backup creation now validates `BACKUP_ENCRYPTION_KEY` before invoking `pg_dump`, preventing a database read and temporary plaintext dump when encryption prerequisites are absent. Backup-integrity/crypto tests pass 4/4; workspace typecheck and script syntax checks pass. Live backup/restore rehearsal remains gated.
559. G21 temporal evidence hardening: independent-verification attempt selection now rejects durable execution records whose completion precedes their start, preventing malformed intervals from authorizing confirmation evidence. Submission-verification tests pass 25/25; API typecheck, root lint, and `git diff --check` pass. PostgreSQL reconciliation remains gated.
560. G31 readiness wording correction: disaster-recovery documentation now reflects the implemented authenticated backup encryption while retaining the genuine production gaps around key management/rotation, restore rehearsal, and measured RPO/RTO.
561. G29 recovery timestamp hardening: automation-job lease, completion, failure, reconciliation, and cancellation paths now reject invalid `Date` inputs through one shared finite-time validator before durable mutation. Automation-job focused tests pass 5/5; API typecheck, root lint, and `git diff --check` pass. Live crash/restart evidence remains gated.
562. G30 aggregate regression: the fresh controlled workspace suite completed with 71 files and 592 tests passed, plus 25 files and 109 tests explicitly skipped by infrastructure gates. No skipped test is counted as passed; PostgreSQL, Redis/S3, live-provider, recovery-drill, and final autonomous-E2E evidence remain unavailable.
563. G22 scheduler boundary hardening: per-user durable schedule claims now reject trimmed/oversized/control-character user identifiers before tenant setup or advisory-lock construction. Scheduler tests pass 12/12; API typecheck, root lint, and `git diff --check` pass. Live scheduler persistence/concurrency remains gated.
564. G26 analytics input hardening: historical timeline and funnel endpoints now share a bounded scalar day-window parser, rejecting query arrays, decimals, and out-of-range values before database access. Analytics tests pass 7/7; API typecheck, root lint, and `git diff --check` pass. Live SQL/dashboard evidence remains gated.
565. G26 analytics completeness: the dashboard API now derives average application-creation-to-submission time from persisted `createdAt`/`appliedAt` intervals, ignores malformed or negative durations, and renders the value beside confirmation latency. Analytics tests pass 8/8; API/web typechecks, root lint, and `git diff --check` pass. Live SQL/dashboard evidence remains gated.
566. G23 SSE protocol hardening: replay and broadcast paths now validate event names and event IDs as bounded control-free protocol tokens before writing headers, closing the SSE injection path for malformed notification data. SSE tests pass 4/4; API typecheck, root lint, and `git diff --check` pass. Live multi-instance delivery remains gated.
567. G19 upload-failure safety: content-addressed private objects are no longer deleted after an uncertain or failed metadata transaction, preventing a concurrent successful upload from losing its object. Artifact attachment uses an owner/object scoped PostgreSQL transaction lock and no longer queries a transaction after a unique-constraint failure. Download URL selection now passes scan/deletion/expiry metadata to the authorization boundary. Focused storage, route, refresh, and security tests pass 76/76 with 4 PostgreSQL tests gated. Full serial suite was started but has not produced a terminal result yet; PostgreSQL and S3 integration wrappers are explicitly GATED because Docker is unavailable.
568. G19/G20 verification: the focused storage, upload-route, refresh-session, and security suite passes 76/76 with 4 database tests skipped by their explicit gate; API/workspace typecheck, build, lint, and diff checks pass. The full serial suite was interrupted after hanging without a terminal result and is not counted. PostgreSQL and S3 integration wrappers remain honestly GATED because Docker is unavailable.
569. G19 retention/RLS hardening: document-retention discovery now runs through the dedicated `withService`/`DATABASE_ADMIN_URL` maintenance boundary instead of querying RLS-protected metadata through the application client; production workers require that separate maintenance URL. Retention, runtime, worker, upload-recovery, and document-route tests pass 27/27; workspace typecheck and lint pass. Live PostgreSQL/S3 evidence remains gated.
570. G19 worker lifecycle hardening: the dedicated maintenance Prisma client now has an explicit disconnect path, and both normal and startup-failure worker shutdown paths release it before the application client. Worker/retention tests pass 12/12; workspace typecheck and lint pass. Live infrastructure evidence remains gated.
571. G19 maintenance-connection security: production worker startup now validates TLS requirements for `DATABASE_ADMIN_URL` independently from the application URL, preventing an unencrypted owner connection from bypassing the normal production database transport check. Worker configuration tests pass 6/6.
572. G17/G19 regression verification: provider-neutral form intelligence plus Lever adapter/service/port/fixture tests pass 60/60 runnable tests, with one Chromium fixture file explicitly skipped by its executable gate. Workspace typecheck and lint pass. A full-worktree `git diff --check --quiet` remains non-clean because of pre-existing line-ending warnings; no whitespace errors are present in the files changed in this increment.
573. G20 human-verification handoff: provider submission now invokes the existing durable HumanVerification flow when a trusted form assessment identifies CAPTCHA/MFA/anti-bot/auth verification, using only bounded provider/field/authorization identifiers and a deterministic idempotency key. The application state machine permits the safe `SUBMISSION_PENDING → WAITING_FOR_USER → READY_TO_SUBMIT` path; the consumed authorization requires explicit re-authorization after resolution. Provider/state tests pass 19/19; workspace typecheck and lint pass. Live durable transition evidence remains PostgreSQL-gated.
574. G20 verification lifecycle coverage: added a PostgreSQL-gated HumanVerification integration case for pausing an authorized submission checkpoint and resuming it to `READY_TO_SUBMIT` for explicit re-authorization. Provider/state tests pass 19/19; 4 database tests are skipped by the explicit infrastructure gate; workspace typecheck and lint pass.
575. G22 scheduler/RLS hardening: durable schedule execution now enumerates active tenants through the dedicated service maintenance boundary rather than the application Prisma client, preventing production RLS from silently suppressing all scheduled discovery. Scheduler tests pass 13/13; workspace typecheck and lint pass. Live PostgreSQL scheduling/concurrency evidence remains gated.
576. G23 notification outbox/RLS hardening: worker-wide outbox claim and lease-update operations now use the dedicated service maintenance DB boundary; per-event notification persistence remains tenant-scoped. Added publisher unit coverage for claim, lease completion, and service-only access. Notification/publisher tests pass 29/29; workspace typecheck and lint pass. Live multi-instance PostgreSQL/Redis delivery evidence remains gated.
577. G23/G24 regression verification: outbox publisher/consumer, notification runtime, email outcome, and mailbox sync tests pass 48/48; workspace typecheck and lint pass. Email ingestion remains tenant-scoped and idempotent; live mailbox and PostgreSQL/Redis delivery evidence remains gated.
578. G25 lifecycle concurrency hardening: interview/offer recording and offer decisions now serialize on deterministic PostgreSQL advisory keys inside the existing tenant transaction, preventing concurrent duplicate source events from racing the application version transition and bypassing replay handling. Lifecycle/email regression tests pass 17/17; workspace typecheck and lint pass. Live PostgreSQL concurrency evidence remains gated.
579. G26 analytics/RLS hardening: dashboard, application-history, and funnel analytics routes now execute all reads inside the existing authenticated `withTenant` boundary instead of using the global application Prisma client, ensuring real tenant-scoped metrics under production RLS. Analytics tests pass 8/8; workspace typecheck and lint pass. Live SQL/dashboard evidence remains gated.
580. G29 stale automation recovery/RLS hardening: expired automation-job lease reconciliation now runs through the existing `withService` maintenance boundary rather than the tenant application Prisma client, so worker-wide recovery is not silently suppressed by production RLS. Focused recovery tests pass 2/2; workspace typecheck, build, and lint pass. Live PostgreSQL/Redis crash-recovery evidence remains gated.
581. G29/G20 stale submission recovery/RLS hardening: worker-wide enumeration of EXECUTING submission authorizations now uses the same service maintenance boundary before tenant-scoped reconciliation, preventing stale submissions in other tenants from remaining unresolved under production RLS. Recovery/boundary tests pass 4/4; workspace typecheck and lint pass. Live PostgreSQL crash-recovery evidence remains gated.
582. G29 queue dispatch/RLS hardening: worker-wide automation-job selection now uses the service maintenance boundary, while dispatch-failure audit writes use the authenticated tenant boundary. Dispatch/recovery tests pass 3/3; workspace typecheck and lint pass. Live PostgreSQL/Redis dispatch and crash-recovery evidence remains gated.
583. G29 worker lease/RLS hardening: worker ownership checks and retry-authority reads now use the service maintenance boundary, preventing valid cross-tenant worker leases from being misclassified as missing under production RLS. Automation-worker/dispatch tests pass 10/10; workspace typecheck and lint pass. Live PostgreSQL/Redis crash-recovery evidence remains gated.
584. G29 automation lifecycle/RLS hardening: retry validation, claim, lease, renewal, completion, and failure mutations now use the dedicated service maintenance boundary for worker-wide job operations; tenant-scoped creation/replay/cancellation remain unchanged. Automation-job/worker tests pass 14/14 and typecheck passes. Live PostgreSQL/Redis lease-recovery evidence remains gated.
585. G29 worker-boundary verification: automation lifecycle changes build and lint cleanly; targeted worker/dispatch/recovery tests pass 14/14, and changed-file diff checks report only normal Windows line-ending warnings. Live PostgreSQL/Redis crash, restart, and lease-recovery evidence remains gated.
586. G27/G28 log-secret hardening: route error logging now redacts bearer credentials, password/token-style assignments, and sensitive URL query parameters even when secrets occur inside exception messages, closing the gap left by key-only metadata redaction. Structured-log tests pass 8/8; workspace typecheck and lint pass. No production log sink rehearsal has been run.
587. G29 forced-shutdown hardening: queue workers now abort active processor signals before `close(true)`, allowing cooperative handlers to stop without continuing lease renewal or browser work after forced shutdown. Queue worker tests pass 6/6; workspace typecheck and lint pass. Live Redis/process-crash recovery evidence remains gated.
588. G29 queue-envelope hardening: BullMQ enqueue now validates bounded control-free identifiers, positive delivery/attempt metadata, safe priority, and finite availability timestamps before creating attempt-specific idempotency keys. Queue semantics/worker tests pass 22/22; workspace typecheck and lint pass. Live Redis duplicate-delivery evidence remains gated.
589. G18 form-intelligence fail-closed hardening: runtime snapshots with unknown control kinds are now classified `UNSUPPORTED` with `REVIEW_ONLY` capability instead of falling through to text filling, and question identity generation emits a safe `UNKNOWN` kind token. Form-intelligence tests pass 44/44; workspace typecheck and lint pass. Provider/browser integration remains gated where infrastructure is unavailable.
590. G18 question-identity boundary hardening: provider-supplied identity aliases are now accepted only when bounded and control-free; unsafe values are replaced with deterministic semantic identities before mapping or persistence. Form-intelligence tests pass 45/45; workspace typecheck and lint pass. Provider/browser integration remains gated where infrastructure is unavailable.
591. G19 encryption-metadata hardening: the low-level verified document read path now requires an explicit encryption reference before accessing object storage, closing the gap where a caller could invoke integrity verification on metadata that did not prove encrypted storage. Document-storage tests pass 48/48; workspace typecheck and lint pass. Live S3 encryption enforcement remains gated.
592. G16/G19 exact-owner document binding: Greenhouse/Lever document transport now receives the authenticated application owner explicitly and binds storage reads to that owner, rather than trusting the document record's embedded owner field. Document-upload/application tests pass 10/10 runnable with 1 database-gated test; workspace typecheck and lint pass. Live S3/provider evidence remains gated.
593. G19 deletion-retry hardening: private-object deletion now treats provider `NoSuchKey`/`NotFound` responses as successful idempotent completion, allowing crash/partial-cleanup retries to converge without weakening owner, bucket, or metadata validation. Document-storage tests pass 49/49; workspace typecheck and lint pass. Live S3 deletion/retention rehearsal remains gated.
594. G20 approved-answer determinism: submission authorization and prepared provider forms now select the newest owner-reviewed answer by version/timestamp instead of relying on nondeterministic relation order, preventing stale or unreviewed answers from controlling submission. Submission-boundary/document tests pass 9/9; workspace typecheck and lint pass. Live PostgreSQL submission reconciliation remains gated.
595. G28 structured-log integrity: event names are now bounded control-free protocol tokens; malformed event values are replaced with `invalid.event` before serialization, preventing newline-based log forging. Structured-log tests pass 9/9; workspace typecheck and lint pass. Production log-sink rehearsal remains unverified.
596. G22 scheduler timestamp hardening: schedule calculation now rejects non-finite `Date` values before timezone/cron evaluation, preventing infinite timestamps from surfacing as misleading runtime or timezone failures. Scheduler tests pass 13/13; workspace typecheck and lint pass. Live scheduler persistence/timezone evidence remains gated.
597. G23 outbox timestamp hardening: notification outbox publishing now rejects non-finite maintenance timestamps before claiming or leasing events, preventing invalid retry/lease dates from reaching the service database. Outbox publisher tests pass 3/3; workspace typecheck and lint pass. Live PostgreSQL/Redis delivery evidence remains gated.
598. G16/G17 browser-navigation hardening: Greenhouse and Lever next-step selectors now require visible controls, preventing hidden or off-screen page-injected “Next” elements from being treated as valid multi-step navigation. Provider-neutral/adapter tests pass 56/56; workspace typecheck and lint pass. Live Chromium provider-fixture execution remains gated.
599. G18 approved-answer revision determinism: the provider-neutral `AnswerResolver` now filters to trusted owner-reviewed answers and selects the highest answer version, using approval time as a deterministic tie-breaker instead of first-match relation order. Form-intelligence tests pass 46/46; workspace typecheck, lint, and production build pass. Live PostgreSQL/provider integration remains gated.
600. Infrastructure gate recheck: the repository’s PostgreSQL, Redis, and S3 integration runners were invoked and each explicitly returned `GATED` because no reachable Docker daemon / writable endpoint is available; no integration test was counted as passed.
601. G19 duplicate-object encryption hardening: conditional immutable-upload reconciliation now accepts an existing content-addressed object only when its S3 server-side encryption matches the configured managed-AES256 or exact KMS key mode; wrong-encryption objects fail closed. Document-storage plus shared form-intelligence tests pass 96/96; workspace typecheck and lint pass. Live S3 verification remains gated.
602. G16/G17 provider regression: provider submission, Lever service/port, and Greenhouse/Lever shared adapter tests pass 33/33 runnable tests; one Chromium-gated test file is skipped and not counted. Exact document transport and human-verification fail-closed paths remain covered; live provider and PostgreSQL reconciliation remain gated.
603. G18 answer-boundary hardening: approved-answer resolution and tenant answer-draft persistence now reject oversized narrative values and option collections before provider filling or database mutation. Form-intelligence and answer-service tests pass 49/49; live PostgreSQL/provider evidence remains gated.
604. G18 identity-injection hardening: form extraction and adapter mapping no longer trust page-supplied identity/questionIdentity aliases, preventing a bounded alias collision from selecting another approved answer; durable identities are recomputed from semantic metadata. Shared form-intelligence tests pass 47/47; workspace typecheck and lint pass. Provider/database integration remains gated.
605. G19 storage regression: document storage, retention, and upload-recovery tests pass 53/53 after encryption and answer-boundary changes; all workspace production builds pass, including the web bundle (1503 modules). Live S3/PostgreSQL retention evidence remains gated.
606. G19/G20 schema integration fix: Prisma now maps `ObjectMetadata.resumeVersionId` to the existing migration column `resume_version_id`; disposable PostgreSQL migrations (41/41) apply successfully. Greenhouse/Lever persistence tests pass 4/4, document/answer integrations pass 3/3, and submission-engine integration passes 4/4.
607. G29/G30 queue integration: the standard queue runner now uses the installed ioredis client instead of requiring a host `redis-cli`, and defaults the maintenance database URL alongside the application URL. The real disposable Redis/PostgreSQL queue suite passes 14/14. The full database runner reaches 88/96 with 8 failures caused by stale pre-existing fixture data in the reused volume; those failures are not counted as passed.
608. G20 temporal-integrity fix: authorized submission attempts now persist `startedAt` from the validated operation clock, preventing fast provider failures from violating the database’s non-negative attempt interval constraint. Submission-engine PostgreSQL integration passes 4/4; workspace typecheck and lint remain clean. S3/MinIO integration remains unavailable because the pinned images cannot be pulled.
609. Static verification after schema, runner, and submission changes: all workspace typechecks and root lint pass. No completion status is advanced: full database cleanup/reproducibility and S3 integration evidence remain outstanding.
610. G19/G20 disposable PostgreSQL verification: after applying all 41 migrations, focused document/answer/provider integrations pass 7/7 and submission integration passes 4/4. The full runner reaches 88/96; its remaining 8 automation-job failures are caused by reused stale fixture rows in the shared disposable volume, so the aggregate suite is not certified clean.
611. G29 queue infrastructure verification: Docker PostgreSQL and Redis containers report healthy; the repaired standard queue runner executes the real Redis/BullMQ integration suite at 14/14. Redis is no longer environment-gated on Windows host tooling.
612. G19 S3 infrastructure gate: the declared MinIO and `mc` image tags fail Docker pull with access-denied errors, so no S3 integration result is claimed. The existing Compose definition remains intact; object-storage unit/route/retention tests continue to provide offline coverage.
613. G19 live S3 verification: disposable Compose now uses Quay-hosted MinIO images with a local KMS secret for SSE-S3; the real upload/read checksum test and concurrent content-addressed upload convergence test pass 2/2. The bucket initializer still disables anonymous access. Production S3 and full database cleanup evidence remain separate gates.
614. G16/G17 exact cover-letter binding: shared provider application execution now rejects cover-letter artifacts owned by another user or attached to a different selected ResumeVersion before reading or uploading them; the Greenhouse entry point passes the selected ResumeVersion binding into the shared helper used by Lever. Provider/document/form regression tests pass 69/69; workspace typecheck and lint pass.
615. G19/G29 infrastructure recheck: with approved Docker access, the Quay-hosted MinIO/KMS disposable service is healthy and the real S3 suite passes 2/2; the Redis/BullMQ suite passes 14/14. These are local integration results only; full database reproducibility remains uncertified because the reused volume retains stale automation fixtures.
616. G16 evidence binding: Greenhouse durable form evidence now records the exact cover-letter `resumeVersionId` alongside object identity/checksum and upload enforcement, preserving the immutable selected-version chain for later verification. Greenhouse document tests pass 11/11 executable tests (one database-gated test remains skipped) and all workspace typechecks pass.
617. G18 durable integration verification: the standard disposable PostgreSQL runner now includes `application-answers.integration.test.ts` and completes 98/98 tests across 18 integration files, covering AI-draft non-approval, explicit owner approval/versioning, tenant denial, provider persistence, and the existing negative constraint/RLS cases. G18 is VERIFIED at 100%; G19 document-backed provider controls and live provider submission remain open.
618. G20 integration evidence refresh: the disposable PostgreSQL submission path is now included in the 98/98 aggregate integration result, alongside the live 2/2 S3 checksum/dedupe suite and 14/14 Redis/BullMQ suite. G20 advances only to 80%; independent provider reconciliation and crash/recovery evidence remain outstanding.
619. G20 crash recovery: added a live PostgreSQL case that simulates a worker crash after authorization/attempt start; stale reconciliation atomically transitions the application to `UNCONFIRMED`, authorization to `OUTCOME_UNKNOWN`, closes the attempt with evidence, records `STALE_EXECUTION`, and creates no retry. The disposable database runner passes 99/99 across 18 files.
620. Static verification after G20 recovery changes: all seven workspace typechecks and root ESLint pass. No completion status is advanced beyond the verified G18 boundary; live provider reconciliation and production storage controls remain open.
621. G21 live persistence verification: added a disposable PostgreSQL fixture proving trusted provider evidence is bound to the exact completed attempt, transitions `UNCONFIRMED` to `CONFIRMED`, closes the attempt, records immutable evidence/audit state, and replays only the exact same evidence. The database runner passes 100/100 across 19 files.
622. G22 durable scheduler integration: added a live PostgreSQL concurrency fixture proving two workers race safely for one due ONCE profile; exactly one claims it, persists `lastRunAt`, deactivates the profile, and creates one discovery run plus one durable AutomationJob. The database runner passes 101/101 across 20 files.
623. G22 recurring scheduler integration: added a live PostgreSQL Europe/London hourly fixture proving a recurring due slot advances to a future durable `nextRunAt`, remains active, and creates exactly one provider discovery run. The database runner passes 102/102 across 20 files.
624. G23 durable notification integration: added a live PostgreSQL fixture that persists an application transition outbox event, consumes it through the notification consumer, verifies one tenant-owned notification, and replays the exact duplicate without a second notification. The database runner passes 103/103 across 21 files.
625. G23 live SSE bridge verification: the standard Redis runner now includes the explicit SSE integration fixture; a separate-instance-origin pub/sub message is delivered to the tenant broadcast path. Queue and SSE integration pass 15/15; durable replay and production multi-instance operations remain open.
626. G24 durable email ingestion: added a live PostgreSQL fixture proving classified email outcomes store hashes/parser evidence instead of raw body text, emit one durable notification outbox event, replay identical message delivery, and reject conflicting reuse of a message ID. The database runner passes 104/104 across 22 files.
627. G25 live interview/offer lifecycle verification: added a disposable PostgreSQL fixture proving confirmed application → interview → offer → accepted transitions, HTTPS meeting-link persistence, source-event replay, conflicting duplicate rejection, and durable audit records. The database runner passes 105/105 across 23 files.
628. G26 live analytics persistence verification: added a disposable PostgreSQL fixture proving tenant-owned lifecycle grouping plus persisted provider, match, ATS, failure, and confirmation-timing inputs; the database runner passes 106/106 across 24 files.
629. G27 live credential boundary verification: added a disposable PostgreSQL fixture proving encrypted credential material is not stored in plaintext, foreign-tenant retrieval is denied, access/storage audit records persist, and revocation deletes encrypted material; the database runner passes 107/107 across 25 files.
630. G29 Redis outage recovery verification: fixed the outage runner to propagate `DATABASE_ADMIN_URL`, then passed the real disposable Redis restart fixture with PostgreSQL authority preserved and exactly-once execution (1/1).
631. Readiness evidence correction: updated `PRODUCTION_READINESS.md` to distinguish the newly completed disposable PostgreSQL/Redis/S3 evidence from still-gated production deployment, provider, backup/restore, telemetry, and final E2E requirements.
632. G31 migration verification: Prisma schema validation passed and live migration status confirmed all 41 migrations are applied to the disposable PostgreSQL database; backup/restore and production deployment drills remain gated because host-native pg_dump/pg_restore are unavailable.
633. G31 live readiness verification: disposable HTTP server fixture passed liveness and PostgreSQL-backed readiness checks; database runner passes 108/108 across 26 files.
634. G31 non-destructive backup verification: added `test:backup`, which uses the existing PostgreSQL Compose container to create a custom dump, encrypt it with AES-256-GCM, verify its SHA-256 manifest, decrypt it, and validate `pg_restore --list`; drill passed for a 280215-byte dump without modifying the database.
635. G28 live observability verification: added an authenticated HTTP fixture proving `/api/automation/metrics` returns tenant-scoped operational metrics through the real API and PostgreSQL boundary; the database runner passes 109/109 across 27 files.
636. G30 controlled full-suite verification: the serial workspace Vitest run completed with 76 files and 629 tests passed, plus 35 files and 125 tests explicitly skipped by declared infrastructure gates; no unexpected failures occurred.
648. Database certification refresh: approved Docker execution ran the aggregate disposable PostgreSQL suite with 28 files and 113 tests passed. Expected constraint/RLS errors are emitted by negative assertions; the process exited successfully.
649. G32 human-handoff resume verification: the live certification fixture now resolves the persisted CAPTCHA HumanVerification, creates the durable resume command, resumes only to the recorded `FORM_FILLED` checkpoint, and verifies idempotent acknowledgement replay; focused fixture passes 3/3.
650. Static and operational verification refresh: all workspace typechecks, root ESLint, production API/web/queue builds, non-destructive encrypted backup drill, and diff checks pass after the certification changes; backup drill produced a 287783-byte restore-readable encrypted dump.
651. G20/G32 provider-service verification: the live disposable fixture now executes `ProviderSubmissionService` from a persisted `EXECUTING` authorization through a real Chromium Greenhouse form, exact approved document upload, submit control, and normalized confirmation evidence; certification fixture passes 4/4.
652. Submission timing integrity: provider execution now supplies its trusted `attemptedAt` as the `UNCONFIRMED` transition effective time, binding `Application.appliedAt` to the observed submit action; this prevents valid independent evidence from being rejected by local transaction-clock skew. State-machine and live certification tests pass 8/8.
653. Database regression refresh: the approved Docker aggregate PostgreSQL runner passes 28 files/114 tests after the timing fix, including the live provider-service and independent-verification path; expected negative constraint/RLS errors remain asserted safeguards.
654. G21 worker-boundary verification: the live Chromium/PostgreSQL provider-flow fixture now consumes queued confirmation evidence through the production `VERIFY_SUBMISSION_CONFIRMATION` automation handler, including payload validation and trusted verifier dispatch, and reaches `CONFIRMED`; fixture remains 4/4.
655. Completion-plan audit: G21 ledger evidence was refreshed to the current 28-file/114-test PostgreSQL run and verified automation-handler path; G22 remains explicitly open for persisted application-run scheduling, daily-limit integration, pause/resume/cancellation operations, and restart evidence.
656. G28 operational alerting: added deterministic bounded alert derivation to the authenticated automation metrics endpoint and real dashboard rendering for queue backlog/failures, retry pressure, aging human verification, and excessive active browser sessions; focused alert tests pass 2/2, with no new telemetry authority introduced.
657. G28 endpoint verification: the live authenticated automation-metrics fixture now asserts the new empty-alert contract against disposable PostgreSQL; focused alert plus endpoint tests pass 3/3.
637. G16 browser document integration: extended the real Chromium Greenhouse fixture suite with exact approved ResumeVersion upload and verified the browser file control receives the expected filename and bytes; focused fixture suite passes 14/14.
638. G17 browser document integration: extended the real Chromium Lever fixture suite with exact approved-document upload and verified the browser file control receives the expected filename and bytes; focused fixture suite passes 7/7.
639. G19 live S3 authorization verification: extended the MinIO suite to verify owner-bound signed URL issuance and rejection of foreign-tenant signing and checksum-tampered reads; S3 integration passes 3/3.
640. G19 live application binding verification: extended the PostgreSQL fixture to prove only a clean document with the exact application ResumeVersion can be attached; mismatched versions and foreign tenants are rejected. The database runner passes 110/110 across 27 files.
641. G16 local provider-flow verification: extended the real Chromium Greenhouse fixture to fill the form, upload the exact approved document, activate the submit control, and parse a deterministic confirmation page; focused fixture suite passes 15/15. This is local fixture evidence, not an external submission.
642. G17 local provider-flow verification: extended the real Chromium Lever fixture to fill the form, upload the exact approved document, activate the submit control, and parse a deterministic confirmation page; focused fixture suite passes 8/8. This is local fixture evidence, not an external submission.
643. G32 certification slice: added a live disposable PostgreSQL + Chromium fixture connecting provider-neutral Greenhouse form interaction and exact file upload to trusted confirmation parsing, independent verification, durable evidence, and `CONFIRMED` application state; database runner passes 111/111 across 28 files. This is a certification slice, not final full-system certification.
644. G32 dashboard readback: extended the certification slice to call authenticated `/api/applications` and `/api/analytics/dashboard`, verifying the confirmed application and 100% submission-success metric are exposed from persisted state; the 111/111 runner remains green.
645. G32 notification readback: extended the certification slice to consume the persisted confirmed-transition outbox event through the real notification consumer and verify the tenant-owned `APPLICATION_SUBMITTED` notification; focused certification fixture passes 1/1.
646. G32 stitched-path verification: the certification fixture now completes browser interaction → trusted verifier → database state → notification outbox → authenticated application/analytics readback in one live disposable run; no external provider submission is claimed.
647. G32 human-handoff verification: extended the live Chromium/PostgreSQL certification fixture with a real CAPTCHA field handled by `GreenhouseApplicationService`; the run creates a pending durable HumanVerification and leaves the application in `WAITING_FOR_USER`, proving fail-closed handoff without bypassing CAPTCHA. The fixture passes 2/2.
658. G29 browser-session recovery: added service-boundary reconciliation for durable ACTIVE browser-session references left by process/browser crashes, recording CLOSED/EXPIRED state plus audit/outbox evidence; worker startup invokes recovery; browser-session integration and unit tests pass 7/7.
659. G17 provider selection: added focused shared-submission coverage proving a Lever application selects the independent Lever form port and completes the exact-document submission path without invoking Greenhouse; provider-submission tests pass 16/16.
660. G19 application-artifact binding: application-linked cover letters, evidence, screenshots, and receipts now persist the owning application’s exact `ResumeVersion` binding, preventing detached artifact references from reaching workers; route/storage focused suites pass 65/65 and workspace typecheck passes.
661. G16/G17 provider regression verification: current Greenhouse and Lever Chromium/document-port fixtures pass 37/37 after the storage-boundary change; this remains deterministic local-fixture evidence and does not claim external provider submission.
662. G18 regression verification: the provider-neutral form-intelligence suite passes 47/47 on the current checkout, including stable identities, hostile-content rejection, policy classification, dynamic fields, validation, and approved-answer resolution.
663. G19 authorization-matrix coverage: added a regression proving an evidence artifact explicitly bound to another ResumeVersion is rejected before object storage access; document route tests now pass 14/14.
664. G20 durable-reference verification: submission preflight now uses the canonical immutable document-reference builder, retaining `resumeVersionId` in authorization and attempt evidence; the full disposable PostgreSQL runner passes 28 files/115 tests, including the exact-version assertion.
665. G16 durable form evidence: Greenhouse form-completion evidence now retains the full exact document identity, including bucket, object key/version, encryption reference, checksum, and ResumeVersion binding; workspace typecheck passes.
666. G16/G20 regression verification: after canonical immutable document evidence changes, the disposable PostgreSQL integration runner completes 28 files/115 tests with no unexpected failures; expected negative constraint/RLS diagnostics remain asserted safeguards.
667. G19 ownership binding: metadata persistence now verifies that a supplied `resumeId` and `resumeVersionId` refer to the same resume, preventing cross-resume document associations; document-storage tests pass 51/51 and workspace typecheck passes.
668. G19 encryption-provenance boundary: metadata persistence now fails closed when a caller supplies a stored object reference without encryption provenance, before duplicate lookup or PostgreSQL mutation; document-storage tests pass 52/52.
669. G19 object-encryption integrity: verified reads now perform a `HeadObject` check and reject objects whose server-side encryption mode or KMS key differs from the immutable metadata before downloading bytes; document-storage tests pass 53/53.
670. G16/G19 provider document boundary: Greenhouse resume upload now rejects an exact-version document whose embedded owner differs from the authenticated owner before reading or uploading bytes; Greenhouse document/form tests pass 59/59 runnable with one explicitly gated database case, and API typecheck passes.
671. G19/G20 preflight encryption boundary: provider submission now rejects resume or cover-letter metadata without encryption provenance before browser-session creation, preventing malformed references from becoming ambiguous provider outcomes; provider submission and boundary tests pass 19/19 and API typecheck passes.
672. G19 owner-bound reference enforcement: authorized reads, deletes, and signed URLs now require the embedded metadata owner to match the authenticated user in addition to validating the private tenant-scoped object key; storage, recovery, provider, and Greenhouse boundary tests pass 80/80.
673. G19 explicit document approval: ObjectMetadata now records approval status, approver, and timestamp; an authenticated owner approval operation emits durable audit evidence, and provider/submission boundaries require approved immutable resume and cover-letter artifacts before browser access. Storage/provider tests pass 79/79 runnable; Prisma client generation and API typecheck pass.
674. G19 cross-package verification: generated Prisma Client after the additive document-approval migration; the full workspace typecheck passes across API, web, AI, job-engine, queue, resume-engine, security, and types packages.
675. G19 application-binding approval: the transaction-scoped document attachment helper now rejects clean but unapproved or incorrectly attributed objects before creating an application binding; storage, document-route, and provider tests pass 86/86 and API typecheck passes.
676. G19 upload-binding approval: application-linked multipart uploads now record authenticated owner approval before creating the `ApplicationDocument` binding; document route and storage tests pass 70/70, and API typecheck/diff checks pass.
677. G19 approval API verification: the authenticated document approval endpoint now has focused HTTP coverage for owner-scoped approval response and invocation; document route tests pass 15/15 and API typecheck passes.
678. G16 pre-browser approval gate: Greenhouse preparation now rejects unapproved or incorrectly attributed exact resume/cover-letter documents before creating a browser session; Greenhouse focused tests pass 12/12 runnable with one database-gated case, and API typecheck passes.
679. G19 application-route approval bypass closure: the multipart application-artifact route now records authenticated owner approval through the shared transaction helper before creating an application document binding; document route tests pass 15/15 and API typecheck/diff checks pass.
680. G19 shared attachment-boundary closure: the multipart application-artifact route now delegates application binding to `attachDocumentToApplicationInTransaction`, ensuring one tenant/version/approval/idempotency policy governs both API uploads and worker-facing attachment paths; document-storage and document-route tests pass 71/71 and API typecheck passes.
681. G18 acceptance revalidation: provider-neutral form intelligence and the Greenhouse/Lever ports were rechecked against stable identity, semantic/accessibility extraction, dynamic/multistep state, deterministic policy classification, approval/provenance gating, and hostile webpage fail-closed behavior; focused shared/provider tests pass 58/58 and the full workspace build passes. G18 remains VERIFIED; no duplicate provider intelligence was introduced.
682. G19 signed-access integrity closure: both authenticated signing entry points now pass through live `HeadObject`/read verification before issuing a temporary URL, so clean metadata cannot authorize a tampered or encryption-mismatched object; storage and route tests pass 72/72 and API typecheck/diff checks pass.
683. G16 durable document-evidence closure: Greenhouse form-step evidence now records the exact resume/cover-letter approval status, approver, and approval timestamp alongside checksum, object identity, encryption, and ResumeVersion binding; Greenhouse document-flow tests pass 12/12 runnable (1 environment-gated) and API typecheck passes.
684. G19 worker-read approval boundary: authenticated worker document reads now independently require explicit owner approval when approval metadata is present, before any object-store access; storage, Greenhouse, and provider-submission tests pass 78/78 runnable (1 environment-gated) and API typecheck passes.
685. G18 resolver identity compatibility: the shared approved-answer resolver now canonicalizes `identity`, `questionIdentity`, or the assessment identity before matching approved answers, preventing provider-port alias records from being silently skipped; shared/provider tests pass 59/59 and job-engine typecheck/diff checks pass.
686. G20/G19 document authorization evidence closure: canonical submission preflight references and subsequent attempt/failure/unknown-outcome evidence now retain document approval status, approver, and approval timestamp alongside the immutable object/checksum/version fields; submission boundary/provider tests pass 19/19 and API typecheck passes.
687. G19 disposable-storage policy hardening: the existing Compose MinIO initializer now explicitly disables anonymous access, enables bucket versioning, and applies a default SSE-S3 encryption policy; YAML/config inspection, root lint, and `git diff --check` pass. Runtime verification remains Docker-gated.
688. G19 retention concurrency hardening: retention workers now serialize per immutable object with a PostgreSQL advisory transaction lock and recheck the live tombstone/legal-hold/expiry state before deleting, preventing duplicate deletion/audit work while preserving fail-closed retry behavior; retention tests pass 4/4 and API typecheck passes.
689. G16/G17 provider fixture regression: Greenhouse and Lever local fixture suites, together with both provider adapter contracts, pass 34/34 on the current checkout; provider-specific DOM transport remains separated from shared form policy, while live document/runtime integration remains unverified.
690. G17 Lever ARIA control extraction: Lever form snapshots now retain generic named `[role="combobox"]` controls by reading the `name` attribute rather than relying on HTML-input properties; a real Chromium fixture covers the named combobox path, provider fixtures/adapters pass 13/13, API typecheck and root lint pass.
691. G16 Greenhouse ARIA control parity: Greenhouse snapshots now retain generic named `[role="combobox"]` controls through the same provider-port attribute extraction rule, with a real Chromium fixture covering the path; Greenhouse/Lever fixture suites pass 24/24, API typecheck and root lint pass.
692. G22 durable application-run scheduling: added an authenticated application schedule endpoint and tenant-scoped scheduler service that validates bounded future slots, checks provider/state eligibility, reuses existing Greenhouse/Lever handlers through `AutomationJob.availableAt`, preserves idempotency, and records `APPLICATION_RUN_SCHEDULED` audit evidence; scheduler tests pass 14/14 and API typecheck passes. Live database scheduling remains environment-gated.
693. G17 Lever recovery/dynamic-form coverage: the real local Chromium Lever fixture now verifies idempotent retry-safe reinspection and discovery/filling of a field rendered after the initial snapshot; Lever fixture/adapter tests pass 14 runnable tests with one explicit database-gated case.
694. G16/G17/G18/G19 focused regression: shared form intelligence, Greenhouse and Lever adapter contracts/Chromium fixtures, document storage, and retention suites pass 147/147 on the current checkout; this does not substitute for the still-gated live PostgreSQL/S3 execution evidence.
695. G19/G20 approval-integrity closure: provider submission replay validation now compares immutable document approval status, approver, and approval timestamp for both resume and cover-letter references; a post-authorization approval mutation is rejected before browser-session creation, and provider/boundary tests pass 20/20.
696. G20 regression verification: after approval-integrity enforcement, provider submission and submission boundary tests pass 20/20, API typecheck passes, root lint passes, and `git diff --check` reports no whitespace errors.
697. G22 schedule-input hardening: durable application scheduling now rejects control characters and malformed whitespace in application, correlation, and idempotency identifiers before tenant/database access; scheduler tests pass 16/16 and API typecheck passes.
698. G22 pause/resume integrity: scheduled application runs may now bind to an owner-validated RUNNING/PAUSED `AutomationRun`, ensuring the existing dispatcher pause gate applies durably instead of allowing unbound scheduled work to run during a pause; scheduler tests pass 17/17, API typecheck/lint pass. Docker live integration remains unavailable because the Docker Desktop Linux engine pipe is absent.
699. G22 dashboard integration: ApplicationsView and the authenticated web API now expose the persisted application form-run schedule action for eligible provider states, with deterministic correlation, generated idempotency, real backend response handling, and refresh from durable state; web API tests pass 18/18 and web typecheck/production build pass.
700. G22 durable schedule readback: authenticated application details now return bounded provider-run job metadata (status, due time, attempts, completion/cancellation, and error) and the dashboard renders those persisted records; web API tests pass 18/18, API/web typechecks pass, and the web production build passes.
701. G22/G23 scheduled-run notification: durable application scheduling now emits an idempotent `application.run.scheduled` outbox event after job creation, and the existing tenant notification consumer maps validated payloads to `APPLICATION_SCHEDULED`; scheduler/notification tests pass 42/42 and API/types typechecks pass.
702. G22 dashboard cancellation: the Applications view and authenticated web API now expose owner-scoped cancellation for pending/available durable provider runs, with persisted-state refresh after cancellation; web API tests pass 19/19 and web typecheck/production build pass.
703. G22 API idempotency boundary: the application schedule route now maps scheduler and `AutomationJobError` domain failures to stable HTTP 400/404/409/422 responses instead of collapsing conflicts into 500, preserving observable retry semantics; API typecheck, root lint, and diff checks pass.
704. G20/G22 provider checkpoint hardening: scheduled provider form runs are now restricted to the `APPLICATION_STARTED` checkpoint; `FORM_FILLED` and `WAITING_FOR_USER` are rejected rather than creating misleading no-op or unsafe jobs, matching the shared provider execution contract; scheduler tests pass 19/19 and API/web typechecks plus web build pass.
705. G22/G23 validation closure: the durable scheduled-run outbox notification path and dashboard cancellation/readback changes remain green under the root validation gates; root lint passes, all workspace builds pass, and `git diff --check` reports no whitespace errors. PostgreSQL/Redis/S3 runtime integration remains environment-gated because the Docker Desktop Linux engine is unavailable.
706. G24 tenant-read isolation: authenticated email-connection and email-outcome listing routes now execute through the existing `withTenant` transaction boundary instead of the global Prisma client, preserving RLS-compatible tenant isolation on reads. Email classifier/sync/notification tests pass 53/53; API typecheck, root lint, and `git diff --check` pass. Live mailbox/provider execution remains gated.
707. G24 email-boundary hardening: message, tenant, application, outcome, consent, idempotency, and correlation identities now reject control characters before normalization, preventing header/log/protocol injection through email metadata. Email outcome/consent/lifecycle/sync tests pass 35/35; API typecheck, root lint, and `git diff --check` pass.
708. G24 review-state readback: the tenant-scoped email-outcome listing now returns durable `reviewedAt`/`reviewedBy` workflow state while continuing to exclude raw message hashes and parser evidence from the general list response. Route/classifier/lifecycle tests pass 29/29; API typecheck, root lint, and `git diff --check` pass.
709. G24 consent metadata hardening: email scopes and opaque credential references now reject control characters before trimming, closing a normalization bypass for protocol/header injection; email consent/classifier tests pass 17/17, API typecheck, and `git diff --check` pass.
710. G24 frontend consent visibility: SettingsView now loads tenant-owned email connections from the authenticated API and exposes persisted active/revoked state, scopes, sync timestamp, and owner-scoped revocation; it deliberately does not handle OAuth tokens or claim provider authorization. Web API tests pass 20/20, web typecheck/production build, root lint, and `git diff --check` pass.
711. G25 lifecycle input hardening: interview/offer identities, source-event keys, and lifecycle free-text fields now reject control characters before trimming, closing normalization-based protocol/log injection paths; lifecycle/email tests pass 16/16, API typecheck, and `git diff --check` pass.
712. G25 persisted-value hardening: lifecycle meeting URLs now reject embedded credentials, and offer amounts are bounded to a finite maximum before tenant persistence; application-lifecycle tests pass 14/14, API typecheck, root lint, and `git diff --check` pass.
713. Environment/test evidence refresh: Docker remains unavailable because the Docker API named pipe is absent; a fresh full `npm test` invocation stalled after Vitest startup with no test completion output and was terminated, so no full-suite result is claimed. Focused suites and static checks remain the authoritative passing evidence.
714. G24 mailbox-provider boundary: `syncEmailConnection` now rejects unregistered connector and consented provider identifiers before invoking mailbox transport, preserving the explicit GMAIL/MICROSOFT_GRAPH/IMAP allowlist; email sync/outcome tests pass 26/26, API typecheck, root lint, and `git diff --check` pass.
715. G27 durable credential boundary: owner, credential, and credential-name identifiers now reject control characters before validation/normalization, preventing the same protocol/log injection bypass across encrypted credential storage and revocation; credential and mailbox tests pass 19/19, API typecheck, root lint, and `git diff --check` pass.
716. G27 RLS refresh integrity: durable credential retrieval now refreshes access timestamps through an owner-, credential-, and non-revoked-scoped mutation, failing closed if the record changes between read and refresh; credential boundary tests pass 7/7, API typecheck, root lint, and `git diff --check` pass.
717. G24 sync runtime boundary: mailbox synchronization now rejects malformed user/connection identities before calling `.trim()` or accessing tenant state, preventing runtime `TypeError` leakage from malformed durable-job payloads; email sync tests pass 16/16, API typecheck, root lint, and `git diff --check` pass.
718. G28/G24 sync traceability: mailbox synchronization accepts a bounded correlation ID and persists it only in completion audit metadata, allowing durable sync operations to be traced without adding raw mailbox content to storage; email sync tests pass 16/16, API typecheck, root lint, and `git diff --check` pass.
719. Readiness evidence correction: `PRODUCTION_READINESS.md` now distinguishes historical disposable-service passes from current-session evidence, explicitly records the unavailable Docker engine and stalled full-suite run, and keeps PostgreSQL/Redis/S3/full-E2E claims gated until reproduced.
720. G24 OAuth-state foundation: added a durable, tenant-scoped, expiring single-use OAuth state record with SHA-256 state lookup, encrypted PKCE verifier storage, redirect/provider/scope validation, conditional consume semantics, and RLS/grants. OAuth-state tests pass 3/3; Prisma validate/generate, API typecheck, root lint, production build, and `git diff --check` pass. Provider-specific authorization/token exchange remains incomplete and is not claimed.
721. G24 OAuth-state hardening: malformed inputs and credential-bearing or fragment-containing redirect URIs now fail closed before persistence; OAuth-state tests pass 4/4.
722. G21 verification-boundary hardening: submission verification now rejects control characters in request identities and persisted evidence identifiers before trusted-boundary processing; submission-verification plus OAuth-state tests pass 30/30.
723. G19 artifact-policy hardening: runtime metadata and upload paths now reject document kinds outside the supported artifact set before persistence or object-store side effects; storage/recovery tests pass 61/61. The real S3-compatible suite ran but was explicitly skipped 3/3 because the disposable service is unavailable; no integration pass is claimed.
724. G19 malformed-input hardening: transaction-level metadata persistence and direct upload validation now fail closed for malformed owner/upload objects before database or content inspection; storage/recovery tests pass 62/62.
725. G20 submission-boundary hardening: authorization/execution identity fields now reject oversized or control-bearing values before any durable claim or provider side effect; submission-boundary plus verification tests pass 30/30.
726. Infrastructure revalidation: the repository’s existing Docker Desktop/Compose path was safely attempted, but the Linux engine API named pipe remained unavailable even though the desktop command reported it running; PostgreSQL/Redis/MinIO integration remains gated and this evidence is recorded in `PRODUCTION_READINESS.md`.
727. G16/G17/G20 provider-boundary hardening: shared provider submission execution now rejects oversized or control-bearing worker, correlation, authorization, and application identities before tenant queries or browser activity; provider-submission tests pass 18/18.
728. G19 storage-client hardening: direct S3-compatible client configuration now rejects credential-bearing/query-bearing endpoints, malformed protocols, unsafe bucket names, and invalid encryption references before object-store access; document-storage tests pass 61/61.
729. G24 consent-boundary hardening: malformed email-consent objects and non-string owner identities now fail closed with domain errors before normalization or tenant access; email connection/OAuth/sync tests pass 25/25.
730. G24 mailbox-input hardening: message IDs, sender/subject headers, and optional application IDs now reject control characters while multiline message bodies remain supported; email sync/concurrency tests pass 19/19.
731. G29 worker-boundary hardening: automation handlers now bound all untrusted durable payload text before database, AI, provider, or mailbox work; handler tests pass 11/11.
732. G24 classifier boundary hardening: malformed classifier input now fails closed to low-confidence `UNCLASSIFIED` instead of leaking a runtime exception; email outcome/lifecycle tests pass 16/16.
733. G27/G29 payload-identity hardening: automation handlers now reject control characters in all durable IDs/headers while explicitly allowing multiline mailbox bodies; handler tests pass 12/12.
734. G19 attachment-boundary hardening: the transaction-level application-document helper now validates owner, application, resume-version, object identities, and attachment type before tenant queries; storage/recovery tests pass 64/64.
735. G24 OAuth authorization progress: added a provider-specific Gmail/Microsoft authorization URL builder using the durable OAuth-state record and S256 PKCE challenge, rejecting IMAP and missing/unsafe client configuration; OAuth authorization/state tests pass 6/6, with token exchange and callback persistence still incomplete and unclaimed. `.env.example` documents the client-ID configuration.
736. G24 OAuth configuration lint closure: replaced the literal control-character regex with code-point validation to satisfy the repository’s no-control-regex rule; OAuth authorization/state tests pass 6/6, API typecheck, and root lint pass.
737. G24 OAuth exchange boundary: added injected Gmail/Microsoft authorization-code exchange with durable single-use state consumption, PKCE verifier use, bounded token-response validation, encrypted credential-vault persistence, and owner-consent activation; OAuth authorization/state tests pass 8/8, API typecheck, and root lint pass. IMAP, missing configuration, failed responses, and token exchange without a valid state fail closed; provider refresh/rotation and live callback integration remain incomplete.
738. G24 OAuth API wiring: authenticated email-connection routes now expose OAuth start and callback operations, returning only authorization metadata/connection status; callback state is always consumed under the authenticated owner, and raw tokens are never returned. OAuth service tests pass 4/4; API typecheck, lint, and build pass.
739. G24 OAuth account binding: the requested mailbox account label is now persisted inside the durable, owner-scoped OAuth state and reused during token exchange; the callback no longer accepts account identity from query data. OAuth authorization/state tests pass 8/8; Prisma validate/generate, API typecheck, root lint, production build, and diff checks pass. Live provider callback execution remains environment-gated.
740. G24 OAuth provider-response hardening: untrusted Gmail/Microsoft token responses now require bounded, control-free access/refresh/token-type fields before credential persistence or consent activation, and expiry uses the validated callback timestamp; OAuth tests pass 10/10 and API typecheck/root lint pass. Live provider exchange remains environment-gated.
741. G19/G20/G21 focused regression: private document storage/retention, provider submission, submission authorization boundaries, and independent verification pass 116/116 focused tests after the OAuth and storage-boundary changes; no live PostgreSQL/Redis/MinIO result is claimed because the Docker Linux engine remains unavailable.
742. G19 tenant-read boundary: the authenticated resume document download route now performs its metadata lookup through `withTenant`, aligning signed access with the existing RLS-compatible owner boundary instead of using the global Prisma client; document/storage/provider focused tests pass 95/95, API typecheck and root lint pass.
743. G27/G19 resume-route isolation: all authenticated resume list, fact lookup, tailoring/ATS validation lookup, detail, version, and document-download reads now use the established tenant transaction boundary; API typecheck/root lint and focused resume/document tests pass 77/77.
744. G23/G27 notification and human-review read isolation: authenticated notification list/read mutations and human-verification list/detail reads now use `withTenant`, preserving RLS-compatible owner scoping for durable user-facing state; API typecheck and root lint pass.
745. G24 OAuth legacy-state fail-closed behavior: OAuth state consumption now rejects legacy or malformed rows without an explicit account label before conditional consumption, credential storage, or consent activation; OAuth state tests pass 5/5 and API typecheck/root lint pass.
746. G24 OAuth regression correction: the durable live-state fixture now includes the required bound account label; OAuth authorization/state tests pass 10/10 after the legacy-state fail-closed guard, with API typecheck, root lint, and diff checks clean.
747. G20/G21/G27 application-route isolation: authenticated application list/detail, quality evaluation, and Greenhouse/Lever completion lookups now share the `withTenant` transaction boundary, including paired application/profile reads; lifecycle/submission/analytics focused tests pass 26/26 and API typecheck/root lint pass.
748. G22/G27 scheduler-profile isolation: authenticated search-profile CRUD now uses `withTenant`, and update readback occurs in the same tenant transaction as the mutation; API typecheck and root lint pass.
749. G22/G27 automation-run isolation: authenticated automation status/start/pause/resume/stop/emergency-stop and event readback now use tenant transactions; concurrent starts serialize on a PostgreSQL advisory lock, and automation-focused tests pass 8/8 with API typecheck/root lint clean.
750. G6/G27 profile isolation: authenticated profile reads and create/update persistence now use `withTenant`; create-versus-existing and update readback decisions occur inside one tenant transaction, preserving candidate-profile isolation; API typecheck/root lint pass. No dedicated profile-route test file exists in the current checkout, so route-test evidence is not claimed.
751. G27 rules isolation: authenticated user-rule CRUD now uses `withTenant`, with update readback occurring in the same tenant transaction as the owner-scoped mutation; API typecheck and root lint pass.
752. Workspace regression after tenant-boundary changes: API and web production builds complete successfully, and `git diff --check` reports no whitespace errors; live service/integration evidence remains separately gated.
753. G23/G27 SSE replay isolation: durable notification cursor and replay queries now execute through `withTenant`, preserving owner-scoped replay semantics for Last-Event-ID while retaining the existing bounded serialization and connection controls; SSE tests plus API typecheck/root lint pass.
754. G19 object-owner invariant: stored document metadata validation now always compares an embedded owner to the tenant segment in its immutable private object key, even for direct verified-read callers; document-storage tests, API typecheck, and root lint pass.
755. G22/G27 search-profile input hardening: durable profile arrays, provider accounts, names, and time zones now reject control-bearing or oversized text before scheduler/discovery persistence; search-profile validation tests pass 10/10, with API typecheck and root lint clean.
756. G6/G27 candidate-profile input hardening: authenticated profile validation now rejects control-bearing/oversized scalar fields and oversized target-role arrays before durable candidate-fact/profile persistence; new profile validation tests pass 5/5, with API typecheck/root lint clean.
757. G19 scanner input boundary: the ClamAV integration now rejects empty, non-buffer, and oversized scan payloads before process creation; document-storage tests pass 64/64, with API typecheck/root lint clean.
758. G19 bucket-configuration hardening: storage configuration and stored metadata now require valid S3-compatible bucket names, rejecting uppercase, short, adjacency-invalid, IP-literal, and malformed names before object-store access; document-storage tests pass 74/74, with API typecheck/root lint clean.
759. G26 plan correction: the current `AnalyticsView` already renders the persisted historical application funnel returned by `/analytics/applications-funnel`; the remaining G26 work is route-level authenticated live evidence and scale testing, not frontend funnel implementation. No completion percentage is inflated by this documentation correction.
760. G19 object-store metadata verification: verified reads now reject conflicting server-reported size, MIME, or SHA-256 metadata before downloading or signing; document storage, recovery, and retention tests pass 83/83, and workspace typecheck, lint, and production build pass. Live S3/KMS/scanner and database cleanup evidence remain environment-gated.
761. G17 Lever regression: provider-neutral Lever adapter/port, dynamic reinspection, controls, exact file transport, hostile-content fail-closed behavior, confirmation parsing, and retry-safe local fixtures pass 21 tests; 1 database-backed service test remains explicitly skipped because the current Docker PostgreSQL engine is unavailable.
762. G19/G27 deletion race closure: owner document deletion now uses a conditional tombstone and emits its audit event only for the single transaction that wins the delete race; concurrent retries remain idempotent. Workspace typecheck/lint/build remain green after the change; live database/object-store race evidence remains gated.
763. G18 acceptance audit: the provider-neutral detector, extractor, mapper, resolver, stable semantic identity, deterministic policy classes, approved-answer provenance/approval checks, tenant persistence, audit evidence, dynamic/multi-step handling, and hostile-input fail-closed tests are present; shared form-intelligence tests pass 48/48. No G18 implementation gap was found. G19 disposable PostgreSQL/Redis/S3 evidence remains gated because the Docker Linux engine is unavailable in the current environment.
764. G16 provider integration regression: Greenhouse adapter/form port, exact approved document upload, local fixture behavior, durable application preparation, provider submission, and submission-boundary tests pass 63 executable tests; 1 database-backed test remains explicitly skipped due to unavailable Docker PostgreSQL.
765. G19 runtime gate revalidation: the repository S3 runner and PostgreSQL runner both executed and returned their explicit `GATED` result because no reachable Docker daemon is available; no infrastructure tests were counted as passed or fabricated.
766. G19 deletion idempotency regression: added direct coverage for a losing concurrent tombstone transaction; document deletion still returns 204 without duplicating the audit event. Document-route and submission-boundary tests pass 20/20 with ESLint clean.
767. G20 provider-result trust boundary: post-execution provider results are now validated for supported provider, bounded/fresh attempt time, and application/provider-consistent confirmation evidence; malformed results fail as `UNKNOWN_OUTCOME` after execution is marked active. Submission-boundary tests pass 5/5, API typecheck and ESLint pass.
768. G18/G20 approved-answer boundary: reviewed approved answers now fail closed when persisted values are malformed, oversized, or contain invalid option shapes; submission-boundary tests pass 6/6, API typecheck and ESLint pass.
769. G22 scheduler budget correctness: daily application capacity now uses each search profile's validated local calendar day rather than UTC, with tenant-level serialization before reservation counting and a per-application/day advisory lock; timezone boundary tests pass 2/2, API typecheck and ESLint pass.
770. G23 notification/live-event regression: durable notification outbox runtime, Redis envelope validation, SSE replay, and tenant-scoped live delivery tests pass 12/12 focused tests; live Redis multi-instance evidence remains environment-gated.
771. G24 email chronology boundary: direct email-outcome ingestion now rejects received timestamps more than five minutes in the future, matching mailbox-sync clock-skew policy; email classifier/validation tests pass 13/13, API typecheck and ESLint pass.
772. G26 analytics correctness: average time-to-submission now uses durable completed `ApplicationAttempt` execution intervals rather than application creation timestamps; analytics tests pass 8/8, API typecheck and ESLint pass.
773. G28 structured-log secret boundary: centralized structured logging now applies credential-pattern redaction to every emitted string, including generic worker/runtime `error` metadata, while retaining existing key-based PII/content redaction and bounds; observability tests pass 10/10. Full production telemetry and infrastructure-backed evidence remain gated.
774. G29 resilience regression: expired automation lease recovery, dispatcher reconciliation, worker failure/retry handling, lease-loss aborts, and queue backoff/generation safeguards pass 38/38 focused tests; workspace typecheck, lint, and production build pass. PostgreSQL/Redis outage and crash-restart integration remain explicitly environment-gated because Docker is unavailable.
775. G28 automation failure traceability: automation status/start/pause/resume/stop/emergency-stop, job cancel/replay, metrics, and event route failures now emit correlation/user/job-scoped structured error records through the centralized redaction boundary; automation route tests pass 5/5 and workspace typecheck/lint pass. Full telemetry backend and alert delivery remain open.
776. G18 multi-step identity correction: provider-neutral extraction and orchestration now derive an implicit `step-{n}` identity when a provider omits `stepIdentity`, preventing questions from different multi-step pages from colliding while preserving explicit semantic step identities; form-intelligence tests pass 49/49, job-engine typecheck, and root lint pass.
777. G19 storage regression after form-identity changes: document storage, recovery, retention, owner authorization, signed-access, deletion, and artifact-route tests pass 99/99 focused tests. No new storage implementation gap was introduced; live S3/KMS/scanner evidence remains environment-gated.
778. G19/G28 document-route traceability: download-URL, owner deletion, and explicit document approval failures now emit correlated structured error records through the shared redaction boundary; document-route tests pass 16/16, API typecheck, and root lint pass.
779. G19 infrastructure recheck: direct Docker daemon inspection still fails because the local Docker engine named pipe is unavailable (and the Docker config is access-denied); no PostgreSQL/Redis/S3 integration test is counted as passed or fabricated.
780. G16/G17 provider regression after shared identity correction: Greenhouse/Lever application orchestration, exact document upload helpers, local fixtures, and provider submission boundary tests pass 55/55 runnable tests; 2 tests remain explicitly skipped. Live provider submission and infrastructure-backed evidence remain unverified.
781. G20/G21 exact-document regression: submission authorization, provider execution, exact immutable document checks, malformed provider-result handling, and independent-verification parser tests pass 50/50 focused tests after the multi-step identity correction; live external submission remains unclaimed.
782. G20/G27 durable failure-privacy boundary: provider exception messages are now bounded, control-character sanitized, and credential-pattern redacted before entering `ApplicationAttempt` or `FailureRecord` evidence; submission-boundary tests pass 7/7, API typecheck and root lint pass.
783. G20/G21 regression after failure-evidence hardening: provider submission, exact-document authorization, independent-verification, and submission boundary tests pass 50/50; no external submission or unavailable infrastructure result is claimed.
784. G20/G28 durable error-privacy consolidation: the shared `safeErrorMessage` sanitizer now bounds, control-sanitizes, and credential-redacts durable automation-job, outbox, worker, and submission failure evidence; focused observability/submission/worker/dispatcher/outbox tests pass 31/31, API typecheck and root lint pass.
785. G29 durable job-error boundary: `failAutomationJob` now sanitizes failure text at the authoritative persistence boundary, covering direct callers as well as worker paths; automation-job/worker/observability tests pass 25/25, API typecheck and root lint pass.
786. G7/G27 durable discovery-error privacy: provider and normalization exception text is now sanitized, control-character safe, credential-redacted, and bounded before persistence in discovery runs/items; discovery tests pass 39/39, API typecheck and root lint pass.
787. G19 worker-read approval boundary: `DocumentStorage.readAuthorized` now requires explicit `APPROVED` status, owner-matching approval actor, and approval timestamp; omitted or legacy approval metadata fails closed before object access. Storage/recovery/route tests pass 95/95, API typecheck, and root lint pass.
788. G17/G18 provider-neutral seam: added the neutral `provider-application.ts` orchestration surface and switched the Lever entry point to consume it, preserving one shared policy/document/verification workflow and provider-owned DOM ports; Lever/provider/form tests pass 67/67 runnable, API typecheck, and root lint pass.
789. G19 worker document-type boundary: `readAuthorized` now requires a supported artifact kind and validates that kind against the private object reference before any object-store read; storage/provider/upload tests pass 104/104, API typecheck, and root lint pass.
790. G28/G31 readiness observability: database readiness failures now emit correlated structured error records, and unknown API routes emit bounded structured warning events without logging query strings or payloads; server configuration tests pass 12/12, API typecheck, and root lint pass.
791. G25 interview lifecycle data preservation: the authenticated interview route now forwards bounded `interviewer` and `meetingUrl` fields to the validated durable lifecycle service instead of silently discarding them; lifecycle and application-route tests pass 17/17, API typecheck, and root lint pass.
792. G25 multi-round interview preservation: the authenticated interview route now validates and forwards optional round numbers (1–100) into durable interview records; lifecycle/application-route tests pass 17/17, API typecheck, and root lint pass.
793. G31 build hygiene: the workspace production build passed for API, web, and queue packages, and `git diff --check` exited cleanly; Docker-backed PostgreSQL/Redis/S3 evidence remains environment-gated.
794. G28 application lifecycle traceability: unexpected failures across application answer, create/list/detail, interview, offer, scheduling, authorization, and status routes now emit correlation/user/application-scoped redacted logs; lifecycle/application-route tests pass 17/17, API typecheck, and root lint pass.
795. G24 outcome-route traceability: email outcome list, ingest, link, review, and apply failures now emit correlation/user/application-scoped redacted logs while preserving safe client responses; email outcome tests pass 31/31, API typecheck, lint, and diff hygiene pass.
796. G18 identity-boundary hardening: stable question identities now bound all webpage-controlled semantic, step, and occurrence components before persistence, preventing oversized labels/ARIA metadata from creating unbounded durable keys; shared form-intelligence tests pass 50/50, job-engine typecheck, and root lint pass.
797. G24/G27 OAuth connection traceability: email connection listing, consent, OAuth start/callback, and revocation failures now emit correlation/user-scoped redacted logs without exposing credential material; email connection/credential/OAuth tests pass 13/13, API typecheck, and root lint pass.
798. G19 infrastructure recheck: Docker CLI is installed but the daemon endpoint `npipe:////./pipe/docker_engine` is unavailable and the Docker config is access-denied; no new PostgreSQL/Redis/S3 integration result is counted, and unit/static work continues without fabrication.
799. G26/G28 analytics traceability: applications-over-time and lifecycle-funnel route failures now emit correlation/user-scoped redacted logs like the dashboard endpoint; analytics route tests pass 8/8, API typecheck, and root lint pass.
800. G19 retention race hardening: retention now uses owner-authorized deletion plus conditional tombstone updates and transaction-scoped object locking, preventing concurrent owner deletion from being overwritten or audited twice; storage/routes/retention tests pass 103/103 and API typecheck/diff checks pass.
801. G21 exact-attempt verification hardening: independent confirmation evidence now carries and validates the durable `ApplicationAttempt` identity through provider execution, automation payloads, and verification persistence, rejecting cross-attempt binding; focused submission/verification/handler tests pass 49/49, API typecheck, lint, and diff checks pass.
802. G19 retention implementation revalidated in the parent worktree after delegated change: owner-authorized deletion, conditional tombstone claiming, and retention audit behavior pass 103/103 focused tests with API typecheck clean; PostgreSQL/S3 integration remains unrun because Docker is unavailable.
803. G21 verification implementation revalidated in the parent worktree after delegated change: exact attempt identity is preserved from durable execution through verifier handler and evidence persistence; focused submission/verification/handler tests pass 49/49 with API typecheck/lint clean.
804. Post-G19/G21 workspace verification: API, web, and queue production builds passed, and `git diff --check` exited 0 after retention and exact-attempt changes; live infrastructure-backed certification remains unclaimed.
805. Full regression after G19/G21 changes: the configured non-infrastructure suite completed with 81 files passed, 36 explicitly skipped, 737 tests passed, and 132 skipped; no skipped infrastructure test is counted as passed.
806. G20/G28 application-queue traceability: quality-evaluation, Greenhouse-completion, and Lever-completion queue failures now emit correlation/user/application/provider-scoped redacted logs while preserving existing safe responses; application-route tests pass 3/3, API typecheck, root lint, and diff checks pass.
807. G18 control-boundary hardening: provider-neutral extraction now rejects empty, oversized, or control-bearing webpage field IDs before they reach provider ports; shared form, SSE, scheduler, queue, email, and analytics focused suites remain green.
808. G23 SSE replay hardening: durable replay/live-event delivery now deduplicates identified notification events by immutable event ID, preventing replay/live double emission while preserving unidentifiable event delivery.
809. G27 browser security consolidation: the existing browser-session manager now delegates initial navigation, explicit navigation, and request interception to the shared DNS-rebinding-safe browser policy API; duplicate manager-side DNS policy logic was removed. Session/security tests pass 18/18.
810. G22/G24/G26 focused regression: scheduler timezone validation, email outcome/connection safety, queue metrics normalization, and lifecycle analytics focused tests pass 99/99; queue and web typechecks pass.
811. G18/G27 shared security boundary: malformed webpage-controlled form IDs are rejected before provider interaction, while browser navigation and request interception now use one shared DNS-rebinding-safe policy implementation; form/session/security tests pass 75/75, package typechecks and root lint pass.
812. Workspace build after cross-cutting hardening: API, web, and queue production builds passed with 1,503 web modules transformed; `git diff --check` exited 0. Infrastructure-backed and final E2E certification remain unclaimed.
813. G22/G28/G29 automation-control race hardening: pause, resume, and stop now perform owner-scoped lookup and mutation under the same tenant transaction and per-user advisory lock, preventing concurrent control requests from acting on stale run state; automation operational/dispatcher tests pass 7/7, workspace typecheck and root lint pass. Live PostgreSQL concurrency evidence remains gated because the Docker engine is unavailable.
814. Post-control-change build verification: API, web, and queue production builds pass, including the 1,503-module web bundle; `git diff --check` reports no whitespace errors. No infrastructure or final E2E certification is inferred from this static/build evidence.
815. G18 resolver compatibility regression correction: an attempted owner requirement for PROFILE_DERIVED resolver calls was reverted after the full provider fixture suite showed that it violated the established provider-neutral adapter contract; Greenhouse/Lever/form-intelligence tests pass 63/63, and no unsupported completion claim was retained.
816. G19 infrastructure gate revalidated: `npm run test:s3`, `npm run test:database`, and `npm run test:queue` each returned their explicit `GATED` result because no reachable Docker daemon/PostgreSQL endpoint is available; zero infrastructure tests were counted as passed. The configured non-infrastructure suite exposed the reverted G18 contract issue, which is now corrected and covered by 63/63 provider/form tests.
817. Full regression after G18 compatibility correction and automation-control hardening: 82 test files passed, 36 were explicitly skipped, 747 tests passed, and 132 were skipped; the skipped infrastructure coverage remains unverified and no final E2E claim is made.
818. G19 checksum canonicalization: immutable document duplicate reconciliation, server metadata verification, and downloaded-byte integrity checks now compare SHA-256 values case-insensitively, matching the existing hexadecimal reference validation; storage/recovery/retention tests pass 86/86 with API typecheck and targeted ESLint clean.
819. G19 workspace verification after checksum hardening: API, web, and queue production builds pass with 1,503 web modules transformed, and `git diff --check` reports no whitespace errors; live S3/PostgreSQL/Redis and final E2E certification remain unclaimed.
820. G30/G31 evidence correction: `PRODUCTION_READINESS.md` now reflects the latest completed full regression (82 files passed, 36 explicitly skipped; 747 tests passed, 132 skipped) instead of the superseded stalled-run note; infrastructure-backed and final E2E claims remain explicitly gated.
